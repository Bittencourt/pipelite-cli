use anyhow::Result;

use crate::cli::config::ConfigCommands;
use crate::context::AppContext;

#[allow(unused_variables)]
pub fn run(ctx: &AppContext, cmd: &ConfigCommands) -> Result<()> {
    todo!("Config command implemented in Task 2")
}
