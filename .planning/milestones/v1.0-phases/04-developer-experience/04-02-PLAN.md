---
phase: 04-developer-experience
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/commands/orgs/create.rs
  - src/commands/orgs/update.rs
  - src/commands/orgs/delete.rs
  - src/commands/people/create.rs
  - src/commands/people/update.rs
  - src/commands/people/delete.rs
  - src/commands/activities/create.rs
  - src/commands/activities/update.rs
  - src/commands/activities/delete.rs
  - src/commands/pipelines/create.rs
  - src/commands/pipelines/update.rs
  - src/commands/pipelines/delete.rs
  - src/commands/stages/create.rs
  - src/commands/stages/update.rs
  - src/commands/stages/delete.rs
autonomous: true
requirements:
  - HEAD-03
  - INTR-01
  - INTR-02

must_haves:
  truths:
    - "All 6 entities' create commands prompt for required fields on TTY"
    - "All 6 entities' create commands fail with MissingInput listing all missing flags in headless mode"
    - "All 6 entities' mutation commands support --dry-run"
    - "All mutations can be performed entirely via flags without any prompts"
    - "FuzzySelect appears for org selection in people create, pipeline/stage in deals/stages create"
    - "--stdin and individual field flags are mutually exclusive on all create commands"
  artifacts:
    - path: "src/commands/orgs/create.rs"
      provides: "Interactive org create with prompts and dry-run"
      contains: "prompt::require_text"
    - path: "src/commands/people/create.rs"
      provides: "Interactive people create with org FuzzySelect"
      contains: "prompt::require_select"
    - path: "src/commands/activities/create.rs"
      provides: "Interactive activities create with dry-run"
      contains: "prompt::require_text"
    - path: "src/commands/pipelines/create.rs"
      provides: "Interactive pipelines create with dry-run"
      contains: "prompt::require_text"
    - path: "src/commands/stages/create.rs"
      provides: "Interactive stages create with pipeline FuzzySelect"
      contains: "prompt::require_select"
  key_links:
    - from: "src/commands/*/create.rs"
      to: "src/prompt.rs"
      via: "require_text and require_select for interactive mode"
      pattern: "prompt::(require_text|require_select|check_missing)"
    - from: "src/commands/*/create.rs"
      to: "src/dry_run.rs"
      via: "dry_run intercept before API call"
      pattern: "dry_run::render_dry_run"
    - from: "src/commands/*/delete.rs"
      to: "src/dry_run.rs"
      via: "dry_run delete intercept"
      pattern: "dry_run::render_dry_run_delete"
---

<objective>
Replicate the interactive prompt, headless validation, and dry-run pattern (proven with deals in Plan 01) across all remaining entities: organizations, people, activities, pipelines, and stages.

Purpose: Complete HEAD-03 (all mutations via flags) and extend INTR-01/INTR-02 (prompts with dropdowns) to every entity.
Output: All 15 create/update/delete commands across 5 entities support prompts, headless mode, and dry-run.
</objective>

<execution_context>
@/home/pedro/.claude/get-shit-done/workflows/execute-plan.md
@/home/pedro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-developer-experience/04-CONTEXT.md
@.planning/phases/04-developer-experience/04-RESEARCH.md
@.planning/phases/04-developer-experience/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- prompt.rs public API -->
```rust
// src/prompt.rs
pub fn require_text(flag_value: &Option<String>, field_name: &str, prompt_label: &str, missing: &mut Vec<String>, no_input: bool) -> Result<Option<String>>;
pub fn require_select(flag_value: &Option<String>, field_name: &str, prompt_label: &str, options: &[(String, String)], missing: &mut Vec<String>, no_input: bool) -> Result<Option<String>>;
pub fn optional_text(flag_value: &Option<String>, prompt_label: &str, no_input: bool) -> Result<Option<String>>;
pub fn optional_number(flag_value: &Option<f64>, prompt_label: &str, no_input: bool) -> Result<Option<f64>>;
pub fn check_missing(missing: &[String], usage_hint: &str) -> Result<(), CliError>;
```

<!-- From Plan 01 -- dry_run.rs public API -->
```rust
// src/dry_run.rs
pub fn render_dry_run(method: &str, url: &str, body: &serde_json::Value, format: &OutputFormat, color: bool) -> Result<()>;
pub fn render_dry_run_delete(entity: &str, id: &str, url: &str, format: &OutputFormat, color: bool) -> Result<()>;
```

<!-- AppContext fields (from Plan 01) -->
```rust
// src/context.rs
pub struct AppContext {
    pub config: AppConfig,
    pub client: PipeliteClient,
    pub output_format: OutputFormat,
    pub quiet: bool,
    pub verbose: bool,
    pub color: bool,
    pub no_input: bool,
    pub dry_run: bool,
}
```

<!-- Entity required fields reference -->
<!-- Orgs create: name (required) -->
<!-- People create: first_name (required), last_name (required), org (FuzzySelect) -->
<!-- Activities create: title (required), type_id (required) -->
<!-- Pipelines create: name (required) -->
<!-- Stages create: name (required), pipeline (required, FuzzySelect) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor orgs, people, and activities create/update/delete</name>
  <files>
    src/commands/orgs/create.rs,
    src/commands/orgs/update.rs,
    src/commands/orgs/delete.rs,
    src/commands/people/create.rs,
    src/commands/people/update.rs,
    src/commands/people/delete.rs,
    src/commands/activities/create.rs,
    src/commands/activities/update.rs,
    src/commands/activities/delete.rs
  </files>
  <action>
Follow the exact pattern established by deals in Plan 01. For each entity:

**Organizations (create.rs):**
- Required fields (prompt order): name (require_text)
- Optional fields: website (optional_text), industry (optional_text), notes (optional_text)
- check_missing with hint: "Usage: pipelite orgs create --name <name>"
- Dry-run: POST to /api/v1/organizations
- --stdin exclusivity check (same as deals)
- batch_create: add dry-run support

**Organizations (update.rs):**
- All fields optional. If no flags and TTY: prompt all as optional.
- If no flags and headless: "No fields to update" validation error.
- Dry-run: PUT to /api/v1/organizations/{id}

**Organizations (delete.rs):**
- Dry-run: render_dry_run_delete("organization", &args.id, ...)

**People (create.rs):**
- Required fields (prompt order): first_name (require_text), last_name (require_text)
- Optional fields: email (optional_text), phone (optional_text), org (require_select with FuzzySelect -- fetch orgs list from API, display as "name (id)"), notes (optional_text)
- Note: org is optional for people, so use optional_select pattern -- if TTY and not no_input, offer FuzzySelect for org with an extra "None/Skip" option at top. If headless, just use flag value or None.
- check_missing with hint: "Usage: pipelite people create --first-name <name> --last-name <name>"
- Dry-run: POST to /api/v1/people
- --stdin exclusivity check

**People (update.rs):**
- All fields optional. Same pattern as orgs update.
- Dry-run: PUT to /api/v1/people/{id}

**People (delete.rs):**
- Dry-run: render_dry_run_delete("person", &args.id, ...)

**Activities (create.rs):**
- Required fields (prompt order): title (require_text), type_id (require_text -- no FuzzySelect, just text input since activity types are not an entity we can list)
- Optional fields: deal (optional_text), due_at (optional_text), notes (optional_text)
- check_missing with hint: "Usage: pipelite activities create --title <title> --type <type_id>"
- Dry-run: POST to /api/v1/activities
- --stdin exclusivity check
- Note: Activities use individual-create loop for batch (no /batch endpoint). Add dry-run to the loop: show each payload.

**Activities (update.rs):**
- All fields optional. Handle --mark-done/--mark-undone as before (these are not prompted).
- If no flags and TTY: prompt optional fields (title, type_id, deal, due_at, notes). Do NOT prompt for mark_done/mark_undone (these are explicit action flags).
- Dry-run: PUT to /api/v1/activities/{id}. For raw value updates (mark-undone), show the raw JSON.

**Activities (delete.rs):**
- Dry-run: render_dry_run_delete("activity", &args.id, ...)

For ALL entities: import `use crate::prompt;` and `use crate::dry_run;`. Pass `ctx.no_input` to prompt functions and check `ctx.dry_run` before API calls.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo build 2>&1 && cargo test 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Orgs/people/activities create prompt for required fields on TTY, fail with MissingInput in headless
    - People create offers org FuzzySelect when on TTY
    - All 9 commands (3 entities x 3 operations) support --dry-run
    - --stdin exclusivity enforced on all create commands
    - All existing tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor pipelines and stages create/update/delete</name>
  <files>
    src/commands/pipelines/create.rs,
    src/commands/pipelines/update.rs,
    src/commands/pipelines/delete.rs,
    src/commands/stages/create.rs,
    src/commands/stages/update.rs,
    src/commands/stages/delete.rs
  </files>
  <action>
**Pipelines (create.rs):**
- Required fields (prompt order): name (require_text)
- Optional fields: default (boolean -- if TTY and not no_input, use dialoguer Confirm with default false; if headless, use flag value)
- check_missing with hint: "Usage: pipelite pipelines create --name <name>"
- Dry-run: POST to /api/v1/pipelines
- --stdin exclusivity check

**Pipelines (update.rs):**
- All fields optional. If no flags and TTY: prompt name (optional), default (Confirm).
- Dry-run: PUT to /api/v1/pipelines/{id}

**Pipelines (delete.rs):**
- Dry-run: render_dry_run_delete("pipeline", &args.id, ...)

**Stages (create.rs):**
- Required fields (prompt order): name (require_text), pipeline (require_select with FuzzySelect -- fetch pipelines list from API, display as "name (id)")
- Optional fields: stage_type (optional_text with hint "open, won, or lost"), color (optional_text), description (optional_text)
- check_missing with hint: "Usage: pipelite stages create --name <name> --pipeline <pipeline_id>"
- Dry-run: POST to /api/v1/stages
- --stdin exclusivity check
- Note: stages use individual-create loop for batch (no /batch endpoint). Same dry-run pattern as activities.

**Stages (update.rs):**
- All fields optional. If no flags and TTY: prompt name, stage_type, color, description as optional.
- Dry-run: PUT to /api/v1/stages/{id}

**Stages (delete.rs):**
- Dry-run: render_dry_run_delete("stage", &args.id, ...)

Same import pattern: `use crate::prompt;` and `use crate::dry_run;`.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo build 2>&1 && cargo test 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Pipelines/stages create prompt for required fields on TTY, fail with MissingInput in headless
    - Stages create offers pipeline FuzzySelect when on TTY
    - All 6 commands (2 entities x 3 operations) support --dry-run
    - --stdin exclusivity enforced on create commands
    - All existing tests pass
    - ALL 18 mutation commands (6 entities x 3) now fully support prompts, headless, and dry-run
  </done>
</task>

</tasks>

<verification>
- `cargo build` succeeds
- `cargo test` -- all tests pass
- Every entity's create.rs imports and uses prompt:: helpers
- Every entity's create/update/delete.rs checks ctx.dry_run before API calls
- grep confirms: `grep -r "prompt::require_text\|prompt::check_missing" src/commands/` shows hits in all 6 entity create files
- grep confirms: `grep -r "dry_run::render_dry_run" src/commands/` shows hits in all 18 mutation files
</verification>

<success_criteria>
- All 6 entities (deals + 5 new) support interactive prompts for create commands
- All 18 mutation commands support --dry-run
- All create commands enforce --stdin/flags mutual exclusivity
- Headless mode (--no-input or non-TTY) fails with exit code 2 listing ALL missing required flags
- FuzzySelect appears for: pipeline (deals create, stages create), org (people create)
</success_criteria>

<output>
After completion, create `.planning/phases/04-developer-experience/04-02-SUMMARY.md`
</output>
