---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - build.rs
  - src/main.rs
  - src/cli/mod.rs
  - src/cli/config.rs
  - src/cli/init.rs
  - src/config.rs
  - src/error.rs
  - src/output/mod.rs
  - tests/fixtures/valid_config.toml
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - CONF-01
  - CONF-04
  - ERRH-01
  - ERRH-02
  - UX-03

must_haves:
  truths:
    - "CLI binary compiles and runs with `cargo run -- --version` showing rich version info"
    - "Config file can be loaded from disk and merged with env var overrides"
    - "Config file is created with 0600 permissions on Unix"
    - "Errors display in structured format with type + message + hint on stderr"
    - "CLI exit codes are 1 for runtime errors and 2 for usage errors (clap default)"
  artifacts:
    - path: "src/cli/mod.rs"
      provides: "Cli struct with global flags, Commands enum with Init/Ping/Config subcommands"
      exports: ["Cli", "Commands", "OutputFormat"]
    - path: "src/config.rs"
      provides: "AppConfig struct, TOML load/save, env var merge, 0600 file permissions"
      exports: ["AppConfig", "load_config", "write_config", "config_path"]
    - path: "src/error.rs"
      provides: "CliError enum with Auth/Connection/Config variants, display_error formatter"
      exports: ["CliError", "display_error"]
    - path: "src/output/mod.rs"
      provides: "OutputFormat enum, TTY detection, quiet mode support"
      exports: ["OutputFormat"]
    - path: "build.rs"
      provides: "RUSTC_VERSION env var for rich version string"
  key_links:
    - from: "src/cli/mod.rs"
      to: "src/output/mod.rs"
      via: "OutputFormat shared type"
      pattern: "use crate::output::OutputFormat"
    - from: "src/config.rs"
      to: "Cargo.toml"
      via: "toml + toml_edit + serde dependencies"
      pattern: "toml::from_str|toml_edit::DocumentMut"
    - from: "src/error.rs"
      to: "stderr"
      via: "display_error writes structured errors to stderr"
      pattern: "eprintln!"
---

<objective>
Set up the Pipelite CLI project skeleton with all dependencies, module structure, and the three leaf modules that everything else depends on: CLI argument parsing (clap derive), configuration management (TOML load/save with 0600 permissions and env var precedence), and structured error handling (thiserror enums with formatted display on stderr). Also establishes the output abstraction with TTY detection and the rich version string via build.rs.

Purpose: Every command in Plan 02 depends on these types and modules. Building them first means Plan 02 can focus purely on command logic.
Output: Compilable binary with `--version`, `--help`, and all module stubs. Config and error modules fully functional with unit tests.
</objective>

<execution_context>
@/home/pedro/.claude/get-shit-done/workflows/execute-plan.md
@/home/pedro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project skeleton -- Cargo.toml, build.rs, module structure, CLI definitions</name>
  <files>Cargo.toml, build.rs, src/main.rs, src/cli/mod.rs, src/cli/config.rs, src/cli/init.rs, src/output/mod.rs</files>
  <behavior>
    - `cargo build` succeeds with all dependencies resolved
    - `cargo run -- --version` prints `pipelite 0.1.0 (rustc X.Y.Z, OS-ARCH)` format
    - `cargo run -- --help` shows "Manage your Pipelite CRM from the terminal" and after_help examples
    - `cargo run -- init --help` shows examples with `--url` and `--key` flags
    - `cargo run -- config --help` shows subcommands: show, set, get
    - Global flags `--format`, `--no-color`, `-q`, `-v` parse without error
    - `cargo run -- config set output.format json` parses the positional args (command handler can be a stub returning Ok)
  </behavior>
  <action>
1. Update `Cargo.toml` with all Phase 1 dependencies from RESEARCH.md Standard Stack section (clap 4.6 with derive+env features, tokio 1 with rt+macros, reqwest 0.13 with json+rustls-tls, serde 1.0 with derive, serde_json 1.0, toml 1.1, toml_edit 0.22, dialoguer 0.12, anyhow 1.0, thiserror 2.0, colored 3.1, indicatif 0.18, comfy-table 7.2). Add dev-dependencies: assert_cmd 2.0, predicates 3.0, tempfile 3.0.

2. Create `build.rs` that captures rustc version via `rustc --version` and sets `RUSTC_VERSION` env var for compile-time embedding. Use `std::process::Command` to run rustc.

3. Create `src/cli/mod.rs` with the full Cli struct (Parser) and Commands enum (Subcommand) following the RESEARCH.md code example exactly. Include:
   - `Cli` struct with global flags: `--format` (Option<OutputFormat>, global), `--no-color` (bool, global), `-q`/`--quiet` (bool, global), `-v`/`--verbose` (bool, global)
   - `Commands` enum: `Init(InitArgs)`, `Ping`, `Config(ConfigCommands)`
   - `build_version()` function using `option_env!("RUSTC_VERSION")` with fallback to "unknown"
   - `after_help` on every command with usage examples per user decision
   - NOTE: `build_version()` must return `&'static str`. Use `concat!` for the static parts and handle RUSTC_VERSION carefully. Since `std::env::consts::OS` and `std::env::consts::ARCH` are already `&str` constants, they work in format strings but NOT in `concat!`. Consider using a `lazy_static` or `once_cell` approach, or use `env!()` macro in build.rs to bake OS/ARCH at build time. Simplest approach: build.rs sets a single `BUILD_INFO` env var containing the full version string.

4. Create `src/cli/init.rs` with `InitArgs` struct (--url: Option<String>, --key: Option<String>).

5. Create `src/cli/config.rs` with `ConfigCommands` enum (Show, Set { key, value }, Get { key }).

6. Create `src/output/mod.rs` with `OutputFormat` enum (Json, Table, Csv, Plain) deriving Clone and clap::ValueEnum. Include a helper function `detect_format(explicit: Option<OutputFormat>) -> OutputFormat` that returns the explicit format if provided, otherwise Table if stdout is a TTY, otherwise Json.

7. Update `src/main.rs` to: declare modules (mod cli, mod config, mod error, mod output, mod api, mod commands, mod context), parse Cli, and dispatch commands with stub handlers that return `Ok(())`. Use `#[tokio::main(flavor = "current_thread")]`. Handle errors by calling `error::display_error` and exiting with code 1. Keep main.rs under 40 lines.

NOTE: For modules not yet created (api, commands, context), create minimal stub files so the project compiles:
- `src/api/mod.rs` with a placeholder comment
- `src/commands/mod.rs` with pub mod stubs
- `src/commands/init.rs`, `src/commands/ping.rs`, `src/commands/config.rs` with `pub async fn run(...) -> Result<()> { todo!() }` stubs
- `src/context.rs` with placeholder AppContext struct

The goal is: `cargo build` succeeds and `cargo run -- --version` works. Command dispatch can use `todo!()` for now -- Plan 01 Task 2 and Plan 02 will fill them in.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo build 2>&1 && cargo run -- --version 2>&1 && cargo run -- --help 2>&1 | head -20</automated>
  </verify>
  <done>Binary compiles. `--version` shows rich version string with rustc version. `--help` shows all subcommands with examples. All global flags defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Error types and config module with full test coverage</name>
  <files>src/error.rs, src/config.rs, tests/fixtures/valid_config.toml</files>
  <behavior>
    - CliError::Auth formats as "error: Authentication failed\n  {detail}\n  hint: {hint}"
    - CliError::Config formats similarly with "Configuration error" title
    - CliError::Connection formats with "Connection failed" title
    - display_error respects color=false (no ANSI codes in output)
    - AppConfig loads from a valid TOML file with [server], [output], [display] sections
    - AppConfig falls back to defaults when sections/keys are missing
    - PIPELITE_API_KEY env var overrides config file api_key
    - PIPELITE_SERVER_URL env var overrides config file server url
    - Config file is written with 0600 permissions (Unix)
    - config_path() returns ~/.pipelite/config.toml by default
    - PIPELITE_CONFIG env var overrides config path
    - generate_commented_config produces a TOML string with comments for every key
    - Config set with dotted path "output.format" modifies the correct key in TOML
  </behavior>
  <action>
1. Create `src/error.rs`:
   - Define `CliError` enum with `thiserror::Error` derive. Variants: `Auth { detail: String, hint: String }`, `Connection { detail: String, hint: String }`, `Config { detail: String, hint: String }`, `NotFound { detail: String, hint: String }`.
   - Implement `display_error(err: &anyhow::Error, color: bool)` that checks for `CliError` downcast and formats structured output. For generic anyhow errors, print `error: {err:#}`.
   - Use `colored` crate: "error" in red+bold, detail as-is, "hint" in dimmed. When `color=false`, use plain text.
   - Return appropriate exit codes: provide a `fn exit_code(err: &anyhow::Error) -> i32` that returns 2 for clap errors (misuse), 1 for everything else.

2. Create `src/config.rs`:
   - Define `AppConfig` with nested structs: `ServerConfig { url: String, api_key: String }`, `OutputConfig { format: Option<String> }`, `DisplayConfig { no_color: Option<bool> }`.
   - `AppConfig` has `server: ServerConfig`, `output: OutputConfig`, `display: DisplayConfig`. Derive `Serialize, Deserialize, Default`.
   - `config_path() -> Result<PathBuf>`: check `PIPELITE_CONFIG` env var first, then `~/.pipelite/config.toml` using `dirs::home_dir()` -- actually no, user decision says hardcoded `~/.pipelite/`. Use `std::env::var("HOME")` on Unix to get home dir.
   - `load_config(path: Option<&Path>) -> Result<AppConfig>`: load from path (or default path), deserialize with `toml::from_str`. If file doesn't exist, return Default. After loading, merge env vars: `PIPELITE_API_KEY` overrides `server.api_key`, `PIPELITE_SERVER_URL` overrides `server.url`.
   - `write_config(path: &Path, config: &AppConfig) -> Result<()>`: create parent dirs, use `OpenOptions::new().write(true).create(true).truncate(true).mode(0o600)` on Unix, write `generate_commented_config`.
   - `generate_commented_config(config: &AppConfig) -> String`: return a hardcoded template string (not toml_edit) with all keys and comments, interpolating actual values from the config struct. Follow the format from RESEARCH.md Config File Structure section.
   - `set_config_value(path: &Path, key: &str, value: &str) -> Result<()>`: use `toml_edit::DocumentMut` to parse existing file, split key on '.', navigate to correct table/key, set value, write back. This preserves comments.

3. Create `tests/fixtures/valid_config.toml` with a sample config for testing.

4. Add `#[cfg(test)] mod tests` in both `error.rs` and `config.rs`:
   - Error tests: verify each variant formats correctly with and without color
   - Config tests: `load_and_save` (round-trip), `env_var_precedence` (PIPELITE_API_KEY overrides file), `file_permissions` (0600 on created file using tempfile), `set_dotted_path` (toml_edit preserves structure), `default_config_path`, `config_path_env_override`.
   - Use `tempfile::TempDir` for file system tests.

NOTE: Update `src/main.rs` error handling to use the real `display_error` and `exit_code` functions instead of stubs.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo test -- error config 2>&1</automated>
  </verify>
  <done>Error types format correctly with structured output on stderr. Config loads from TOML, merges env vars (PIPELITE_API_KEY wins), writes with 0600 permissions. Config set preserves comments via toml_edit. All unit tests pass.</done>
</task>

</tasks>

<verification>
- `cargo build` succeeds with zero warnings (use `#[allow(dead_code)]` on stubs only)
- `cargo test` passes all unit tests in error and config modules
- `cargo run -- --version` shows rich version info
- `cargo run -- --help` shows all commands with after_help examples
- Config round-trip test: create config, read back, values match
- File permission test: created config file has 0600 mode
</verification>

<success_criteria>
- Binary compiles and CLI skeleton is fully functional (--version, --help, all subcommands parse)
- Error module provides structured formatting with color support on stderr
- Config module loads TOML, merges env vars with correct precedence, writes with 0600 permissions
- Config set preserves comments via toml_edit
- All unit tests green
- All command stubs compile (filled in by Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
