---
phase: 03-full-entity-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/models.rs
  - src/api/mod.rs
  - src/cli/mod.rs
  - src/cli/pipelines.rs
  - src/cli/stages.rs
  - src/commands/mod.rs
  - src/commands/pipelines/mod.rs
  - src/commands/pipelines/list.rs
  - src/commands/pipelines/get.rs
  - src/commands/pipelines/create.rs
  - src/commands/pipelines/update.rs
  - src/commands/pipelines/delete.rs
  - src/commands/stages/mod.rs
  - src/commands/stages/list.rs
  - src/commands/stages/get.rs
  - src/commands/stages/create.rs
  - src/commands/stages/update.rs
  - src/commands/stages/delete.rs
  - src/main.rs
  - tests/pipelines_integration.rs
  - tests/stages_integration.rs
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04
  - PIPE-05
  - STAG-01
  - STAG-02
  - STAG-03
  - STAG-04
  - STAG-05

must_haves:
  truths:
    - "User can run `pipelite pipelines list` and see pipelines in table format"
    - "User can create, get, update, and delete pipelines"
    - "User can run `pipelite stages list --pipeline <id>` and see stages for that pipeline"
    - "Running `pipelite stages list` without --pipeline fails with a helpful error"
    - "User can create a stage with --name and --pipeline (both required)"
    - "User can get, update, and delete stages by ID (no --pipeline needed)"
    - "Both entities support --stdin with individual-create loop"
  artifacts:
    - path: "src/api/models.rs"
      provides: "Pipeline, Stage structs + Create/Update payloads + table configs"
      contains: "struct Pipeline"
    - path: "src/cli/pipelines.rs"
      provides: "PipelinesCommands enum"
      exports: ["PipelinesCommands"]
    - path: "src/cli/stages.rs"
      provides: "StagesCommands enum"
      exports: ["StagesCommands"]
    - path: "src/commands/stages/list.rs"
      provides: "Stages list with required --pipeline validation"
      contains: "Missing required flag: --pipeline"
  key_links:
    - from: "src/main.rs"
      to: "src/commands/pipelines/mod.rs"
      via: "Commands::Pipelines match arm"
      pattern: "Commands::Pipelines"
    - from: "src/main.rs"
      to: "src/commands/stages/mod.rs"
      via: "Commands::Stages match arm"
      pattern: "Commands::Stages"
    - from: "src/commands/stages/list.rs"
      to: "src/api/mod.rs"
      via: "client.list_stages() with pipeline_id param"
      pattern: "list_stages"
    - from: "src/commands/stages/create.rs"
      to: "src/api/mod.rs"
      via: "client.create_stage() with pipeline_id in payload"
      pattern: "create_stage"
---

<objective>
Implement full CRUD for pipelines and stages entities, with stages requiring --pipeline on list and create.

Purpose: Pipelines and stages are logically related (stages belong to a pipeline). Both lack batch endpoints (use individual-create loop). The locked decision that `stages list` requires `--pipeline` is a key behavioral difference from other entities.
Output: Working `pipelite pipelines` and `pipelite stages` commands with all CRUD subcommands, --pipeline enforcement on stages, and integration tests.
</objective>

<execution_context>
@/home/pedro/.claude/get-shit-done/workflows/execute-plan.md
@/home/pedro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-full-entity-coverage/03-CONTEXT.md
@.planning/phases/03-full-entity-coverage/03-RESEARCH.md

<interfaces>
<!-- Same deals pattern. Key differences: no batch endpoints, stages list requires --pipeline. -->

From src/api/models.rs:
```rust
pub struct ApiSingleResponse<T> { pub data: T }
pub struct ApiListResponse<T> { pub data: Vec<T>, pub meta: PaginationMeta }
pub struct TableConfig { pub default_columns: Vec<&'static str> }
```

From src/api/mod.rs:
```rust
// All CRUD methods follow the deals pattern.
// NO batch endpoints for pipelines or stages.
// Stages list sends pipeline_id as required query param.
```

From src/commands/deals/create.rs (for individual-create loop pattern):
```rust
// Read JSON array from stdin -> loop individual creates -> progress -> partial failure summary
```

From CONTEXT.md locked decisions:
```
- stages list REQUIRES --pipeline <id> -- error with hint if omitted
- stages create REQUIRES --pipeline <id> as named flag
- stages get/update/delete take stage ID as positional (no --pipeline needed)
- Pipeline alias: "pl" (avoids collision with "p" for people)
- Stages alias: "s"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pipelines entity -- models, API client, CLI, commands, and tests</name>
  <files>src/api/models.rs, src/api/mod.rs, src/cli/pipelines.rs, src/cli/mod.rs, src/commands/pipelines/mod.rs, src/commands/pipelines/list.rs, src/commands/pipelines/get.rs, src/commands/pipelines/create.rs, src/commands/pipelines/update.rs, src/commands/pipelines/delete.rs, src/commands/mod.rs, src/main.rs, tests/pipelines_integration.rs</files>
  <action>
**1. Data models in src/api/models.rs** -- Add after Activity section:
- `Pipeline` struct: id (String), name (String), is_default (bool), owner_id (String), created_at (String), updated_at (String). Derive Debug, Clone, Serialize, Deserialize.
- `PipelineCreate` struct: name (String required), is_default Option<bool> with skip_serializing_if.
- `PipelineUpdate` struct: name (Option<String>), is_default (Option<bool>), both with skip_serializing_if.
- `pipelines_table_config()` returning TableConfig with default_columns: ["id", "name", "updated_at"]. (3 columns per CONTEXT.md decision.)
- Unit tests: `pipeline_deserializes_from_json`, `pipeline_create_required_only`.

**2. API client methods in src/api/mod.rs**:
- Add Pipeline, PipelineCreate, PipelineUpdate to use statement.
- `list_pipelines(&self, params: &PipelinesListParams) -> Result<ApiListResponse<Pipeline>>` -- GET /api/v1/pipelines
- `get_pipeline(&self, id: &str, expand: Option<&[String]>) -> Result<Pipeline>` -- unwrap ApiSingleResponse
- `create_pipeline(&self, data: &PipelineCreate) -> Result<Pipeline>` -- unwrap ApiSingleResponse
- `update_pipeline(&self, id: &str, data: &PipelineUpdate) -> Result<Pipeline>` -- unwrap ApiSingleResponse
- `delete_pipeline(&self, id: &str) -> Result<()>` -- handle_delete_response
- NO batch method.
- `PipelinesListParams` struct: limit, offset, expand. No entity-specific filters per CONTEXT.md. `to_query_pairs()` with limit, offset, expand.

**3. CLI args in src/cli/pipelines.rs** -- New file:
- `PipelinesCommands` enum: List, Get, Create, Update, Delete with after_help examples.
- `PipelinesListArgs`: --limit, --offset, --all, --fields, --expand. NO entity-specific filters per CONTEXT.md.
- `PipelinesGetArgs`: id positional, --fields, --expand.
- `PipelinesCreateArgs`: --name (Option, validated at runtime), --default (bool flag for is_default), --custom-field (Vec -- likely unused but keep for consistency), --stdin.
- `PipelinesUpdateArgs`: id positional, --name, --default (bool flag).
- `PipelinesDeleteArgs`: id positional.

**4. Register in src/cli/mod.rs**:
- Add `pub mod pipelines;`
- Add `use pipelines::PipelinesCommands;`
- Add to Commands enum: `#[command(subcommand, alias = "pl")] Pipelines(PipelinesCommands),` -- alias "pl" per research recommendation to avoid collision with "p" (people).

**5. Command handlers in src/commands/pipelines/** -- New directory, 6 files:
- `mod.rs`: dispatch function.
- `list.rs`: fetch_page/fetch_all with PipelinesListParams, pipelines_table_config().
- `get.rs`: client.get_pipeline(), render_single.
- `create.rs`: single_create validates --name required. batch_create uses individual-create loop (same pattern as activities plan). Include parse_custom_fields.
- `update.rs`: build PipelineUpdate from flags (--name, --default maps to is_default), client.update_pipeline().
- `delete.rs`: client.delete_pipeline().

**6. Register in src/commands/mod.rs**: Add `pub mod pipelines;`
**7. Wire in src/main.rs**: Add `Commands::Pipelines(ref cmd)` match arm.

**8. Integration tests in tests/pipelines_integration.rs**:
- `pipelines_help_shows_subcommands`
- `pipelines_list_help_shows_flags` -- checks --limit, --offset, --all
- `pipelines_create_help_shows_required_flags` -- checks --name, --default, --stdin
- `pipelines_get_without_id_fails_with_exit_code_2`
- `pipelines_delete_without_id_fails_with_exit_code_2`
- `pipelines_update_help_shows_optional_flags` -- checks --name, --default
- `main_help_shows_pipelines_subcommand`
  </action>
  <verify>
    <automated>cargo test --test pipelines_integration -- --nocapture && cargo test api::models::tests::pipeline -- --nocapture</automated>
  </verify>
  <done>All `pipelite pipelines` subcommands registered and working. Pipeline alias is "pl". Create validates --name as required. Default table columns: id, name, updated_at. Integration and model tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Stages entity -- models, API client, CLI, commands (with --pipeline enforcement), and tests</name>
  <files>src/api/models.rs, src/api/mod.rs, src/cli/stages.rs, src/cli/mod.rs, src/commands/stages/mod.rs, src/commands/stages/list.rs, src/commands/stages/get.rs, src/commands/stages/create.rs, src/commands/stages/update.rs, src/commands/stages/delete.rs, src/commands/mod.rs, src/main.rs, tests/stages_integration.rs</files>
  <action>
**1. Data models in src/api/models.rs** -- Add after Pipeline section:
- `Stage` struct: id (String), pipeline_id (String), name (String), description (Option<String>), color (Option<String>), r#type (String) -- note: "type" is a Rust keyword, use `#[serde(rename = "type")] pub stage_type: String` or `pub r#type: String`. Prefer `#[serde(alias = "type")] pub stage_type: String` -- actually simplest: use `#[serde(rename = "type")] pub stage_type: String` for serialization/deserialization. But for table output (serde_json::Value), the JSON field will be "type" which is what the API returns. So use: `pub r#type: String` which serde will serialize as "type" by default. Test this. Alternative: `#[serde(rename = "type")] pub stage_type: String`. This is cleaner for Rust code. Use this approach.
- Also: position (i64), created_at (String), updated_at (String). Derive Debug, Clone, Serialize, Deserialize.
- `StageCreate` struct: name (String required), pipeline_id (String required), description/color Option with skip_serializing_if. `#[serde(rename = "type")] pub stage_type: Option<String>` with skip_serializing_if (enum: open, won, lost; default: open).
- `StageUpdate` struct: name, description, color, stage_type all Option with skip_serializing_if.
- `stages_table_config()` returning TableConfig with default_columns: ["id", "name", "pipeline_id", "position"]. (4 columns per CONTEXT.md.)
- Unit tests: `stage_deserializes_from_json`, `stage_create_required_only`.

**2. API client methods in src/api/mod.rs**:
- Add Stage, StageCreate, StageUpdate to use statement.
- `list_stages(&self, params: &StagesListParams) -> Result<ApiListResponse<Stage>>` -- GET /api/v1/stages with pipeline_id query param
- `get_stage(&self, id: &str, expand: Option<&[String]>) -> Result<Stage>` -- unwrap ApiSingleResponse
- `create_stage(&self, data: &StageCreate) -> Result<Stage>` -- unwrap ApiSingleResponse
- `update_stage(&self, id: &str, data: &StageUpdate) -> Result<Stage>` -- unwrap ApiSingleResponse
- `delete_stage(&self, id: &str) -> Result<()>` -- handle_delete_response
- NO batch method.
- `StagesListParams` struct: pipeline_id (String -- required, not Option), limit, offset, expand. `to_query_pairs()` always includes pipeline_id.

**3. CLI args in src/cli/stages.rs** -- New file:
- `StagesCommands` enum: List, Get, Create, Update, Delete with after_help examples.
- `StagesListArgs`: --pipeline (Option<String> -- validated as required at runtime in the handler, NOT clap-required, so we can give a good error message), --limit, --offset, --all, --fields, --expand.
- `StagesGetArgs`: id positional, --fields, --expand. NO --pipeline (ID is unique per user decision).
- `StagesCreateArgs`: --name (Option), --pipeline (Option, validated at runtime as required), --color, --type (Option<String>, open/won/lost), --description, --custom-field (Vec), --stdin.
- `StagesUpdateArgs`: id positional, --name, --color, --type, --description. NO --pipeline (can't change pipeline of existing stage).
- `StagesDeleteArgs`: id positional. NO --pipeline.

**4. Register in src/cli/mod.rs**:
- Add `pub mod stages;`
- Add `use stages::StagesCommands;`
- Add to Commands enum: `#[command(subcommand, alias = "s")] Stages(StagesCommands),`

**5. Command handlers in src/commands/stages/** -- New directory, 6 files:
- `mod.rs`: dispatch function.

- `list.rs`: **CRITICAL** -- validate --pipeline is provided BEFORE making API call:
  ```rust
  let pipeline_id = args.pipeline.as_ref().ok_or_else(|| CliError::Validation {
      detail: "Missing required flag: --pipeline".to_string(),
      hint: "Usage: pipelite stages list --pipeline <pipeline_id>".to_string(),
  })?;
  ```
  Then proceed with fetch_page/fetch_all using StagesListParams (which takes pipeline_id as String, not Option).

- `get.rs`: client.get_stage(), render_single. No --pipeline needed.
- `create.rs`: single_create validates both --name and --pipeline required. batch_create uses individual-create loop. Include parse_custom_fields.
- `update.rs`: build StageUpdate from flags (--name, --color, --type maps to stage_type, --description). client.update_stage().
- `delete.rs`: client.delete_stage().

**6. Register in src/commands/mod.rs**: Add `pub mod stages;`
**7. Wire in src/main.rs**: Add `Commands::Stages(ref cmd)` match arm.

**8. Integration tests in tests/stages_integration.rs**:
- `stages_help_shows_subcommands`
- `stages_list_help_shows_pipeline_flag` -- checks --pipeline, --limit, --offset
- `stages_create_help_shows_required_flags` -- checks --name, --pipeline, --color, --type
- `stages_get_without_id_fails_with_exit_code_2`
- `stages_delete_without_id_fails_with_exit_code_2`
- `stages_update_help_shows_optional_flags` -- checks --name, --color, --type, --description
- `main_help_shows_stages_subcommand`
- `stages_list_without_config_fails_with_exit_code_1` -- running `pipelite stages list --pipeline pl_123` fails at runtime (no config), NOT exit code 2 (args are valid)
  </action>
  <verify>
    <automated>cargo test --test stages_integration -- --nocapture && cargo test api::models::tests::stage -- --nocapture</automated>
  </verify>
  <done>All `pipelite stages` subcommands registered and working. `stages list` requires --pipeline flag with validation error if omitted. `stages create` requires both --name and --pipeline. `stages get/update/delete` use ID only (no --pipeline). Alias is "s". Default table columns: id, name, pipeline_id, position. Integration and model tests pass.</done>
</task>

</tasks>

<verification>
- `cargo test` -- all tests pass (existing + pipelines + stages)
- `cargo build` -- compiles without warnings
- `pipelite --help` shows pipelines and stages subcommands
- `pipelite pipelines --help` shows list/get/create/update/delete
- `pipelite stages --help` shows list/get/create/update/delete
- `pipelite stages list --help` shows --pipeline flag
</verification>

<success_criteria>
- Pipelines and Stages CRUD fully wired end-to-end
- Pipelines alias "pl", Stages alias "s"
- `stages list` validates --pipeline is required at runtime (CliError::Validation, not clap error)
- `stages create` validates both --name and --pipeline required
- `stages get/update/delete` take only stage ID (no --pipeline needed)
- Both entities support --stdin with individual-create loop
- Default table columns match CONTEXT.md: Pipelines (id, name, updated_at), Stages (id, name, pipeline_id, position)
- All integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-full-entity-coverage/03-03-SUMMARY.md`
</output>
