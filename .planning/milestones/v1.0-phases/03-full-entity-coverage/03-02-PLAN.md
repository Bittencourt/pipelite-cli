---
phase: 03-full-entity-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/models.rs
  - src/api/mod.rs
  - src/cli/mod.rs
  - src/cli/activities.rs
  - src/commands/mod.rs
  - src/commands/activities/mod.rs
  - src/commands/activities/list.rs
  - src/commands/activities/get.rs
  - src/commands/activities/create.rs
  - src/commands/activities/update.rs
  - src/commands/activities/delete.rs
  - src/main.rs
  - tests/activities_integration.rs
autonomous: true
requirements:
  - ACTV-01
  - ACTV-02
  - ACTV-03
  - ACTV-04
  - ACTV-05

must_haves:
  truths:
    - "User can run `pipelite activities list` and see activities in table format"
    - "User can filter activities with --deal, --type, and --done flags"
    - "User can run `pipelite activities get <id>` and see full activity details"
    - "User can create an activity with --title and --type (required)"
    - "User can update an activity and mark it done with --mark-done"
    - "User can delete an activity"
    - "Batch create via --stdin uses individual-create loop with progress"
  artifacts:
    - path: "src/api/models.rs"
      provides: "Activity struct + Create/Update payloads + table config"
      contains: "struct Activity"
    - path: "src/cli/activities.rs"
      provides: "ActivitiesCommands enum"
      exports: ["ActivitiesCommands"]
    - path: "src/commands/activities/mod.rs"
      provides: "Activity command dispatch"
    - path: "src/commands/activities/create.rs"
      provides: "Single create + individual-create loop for --stdin"
      contains: "batch_create_loop"
  key_links:
    - from: "src/main.rs"
      to: "src/commands/activities/mod.rs"
      via: "Commands::Activities match arm"
      pattern: "Commands::Activities"
    - from: "src/commands/activities/create.rs"
      to: "src/api/mod.rs"
      via: "client.create_activity() in loop"
      pattern: "create_activity"
    - from: "src/commands/activities/list.rs"
      to: "src/api/mod.rs"
      via: "client.list_activities()"
      pattern: "list_activities"
---

<objective>
Implement full CRUD for the activities entity with individual-create loop fallback for batch operations.

Purpose: Activities is the most complex entity (type_id mapping, completed_at for done status, no batch endpoint). Implementing it separately keeps plan scope manageable.
Output: Working `pipelite activities` command with list, get, create, update, delete subcommands, --done filter, --mark-done/--mark-undone on update, and individual-create loop for --stdin.
</objective>

<execution_context>
@/home/pedro/.claude/get-shit-done/workflows/execute-plan.md
@/home/pedro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-full-entity-coverage/03-CONTEXT.md
@.planning/phases/03-full-entity-coverage/03-RESEARCH.md

<interfaces>
<!-- Same patterns as Plan 01 -- replicate from deals. Key difference: NO batch endpoint. -->

From src/api/models.rs:
```rust
pub struct ApiSingleResponse<T> { pub data: T }
pub struct ApiListResponse<T> { pub data: Vec<T>, pub meta: PaginationMeta }
pub struct TableConfig { pub default_columns: Vec<&'static str> }
```

From src/api/mod.rs:
```rust
// All CRUD methods follow the deals pattern.
// CRITICAL: NO batch endpoint for activities. --stdin must use individual-create loop.
// Pattern for map_request_error, handle_response, handle_delete_response -- reuse existing.
```

From src/commands/deals/create.rs:
```rust
// batch_create() pattern -- reads stdin JSON array, validates terminal, parses Vec<EntityCreate>
// For activities: loop individual creates instead of batch endpoint
// Progress: eprint!("\rCreating {}/{}...", i + 1, total)
// Partial failure: continue processing, report summary, non-zero exit on any failure
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activities data models, API client, and CLI definition</name>
  <files>src/api/models.rs, src/api/mod.rs, src/cli/activities.rs, src/cli/mod.rs</files>
  <action>
**1. Data models in src/api/models.rs** -- Add after Person section:
- `Activity` struct: id (String), title (String), type_id (String), deal_id (Option<String>), owner_id (String), due_at (Option<String>), completed_at (Option<String>), notes (Option<String>), custom_fields (Option<serde_json::Value>), created_at (String), updated_at (String). Derive Debug, Clone, Serialize, Deserialize.
- `ActivityCreate` struct: title (String required), type_id (String required), deal_id/owner_id/due_at/notes/custom_fields all Option with skip_serializing_if.
- `ActivityUpdate` struct: all fields Option with skip_serializing_if. Include completed_at (Option<Option<String>>) -- outer Option means "field not provided", inner Option means "set to null" (mark undone). Use `#[serde(skip_serializing_if = "Option::is_none")]` on outer. For simplicity, just use Option<String> and set to empty string or current time -- actually use Option<String> with skip_serializing_if and handle the null-clearing via a separate serde_json::Value approach in the update command handler instead. Keep the struct simple: completed_at as Option<String> with skip_serializing_if.
- `activities_table_config()` returning TableConfig with default_columns: ["id", "title", "type_id", "deal_id", "due_at", "completed_at"]. Note: use real API field names, NOT the CONTEXT.md names (type not type_id, subject not title, etc. were user-facing names -- code uses API names).
- Unit tests: `activity_deserializes_from_json`, `activity_create_required_only_serializes`.

**2. API client methods in src/api/mod.rs**:
- Add Activity, ActivityCreate, ActivityUpdate to use statement.
- `list_activities(&self, params: &ActivitiesListParams) -> Result<ApiListResponse<Activity>>` -- GET /api/v1/activities
- `get_activity(&self, id: &str, expand: Option<&[String]>) -> Result<Activity>` -- unwrap ApiSingleResponse
- `create_activity(&self, data: &ActivityCreate) -> Result<Activity>` -- unwrap ApiSingleResponse
- `update_activity(&self, id: &str, data: &ActivityUpdate) -> Result<Activity>` -- unwrap ApiSingleResponse
- `delete_activity(&self, id: &str) -> Result<()>` -- handle_delete_response
- NO batch_create_activities method -- batch is handled via loop in the command handler.
- `ActivitiesListParams` struct: type_id (Option<String>), deal_id (Option<String>), owner_id (Option<String>), done (Option<bool>), limit, offset, expand. `to_query_pairs()` maps type_id, deal_id, owner_id as query params. For `done`, try sending "completed=true/false" -- if API ignores it, client-side filtering will be done in the list command.

**3. CLI args in src/cli/activities.rs** -- New file:
- `ActivitiesCommands` enum: List, Get, Create, Update, Delete with after_help examples.
- `ActivitiesListArgs`: --type (maps to type_id), --deal (maps to deal_id), --owner, --done (bool flag), --limit, --offset, --all, --fields, --expand.
- `ActivitiesGetArgs`: id positional, --fields, --expand.
- `ActivitiesCreateArgs`: --title (Option), --type (Option, maps to type_id), --deal, --due-at, --notes, --custom-field (Vec), --stdin.
- `ActivitiesUpdateArgs`: id positional, --title, --type, --deal, --due-at, --notes, --custom-field (Vec), --completed-at (explicit datetime), --mark-done (bool flag, sets completed_at to current UTC time), --mark-undone (bool flag, sends completed_at: null to clear it).
- `ActivitiesDeleteArgs`: id positional.

**4. Register in src/cli/mod.rs**:
- Add `pub mod activities;`
- Add `use activities::ActivitiesCommands;`
- Add to Commands enum: `#[command(subcommand, alias = "a")] Activities(ActivitiesCommands),`
  </action>
  <verify>
    <automated>cargo test api::models::tests::activity -- --nocapture && cargo build 2>&1 | head -20</automated>
  </verify>
  <done>Activity models serialize/deserialize correctly, API client methods compile, CLI args are defined with all flags per CONTEXT.md decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Activities command handlers, main.rs wiring, and integration tests</name>
  <files>src/commands/activities/mod.rs, src/commands/activities/list.rs, src/commands/activities/get.rs, src/commands/activities/create.rs, src/commands/activities/update.rs, src/commands/activities/delete.rs, src/commands/mod.rs, src/main.rs, tests/activities_integration.rs</files>
  <action>
**1. Command handlers in src/commands/activities/** -- New directory, 6 files:

- `mod.rs`: dispatch function matching ActivitiesCommands variants to handlers.

- `list.rs`: fetch_page and fetch_all with auto-pagination using ActivitiesListParams and activities_table_config(). If --done flag is set and server returns unfiltered results (done filter not supported), apply client-side filtering: keep only items where completed_at is not null (for --done) or is null (for no --done). Actually, simpler: --done is a boolean presence flag. When present, filter to completed_at != null. When absent, show all.

- `get.rs`: client.get_activity(), render_single with all columns.

- `create.rs`:
  - `single_create`: validate --title and --type are both provided (required). Map --type to type_id field. Build ActivityCreate, call client.create_activity().
  - `batch_create`: NO batch endpoint. Implement individual-create loop pattern:
    1. Read JSON array from stdin into Vec<ActivityCreate>
    2. Loop with progress: `eprint!("\rCreating {}/{}...", i + 1, total)`
    3. On success, collect created items
    4. On failure, increment failed counter, eprintln error for that item, continue
    5. After loop, render all created items using output::render_list
    6. If any failed: eprintln summary "Created X/Y. Z failed (see errors above)." and return Err to trigger non-zero exit
  - Include parse_custom_fields (copy from deals create.rs).

- `update.rs`: Build ActivityUpdate from flags. Handle --mark-done by setting completed_at to current UTC datetime (use `chrono::Utc::now().to_rfc3339()` -- but chrono is not a dependency. Instead use a simple approach: build a serde_json::Value manually and set "completed_at" to current time string. Actually, simpler: construct the time with `std::time::SystemTime` formatted as ISO 8601 -- or just use a basic format. Recommended: use the `time` crate if available, otherwise manually format "YYYY-MM-DDTHH:MM:SSZ" from SystemTime. Actually simplest: add the completed_at field to ActivityUpdate as Option<String> and in the command handler, if --mark-done set it to a hardcoded-looking format. Use `humantime::format_rfc3339(std::time::SystemTime::now())` -- but humantime may not be available. Just use: `let now = std::time::SystemTime::now().duration_since(std::time::UNIX_EPOCH).unwrap(); format!("{}",  now.as_secs())` -- no, the API expects ISO datetime. Simplest solution without new deps: use `chrono` if it's already a dep, otherwise just send the current time formatted manually. Check Cargo.toml -- the project uses `chrono` for format_value in output. So use `chrono::Utc::now().to_rfc3339()` for --mark-done. For --mark-undone, need to send completed_at: null. Handle this by building a serde_json::Value payload instead of the typed struct when --mark-undone is set, inserting `"completed_at": null` explicitly.

- `delete.rs`: client.delete_activity(), print confirmation.

**2. Register in src/commands/mod.rs**: Add `pub mod activities;`

**3. Wire in src/main.rs**: Add `Commands::Activities(ref cmd)` match arm.

**4. Integration tests in tests/activities_integration.rs**:
- `activities_help_shows_subcommands`
- `activities_list_help_shows_filter_flags` -- checks --type, --deal, --done, --limit, --offset
- `activities_create_help_shows_required_flags` -- checks --title, --type, --stdin
- `activities_get_help_shows_id_argument`
- `activities_get_without_id_fails_with_exit_code_2`
- `activities_delete_without_id_fails_with_exit_code_2`
- `activities_update_help_shows_flags` -- checks --title, --type, --mark-done, --mark-undone, --completed-at
- `main_help_shows_activities_subcommand`
  </action>
  <verify>
    <automated>cargo test --test activities_integration -- --nocapture</automated>
  </verify>
  <done>All `pipelite activities` subcommands are registered and working. Create validates --title and --type as required. --stdin uses individual-create loop with progress and partial failure handling. --mark-done sets completed_at, --mark-undone clears it. Integration tests pass.</done>
</task>

</tasks>

<verification>
- `cargo test` -- all tests pass (existing + activities)
- `cargo build` -- compiles without warnings
- `pipelite activities --help` shows list/get/create/update/delete
- `pipelite activities list --help` shows --type, --deal, --done filters
- `pipelite activities update --help` shows --mark-done, --mark-undone
</verification>

<success_criteria>
- Activities CRUD fully wired end-to-end
- --stdin uses individual-create loop (NOT batch endpoint) with "Creating X/Y..." progress and partial failure reporting
- --done filter on list works (client-side if API doesn't support it)
- --mark-done on update sets completed_at to current time
- --mark-undone on update clears completed_at (sends null)
- Default table columns: id, title, type_id, deal_id, due_at, completed_at
- All integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-full-entity-coverage/03-02-SUMMARY.md`
</output>
