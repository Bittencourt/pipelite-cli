---
phase: 07-batch-operations-for-all-entities
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/cli/pipelines.rs
  - src/cli/stages.rs
  - src/cli/workflows.rs
  - src/commands/pipelines/update.rs
  - src/commands/pipelines/delete.rs
  - src/commands/stages/update.rs
  - src/commands/stages/delete.rs
  - src/commands/workflows/update.rs
  - src/commands/workflows/delete.rs
autonomous: true
requirements: [BATCH-01, BATCH-02, BATCH-03]

must_haves:
  truths:
    - "pipelite pipelines update --stdin processes JSON array of pipeline updates with id fields"
    - "pipelite pipelines delete id1 id2 id3 deletes multiple pipelines with confirmation"
    - "pipelite stages update --stdin processes JSON array of stage updates with id fields"
    - "pipelite stages delete id1 id2 id3 deletes multiple stages with confirmation"
    - "pipelite workflows update --stdin processes JSON array of workflow updates with id fields"
    - "pipelite workflows delete id1 id2 id3 deletes multiple workflows with confirmation"
    - "All batch ops use continue-on-error via BatchOutcome"
  artifacts:
    - path: "src/cli/pipelines.rs"
      provides: "PipelinesUpdateArgs with --stdin, PipelinesDeleteArgs with ids Vec and --stdin"
      contains: "required_unless_present"
    - path: "src/cli/stages.rs"
      provides: "StagesUpdateArgs with --stdin, StagesDeleteArgs with ids Vec and --stdin"
      contains: "required_unless_present"
    - path: "src/cli/workflows.rs"
      provides: "WorkflowsUpdateArgs with --stdin, WorkflowsDeleteArgs with ids Vec and --stdin"
      contains: "required_unless_present"
    - path: "src/commands/pipelines/update.rs"
      provides: "batch_update function"
      contains: "async fn batch_update"
    - path: "src/commands/stages/update.rs"
      provides: "batch_update function"
      contains: "async fn batch_update"
    - path: "src/commands/workflows/update.rs"
      provides: "batch_update function"
      contains: "async fn batch_update"
  key_links:
    - from: "src/commands/pipelines/update.rs"
      to: "src/batch.rs"
      via: "BatchOutcome::finalize"
      pattern: "batch::BatchOutcome"
    - from: "src/commands/pipelines/delete.rs"
      to: "src/batch.rs"
      via: "BatchOutcome::finalize"
      pattern: "batch::BatchOutcome"
    - from: "src/commands/stages/update.rs"
      to: "src/batch.rs"
      via: "BatchOutcome::finalize"
      pattern: "batch::BatchOutcome"
    - from: "src/commands/stages/delete.rs"
      to: "src/batch.rs"
      via: "BatchOutcome::finalize"
      pattern: "batch::BatchOutcome"
    - from: "src/commands/workflows/update.rs"
      to: "src/batch.rs"
      via: "BatchOutcome::finalize"
      pattern: "batch::BatchOutcome"
    - from: "src/commands/workflows/delete.rs"
      to: "src/batch.rs"
      via: "BatchOutcome::finalize"
      pattern: "batch::BatchOutcome"
---

<objective>
Replicate the deals batch update and batch delete pattern to pipelines, stages, and workflows.

Purpose: Complete batch operations coverage for the remaining 3 entity types.
Output: Batch update + batch delete working for pipelines, stages, and workflows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-batch-operations-for-all-entities/07-CONTEXT.md
@.planning/phases/07-batch-operations-for-all-entities/07-01-SUMMARY.md

<interfaces>
<!-- Reference: deals implementation from Plan 01 -->

From src/batch.rs:
```rust
pub struct BatchOutcome { pub total: usize, pub succeeded: usize, pub failed: usize }
impl BatchOutcome {
    pub fn new(total: usize) -> Self;
    pub fn record_success(&mut self);
    pub fn record_failure(&mut self, index: usize, id: &str, err: &dyn std::fmt::Display);
    pub fn finalize(self, entity_name: &str, operation: &str) -> Result<()>;
}
pub fn read_stdin_json<T: serde::de::DeserializeOwned>(entity_hint: &str) -> Result<Vec<T>>;
```

API client methods:
```rust
// Pipelines
pub async fn update_pipeline(&self, id: &str, data: &PipelineUpdate) -> Result<Pipeline>;
pub async fn delete_pipeline(&self, id: &str) -> Result<()>;
// Stages
pub async fn update_stage(&self, id: &str, data: &StageUpdate) -> Result<Stage>;
pub async fn delete_stage(&self, id: &str) -> Result<()>;
// Workflows
pub async fn update_workflow(&self, id: &str, data: &WorkflowUpdate) -> Result<Workflow>;
pub async fn delete_workflow(&self, id: &str) -> Result<()>;
```

Cache keys: `KEY_PIPELINES`, `KEY_STAGES`, `KEY_WORKFLOWS`
Note: Pipelines also invalidates `stages_` prefix: `cache.invalidate_prefix("stages_")`

API URLs: `/api/v1/pipelines/{id}`, `/api/v1/stages/{id}`, `/api/v1/workflows/{id}`

Table configs: `pipelines_table_config()`, `stages_table_config()`, `workflows_table_config()`

IMPORTANT for workflows: WorkflowsDeleteArgs currently has a `pub force: bool` field. When converting to batch delete, keep `force` as an alternative to the confirmation prompt (if force is set, skip confirmation). The batch_delete confirmation prompt should be skipped when `args.force` is true OR `ctx.no_input` is true.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Batch update + delete for pipelines and stages (CLI args + handlers)</name>
  <files>src/cli/pipelines.rs, src/cli/stages.rs, src/commands/pipelines/update.rs, src/commands/pipelines/delete.rs, src/commands/stages/update.rs, src/commands/stages/delete.rs</files>
  <read_first>
    - src/commands/deals/update.rs (reference batch_update implementation from Plan 01)
    - src/commands/deals/delete.rs (reference batch_delete implementation from Plan 01)
    - src/cli/deals.rs (reference CLI args changes from Plan 01)
    - src/cli/pipelines.rs (current PipelinesUpdateArgs and PipelinesDeleteArgs)
    - src/cli/stages.rs (current StagesUpdateArgs and StagesDeleteArgs)
    - src/commands/pipelines/update.rs (current handler — note: already has batch_create logic)
    - src/commands/pipelines/delete.rs (current single-delete handler)
    - src/commands/stages/update.rs (current single-update handler)
    - src/commands/stages/delete.rs (current single-delete handler)
    - src/api/models.rs (PipelineUpdate and StageUpdate structs)
  </read_first>
  <action>
Apply the deals batch pattern to pipelines and stages:

**CLI Args (src/cli/pipelines.rs):**
- Change `PipelinesUpdateArgs.id: String` to `id: Option<String>` with `#[arg(required_unless_present = "stdin")]`
- Add `#[arg(long)] pub stdin: bool` to PipelinesUpdateArgs
  NOTE: PipelinesUpdateArgs already has no `stdin` field. The `stdin` on PipelinesCreateArgs is separate.
- Change `PipelinesDeleteArgs.id: String` to `ids: Vec<String>` with `#[arg(required_unless_present = "stdin")]`
- Add `#[arg(long)] pub stdin: bool` to PipelinesDeleteArgs
- Keep existing ArgValueCandidates
- Update after_help examples

**CLI Args (src/cli/stages.rs):**
- Same changes as above for StagesUpdateArgs and StagesDeleteArgs

**Update handlers (src/commands/pipelines/update.rs and src/commands/stages/update.rs):**
Follow deals/update.rs exactly. Extract id with `args.id.as_deref().ok_or_else(|| CliError::Validation { detail: "Missing ID".to_string(), hint: "Provide an ID or use --stdin.".to_string() })?` in single-update path. Do NOT use `.unwrap()` -- CLAUDE.md forbids it in production code.

Entity-specific values for pipelines:
- `PipelineUpdate`, `ctx.client.update_pipeline`, `KEY_PIPELINES`, `pipelines_table_config()`
- Entity name: `"pipeline"`, API URL: `/api/v1/pipelines/{id}`
- IMPORTANT: After batch update success, also call `cache.invalidate_prefix("stages_")` (matching existing pipeline mutation cache pattern from pipelines/create.rs)

Entity-specific values for stages:
- `StageUpdate`, `ctx.client.update_stage`, `KEY_STAGES`, `stages_table_config()`
- Entity name: `"stage"`, API URL: `/api/v1/stages/{id}`

**Delete handlers (src/commands/pipelines/delete.rs and src/commands/stages/delete.rs):**
Same deals pattern. **CRITICAL:** dry-run check MUST come BEFORE confirmation prompt (per D-05).
- Pipelines: `"Delete {} pipeline(s)?"`, `ctx.client.delete_pipeline`, `KEY_PIPELINES`, `"pipeline"`
  Also invalidate `cache.invalidate_prefix("stages_")` on success
- Stages: `"Delete {} stage(s)?"`, `ctx.client.delete_stage`, `KEY_STAGES`, `"stage"`
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/pipelines.rs PipelinesUpdateArgs contains `pub id: Option<String>` and `pub stdin: bool`
    - src/cli/pipelines.rs PipelinesDeleteArgs contains `pub ids: Vec<String>` and `pub stdin: bool`
    - src/cli/stages.rs StagesUpdateArgs contains `pub id: Option<String>` and `pub stdin: bool`
    - src/cli/stages.rs StagesDeleteArgs contains `pub ids: Vec<String>` and `pub stdin: bool`
    - src/commands/pipelines/update.rs contains `async fn batch_update`
    - src/commands/pipelines/update.rs uses `ok_or_else` with CliError (NOT unwrap) for args.id extraction
    - src/commands/pipelines/delete.rs contains `async fn batch_delete`
    - src/commands/pipelines/delete.rs contains `invalidate_prefix("stages_")`
    - src/commands/stages/update.rs contains `async fn batch_update`
    - src/commands/stages/update.rs uses `ok_or_else` with CliError (NOT unwrap) for args.id extraction
    - src/commands/stages/delete.rs contains `async fn batch_delete`
    - `cargo check` succeeds
  </acceptance_criteria>
  <done>
    Pipelines and stages have batch update and batch delete. Pipeline mutations properly invalidate stages cache. No unwrap() in production code. cargo check passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Batch update + delete for workflows (CLI args + handlers)</name>
  <files>src/cli/workflows.rs, src/commands/workflows/update.rs, src/commands/workflows/delete.rs</files>
  <read_first>
    - src/commands/deals/update.rs (reference batch_update implementation)
    - src/commands/deals/delete.rs (reference batch_delete implementation)
    - src/cli/deals.rs (reference CLI args)
    - src/cli/workflows.rs (current WorkflowsUpdateArgs and WorkflowsDeleteArgs — NOTE: DeleteArgs has force field)
    - src/commands/workflows/update.rs (current single-update handler)
    - src/commands/workflows/delete.rs (current single-delete handler — NOTE: has confirmation prompt with --force)
    - src/api/models.rs (WorkflowUpdate struct fields)
  </read_first>
  <action>
Apply the deals batch pattern to workflows. NOTE: Workflows delete already has a `--force` flag and confirmation prompt that must be preserved.

**CLI Args (src/cli/workflows.rs):**
- Change `WorkflowsUpdateArgs.id: String` to `id: Option<String>` with `#[arg(required_unless_present = "stdin")]`
- Add `#[arg(long)] pub stdin: bool` to WorkflowsUpdateArgs
- Change `WorkflowsDeleteArgs.id: String` to `ids: Vec<String>` with `#[arg(required_unless_present = "stdin")]`
- Add `#[arg(long)] pub stdin: bool` to WorkflowsDeleteArgs
- KEEP the existing `pub force: bool` field on WorkflowsDeleteArgs
- Update after_help examples

**Update handler (src/commands/workflows/update.rs):**
Extract id with `args.id.as_deref().ok_or_else(|| CliError::Validation { detail: "Missing workflow ID".to_string(), hint: "Provide a workflow ID or use --stdin.".to_string() })?` in single-update path. Do NOT use `.unwrap()` -- CLAUDE.md forbids it in production code.

Entity-specific values:
- `WorkflowUpdate`, `ctx.client.update_workflow`, `KEY_WORKFLOWS`, `workflows_table_config()`
- Entity name: `"workflow"`, API URL: `/api/v1/workflows/{id}`

**Delete handler (src/commands/workflows/delete.rs):**
The current handler already has a confirmation prompt with `--force` override. Extend it:
1. Add `collect_ids` function (same pattern, but also validate mutual exclusivity of --stdin and positional IDs)
2. For `single_delete`: preserve existing behavior (uses `args.force` to skip prompt)
3. For `batch_delete`: **CRITICAL:** dry-run check MUST come BEFORE confirmation prompt (per D-05). Skip confirmation when `args.force || ctx.no_input || !io::stdin().is_terminal()`
   - Confirmation text: `"Delete {} workflow(s)?"`
   - `ctx.client.delete_workflow`, `KEY_WORKFLOWS`, entity_name `"workflow"`
4. The `run()` function needs access to both `ctx` and `args` (for `args.force`), so pass `args` to `batch_delete`
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo check 2>&1 | tail -5 && cargo test workflows 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/workflows.rs WorkflowsUpdateArgs contains `pub id: Option<String>` and `pub stdin: bool`
    - src/cli/workflows.rs WorkflowsDeleteArgs contains `pub ids: Vec<String>` and `pub stdin: bool` and `pub force: bool`
    - src/commands/workflows/update.rs contains `async fn batch_update`
    - src/commands/workflows/update.rs contains `BatchOutcome::new`
    - src/commands/workflows/update.rs uses `ok_or_else` with CliError (NOT unwrap) for args.id extraction
    - src/commands/workflows/delete.rs contains `async fn batch_delete`
    - src/commands/workflows/delete.rs contains `args.force` check in batch_delete
    - `cargo check` succeeds
    - `cargo test workflows` passes
  </acceptance_criteria>
  <done>
    All 7 entities now support batch update (--stdin) and batch delete (multi-ID + --stdin + confirmation). Workflows preserves existing --force flag behavior. No unwrap() in production code. Full entity coverage complete.
  </done>
</task>

</tasks>

<verification>
- `cargo check` passes
- `cargo test` passes (all existing tests)
- `pipelite pipelines update --help` shows --stdin flag
- `pipelite stages delete --help` shows multiple IDs and --stdin
- `pipelite workflows delete --help` shows --force, multiple IDs, and --stdin
</verification>

<success_criteria>
- Pipelines, stages, and workflows all have batch update (--stdin) and batch delete (multi-ID + --stdin + confirmation)
- All use BatchOutcome for continue-on-error reporting
- Workflows preserves existing --force flag on delete
- Pipeline mutations invalidate stages cache
- No unwrap() in production code
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-batch-operations-for-all-entities/07-03-SUMMARY.md`
</output>
