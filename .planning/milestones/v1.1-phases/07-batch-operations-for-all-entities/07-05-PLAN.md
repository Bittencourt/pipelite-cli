---
phase: 07-batch-operations-for-all-entities
plan: 05
type: execute
wave: 1
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - src/error.rs
  - src/batch.rs
  - src/api/mod.rs
  - Cargo.toml
  - src/commands/deals/update.rs
  - src/commands/orgs/update.rs
  - src/commands/people/update.rs
  - src/commands/activities/update.rs
  - src/commands/pipelines/update.rs
  - src/commands/stages/update.rs
  - src/commands/workflows/update.rs
  - tests/batch_error_test.rs
autonomous: true
gap_closure: true
requirements: [BATCH-04]

must_haves:
  truths:
    - "Batch update stdin containing ANY object without a string 'id' field is rejected before the first HTTP call — zero requests fired, nothing mutated (mixed valid/invalid batches included)"
    - "Structural batch input failures (invalid JSON, empty update list, empty ID list, --stdin conflicts, missing-ID) exit code 2; per-item batch failures stay exit 1; all-ok stays exit 0"
    - "An HTTP 429 item retries exactly once after sleeping per Retry-After; if the retry succeeds the item succeeds (exit 0); if still 429 the item is reported as a failed item with rate-limit detail and the batch exits 1"
  artifacts:
    - path: "src/error.rs"
      provides: "CliError::InvalidInput variant mapped to exit code 2 in exit_code()"
      contains: "InvalidInput"
    - path: "src/batch.rs"
      provides: "Pre-loop id pre-validation in run_batch_update; all stdin-structural sites raise InvalidInput"
      contains: "missing a string 'id' field"
    - path: "src/api/mod.rs"
      provides: "send_with_retry wrapper (429 + Retry-After sleep, one retry) routed through all entity calls; 429-specific Api detail"
      contains: "send_with_retry"
    - path: "tests/batch_error_test.rs"
      provides: "Mixed-batch zero-HTTP test, exit-2 contract tests, stub-server 429 retry tests"
      contains: "spawn_stub_server"
  key_links:
    - from: "src/batch.rs"
      to: "CliError::InvalidInput"
      via: "structural rejections raise the exit-2 variant"
      pattern: "InvalidInput"
    - from: "src/api/mod.rs entity methods"
      to: "send_with_retry"
      via: "all request sends go through the retry wrapper"
      pattern: "send_with_retry"
    - from: "tests/batch_error_test.rs 429 tests"
      to: "app server URL override"
      via: "PIPELITE_SERVER_URL points at the stub listener"
      pattern: "PIPELITE_SERVER_URL"
---

<objective>
Close the three verification gaps blocking BATCH-04 ("results scripts can trust"): (1) missing-ID items rejected before any HTTP call, (2) 429 Retry-After retry-once in the API client, (3) exit code 2 for structural batch input failures.

Purpose: BATCH-04 is the only open requirement of Phase 7; these are silent omissions relative to the roadmap contract (not covered by any later phase). Together they make the exit-code matrix 0/1/2 meaningful and guarantee zero mutations on structurally invalid input.
Output: Hardened src/error.rs + src/batch.rs + src/api/mod.rs, extended tests/batch_error_test.rs, full test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-batch-operations-for-all-entities/07-VERIFICATION.md (gaps: section — the three items this plan closes)
@.planning/phases/07-batch-operations-for-all-entities/07-01-SUMMARY.md (how batch.rs is built and consumed)
@.planning/REQUIREMENTS.md (BATCH-04 text)
@CLAUDE.md

<interfaces>
<!-- Existing contracts the executor needs. Extracted from the codebase. -->

From src/error.rs (current):
```rust
pub enum CliError {
    Auth { detail: String, hint: String },
    Connection { detail: String, hint: String },
    Config { detail: String, hint: String },      // #[allow(dead_code)]
    NotFound { detail: String, hint: String },
    Validation { detail: String, hint: String },
    Api { status: u16, detail: String, hint: String },
    MissingInput { detail: String, hint: String },
}
// exit_code(): clap::Error -> 2, MissingInput -> 2, everything else -> 1
```

From src/batch.rs (current, lines that matter):
```rust
// read_stdin_json: stdin read failure + invalid JSON -> CliError::Validation
// collect_delete_ids: exclusivity / no-stdin-data / empty ID list -> CliError::Validation
// run_batch_update: no-stdin-data (line ~271) + "Empty update list" (line ~287)
//   -> CliError::Validation; missing-id handled IN-LOOP at lines 306-313 as
//   per-item record_failure("unknown", "missing 'id' field")  <-- GAP 1 target
// BatchOutcome::finalize -> CliError::Validation on failures  <-- must STAY exit 1
// 3 unit tests assert finalize returns CliError::Validation   <-- must keep passing
```

From src/api/mod.rs (current):
```rust
// ~38 entity call sites follow this shape:
let response = self.client.put(&url).json(data).send().await
    .map_err(|e| self.map_request_error(e))?;
let wrapper: ApiSingleResponse<Deal> = self.handle_response(response).await?;
// handle_response: 401/403->Auth, 404->NotFound, 422->Validation, else->Api (429 falls here)
// handle_delete_response: 401/403->Auth, 404->NotFound, else->Api (429 falls here)
// send_ping_request keeps its own .send() — leave it alone
```

From src/api/models.rs (for the 429 stub-server success body):
```rust
pub struct ApiSingleResponse<T> { pub data: T }
pub struct Deal { id, title, value: Option<f64>, stage_id, organization_id: Option<String>,
  person_id: Option<String>, owner_id, position: Option<i64>, expected_close_date: Option<String>,
  notes: Option<String>, custom_fields: Option<Value>, created_at, updated_at }  // all Deserialize
```

From tests/batch_error_test.rs:
```rust
fn cmd() -> Command  // sets PIPELITE_URL + PIPELITE_SERVER_URL = http://127.0.0.1:1 + fake-test-key
// app reads PIPELITE_SERVER_URL (src/config.rs:110); keep both vars in any helper
```
</interfaces>

**Do NOT touch (accepted overrides — recorded in 07-VERIFICATION.md):**
- No NDJSON support (D-01: JSON-array only). Do not "fix" the NDJSON rejection.
- No `--continue-on-error` flag (D-06: continue-on-error is unconditional).
- Keep the CR-01 `--force` gate on non-interactive piped batch deletes.
- Keep `BatchOutcome::finalize` returning `CliError::Validation` (exit 1) — per-item failure stays exit 1.

**No new dependencies.** tokio is already a main dependency (add its `time` feature only); the 429 stub server is hand-rolled on `std::net::TcpListener` — do NOT add wiremock/mockito/httpmock or any new crate.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exit code 2 for structural batch input failures (gap 3)</name>
  <files>src/error.rs, src/batch.rs, src/commands/deals/update.rs, src/commands/orgs/update.rs, src/commands/people/update.rs, src/commands/activities/update.rs, src/commands/pipelines/update.rs, src/commands/stages/update.rs, src/commands/workflows/update.rs, tests/batch_error_test.rs</files>
  <behavior>
    - `not json` piped to `deals update --stdin` → exit 2, stderr contains "Invalid JSON input" plus hint
    - `[]` piped to `deals update --stdin` → exit 2, stderr contains "Empty update list" (existing test tightened from .failure() to .code(2))
    - `[]` piped to `deals delete --stdin` → exit 2, stderr contains "Empty ID list"
    - `--stdin` together with positional IDs on delete → exit 2, stderr contains "mutually exclusive"
    - `--stdin` together with a field flag (e.g. `deals update --stdin --title X`) → exit 2, stderr contains "mutually exclusive" (all 7 entities share the identical code path shape; test one entity — deals)
    - All-valid items against unreachable server → exit 1 (per-item failure contract unchanged; existing test tightened with explicit .code(1))
    - Unit: exit_code(CliError::InvalidInput) == 2; exit_code(Validation) still 1
  </behavior>
  <action>
Per BATCH-04's exit contract, scripts must distinguish "whole input structurally broken, nothing ran" (2) from "some items failed" (1). Verification gap 3 reason names these exact sites: Invalid JSON input, Empty update list, Empty ID list, mutual-exclusivity.

1. **src/error.rs** — add variant `InvalidInput { detail: String, hint: String }` to `CliError` with `#[error("Invalid input")]`. Add the matching arm to `display_error`'s exhaustive match (same title/detail/hint rendering as Validation). Extend `exit_code()`: the existing `matches!(e, CliError::MissingInput { .. })` arm becomes a match covering BOTH `MissingInput` and `InvalidInput` → 2; clap → 2 and the final fallthrough → 1 stay exactly as they are. Add a unit test asserting `exit_code` returns 2 for `InvalidInput` and 1 for `Validation` (mirror the existing `exit_code_returns_1_for_runtime_errors` test style).

2. **src/batch.rs** — convert ONLY the pre-HTTP structural rejection sites from `CliError::Validation` to `CliError::InvalidInput` (detail/hint text unchanged): the two in `read_stdin_json` (stdin read failure, invalid JSON), the three in `collect_delete_ids` (mutual exclusivity, no data on stdin, empty ID list), and the two in `run_batch_update` (no data on stdin, "Empty update list"). That is 7 sites. Do NOT touch `BatchOutcome::finalize` (stays Validation → exit 1 — the 3 existing unit tests in batch.rs assert the Validation variant and must keep passing) and do NOT touch `parse_custom_fields` (single-command flag parsing, outside the batch stdin contract).

3. **src/commands/{deals,orgs,people,activities,pipelines,stages,workflows}/update.rs** (7 files, one edit each) — inside the `if args.stdin` block only, the exclusivity error ("--stdin and individual field flags are mutually exclusive") changes from `CliError::Validation` to `CliError::InvalidInput`, same detail/hint strings. It is the only error on the `--stdin` batch path in these handlers. Leave every other `Validation` site (single-update missing-id extraction, non-interactive no-input guard) untouched — those are single-command usage errors, not batch stdin validation.

4. **tests/batch_error_test.rs** — tighten existing structural tests and add the ones missing from the behavior list: change `.failure()` to `.code(2)` on `batch_update_empty_array_fails`; add tests for invalid-JSON→2, delete empty-list→2, delete stdin+positional exclusivity→2, update stdin+flag exclusivity→2 (deals); change `batch_update_unreachable_server_exits_nonzero` and `batch_delete_unreachable_server_exits_nonzero` from `.failure()` to `.code(1)` (they lock in the per-item exit-1 contract). Use the existing `cmd()` helper and `write_stdin` patterns.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo test --test batch_error_test 2>&1 | tail -5 && cargo test --test exit_code_test 2>&1 | tail -3 && cargo test --bin pipelite batch 2>&1 | tail -3 && test "$(grep -c 'CliError::InvalidInput' src/batch.rs)" -ge 7 && echo GATE-OK</automated>
  </verify>
  <done>
    All structural batch stdin failures exit 2 with detail + hint; per-item batch failures still exit 1; clap misuse still exits 2; all-others still exit 1. Existing BatchOutcome unit tests (Validation variant) pass unchanged. Full batch test suite green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Missing-ID pre-validation before any HTTP call (gap 1)</name>
  <files>src/batch.rs, tests/batch_error_test.rs</files>
  <behavior>
    - Mixed batch `[{"id":"deal_1","title":"X"},{"title":"No ID Here"}]` against unreachable server → exit 2; stderr contains "missing a string 'id' field" and position "2"; stderr does NOT contain "Connection failed" and does NOT contain "[1/2] Failed" (zero HTTP attempts — the core regression the verifier probed)
    - Single no-id item `[{"title":"No ID Here"}]` → exit 2 with the same structural message (rewrites `batch_update_missing_id_in_item_shows_error`, which currently locks in the non-conforming in-loop path and its weak `contains("missing").or(contains("id"))` predicate)
    - Non-string id `[{"id":123,"title":"X"}]` → exit 2 (id must be a string, matching the in-loop `as_str()` semantics)
    - `--dry-run` with a missing-id item → exit 2, no per-item dry-run output (pre-scan runs BEFORE the dry-run block)
    - All-valid items → behavior unchanged (per-item HTTP still happens; unreachable server → exit 1)
  </behavior>
  <action>
Verification gap 1: an item missing its `id` is discovered inside the processing loop while earlier items are already mutated — probe proved item 1's HTTP call fired before the id-less item was reported. The fix is a full pre-scan.

1. **src/batch.rs** — add a private helper `fn prevalidate_item_ids(items: &[serde_json::Value]) -> Result<()>`: collect the 1-based positions of every item where `item.get("id").and_then(|v| v.as_str())` is None (this treats both absent and non-string ids as missing, matching the existing in-loop extraction semantics). If any, return `CliError::InvalidInput` with detail formatted as "Item(s) missing a string 'id' field: 2, 5" (comma-separated 1-based positions) and hint "Every object in the JSON array must contain a string 'id' field identifying the record to update." Call it in `run_batch_update` immediately AFTER the empty-list check and BEFORE the `ctx.dry_run` block — dry-run is how users validate input, so structurally broken input must be rejected there too.

2. Keep the in-loop id-extraction `match` in the processing loop as-is, including its `None` arm calling `record_failure` — it is now unreachable defense-in-depth. Do NOT use `unwrap()`/`expect()` to "simplify" it (CLAUDE.md forbids unwrap in production code).

3. **tests/batch_error_test.rs** — rewrite `batch_update_missing_id_in_item_shows_error` per the behavior list (exit 2 + exact structural message — this also resolves the carried IN-04 weak-predicate finding). Add the mixed-batch zero-HTTP test: the unreachable 127.0.0.1:1 server guarantees any HTTP attempt prints "Connection failed", so asserting `"Connection failed"` is absent from stderr proves zero requests fired; also assert `"[1/2] Failed"` is absent. Add the non-string-id test and the dry-run pre-scan test (`--dry-run` before `--stdin` args, assert exit 2 and empty stdout).
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo test --test batch_error_test 2>&1 | tail -8 && test "$(grep -c 'prevalidate_item_ids' src/batch.rs)" -ge 2 && echo GATE-OK</automated>
  </verify>
  <done>
    Any stdin batch containing an item without a string `id` is rejected with exit 2 before the first HTTP call — valid items in a mixed batch are NOT mutated. Verification truth 4 ("zero mutations on invalid input") now holds without override.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 429 Retry-After retry-once in the API client (gap 2)</name>
  <files>src/api/mod.rs, Cargo.toml, tests/batch_error_test.rs</files>
  <behavior>
    - Stub server: request 1 → 429 with `Retry-After: 0`; request 2 → 200 with valid deal JSON → `deals update --stdin` (one item) exits 0, no "Failed" line on stderr, server request count == 2 (exactly one retry)
    - Stub server: 429 + `Retry-After: 0` twice → exit 1; stderr carries the per-item failure line with rate-limit detail (contains "429" or "Rate limited"); server request count == 2 (one retry, not two)
    - Sleep is driven by the Retry-After header value, clamped to 0..=60 seconds (tests use `Retry-After: 0` so they stay fast)
  </behavior>
  <action>
Verification gap 2: no 429/Retry-After handling exists anywhere; 429 falls into the generic Api arm with no retry. Implement the retry at the client layer so single and batch operations both get it; batch classification is already automatic (a client Err after retry becomes a per-item `record_failure` with the error's detail).

1. **Cargo.toml** — add `"time"` to the tokio features list (`features = ["rt", "macros", "time"]`). This enables `tokio::time::sleep`; no new crate is added.

2. **src/api/mod.rs** — add `async fn send_with_retry(&self, request: reqwest::RequestBuilder) -> Result<reqwest::Response>`: call `request.send().await` mapped through `self.map_request_error`; if the response status is `reqwest::StatusCode::TOO_MANY_REQUESTS`, read the `Retry-After` header from the response (`reqwest::header::RETRY_AFTER`), parse it as u64 seconds (missing or non-numeric → default 1 second; HTTP-date form is out of scope — treat as default), clamp to 0..=60, `tokio::time::sleep` that duration, then resend the SAME `RequestBuilder` (`send` takes `&self`, so the builder is reusable) and return the second response regardless of status. Exactly one retry, never more. Header value of 0 sleeps 0 — that is the testable path.

3. **Route all entity call sites through it** — replace the pattern `.send().await.map_err(|e| self.map_request_error(e))?` at every entity method (list/get/create/update/delete/batch_create across all 7 entities, plus `trigger_workflow` — 38 sites) with `self.send_with_retry(<same builder>).await?`. Leave `send_ping_request` unchanged. Grep gates below pin the counts.

4. **429 detail arm** — in BOTH `handle_response` and `handle_delete_response`, add a `429` arm before the catch-all `_` arm returning `CliError::Api` with status 429, detail "Rate limited: server returned 429 again after one retry (Retry-After applied)", hint "The server is rate-limiting requests. Wait before retrying, or reduce the batch size." This supplies the "failed item with detail" the truth requires (error_detail extraction from the body can stay as-is for this arm's detail suffix if trivial, otherwise the fixed detail text above is sufficient).

5. **tests/batch_error_test.rs** — add a `cmd_with_server(url: &str)` helper (sets `PIPELITE_SERVER_URL` + `PIPELITE_URL` to the stub URL and `PIPELITE_API_KEY` to `fake-test-key`; the existing unreachable-server `cmd()` stays for the other tests). Add `spawn_stub_server(script: &[(u16, Option<&str>)]) -> (String, Arc<AtomicUsize>)` using `std::net::TcpListener::bind("127.0.0.1:0")`: spawn a `std::thread` that, per scripted entry, accepts one connection, reads the request headers until `\r\n\r\n`, reads exactly `Content-Length` more body bytes if present, then writes an HTTP/1.1 response — status line, the optional `Retry-After: <v>\r\n` header, `Content-Length: <body.len()>\r\n`, `Connection: close\r\n\r\n`, then the body — flushes, drops the stream, and increments the `Arc<AtomicUsize>` counter. Bodies: 429 → `{"error":"rate limited"}`; 200 → a complete `ApiSingleResponse<Deal>` JSON wrapper (see interfaces block — all Deal fields required, use e.g. id "deal_1", title "Updated", stage_id "s1", owner_id "u1", created_at/updated_at "2026-01-01T00:00:00Z"). Add the two tests from the behavior list (deals only — the retry lives in the shared client, one entity proves it). Tests are synchronous std code; the CLI runs as a subprocess via assert_cmd, so no async test runtime is needed.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo test --test batch_error_test 2>&1 | tail -8 && test "$(grep -c 'send_with_retry(' src/api/mod.rs)" -eq 39 && test "$(grep -c 'map_request_error(e)' src/api/mod.rs)" -eq 4 && echo GATE-OK && cargo test 2>&1 | tail -5</automated>
  </verify>
  <done>
    A 429 item retries exactly once per Retry-After (count == 2 proven by the stub); retry success → exit 0; still-429 → failed item with rate-limit detail, exit 1, no crash or silent skip. All ~38 entity sends go through send_with_retry (grep gates 39 and 4). Verification truth 5 holds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client | Untrusted HTTP responses (status + Retry-After header) drive new control flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07G-01 | DoS | send_with_retry retry loop | mitigate | Hard cap: exactly one retry, sleep clamped to 0..=60s — cannot be talked into a retry storm by a hostile Retry-After header |
| T-07G-02 | Tampering | Retry-After header parsing | mitigate | Parsed as u64 seconds with fallthrough default on missing/non-numeric values; invalid input can only change the sleep length within the clamp |
| T-07G-03 | Repudiation | Exit-code contract in scripts | mitigate | Exit 0/1/2 pinned by integration tests (.code assertions), so scripts can trust the matrix |
| T-07-SC | Tampering | dependency changes | accept | No new crates — tokio `time` feature on an existing pinned dependency; no npm/pip/cargo install, no legitimacy checkpoint required |
</threat_model>

<verification>
- `cargo test` full suite passes (batch_error_test includes the new exit-2, zero-HTTP, and 429 stub tests; existing batch_update/batch_delete/batch_cli_stub suites unbroken)
- `echo '[{"id":"deal_1","title":"X"},{"title":"No ID"}]' | PIPELITE_SERVER_URL=http://127.0.0.1:1 pipelite deals update --stdin` → exit 2, no "Connection failed" in output (gap 1 probe now conforming)
- `echo 'not json' | pipelite deals update --stdin` → exit 2 (gap 3 probe now conforming)
- 429-then-success and 429-twice stub tests pass with request count == 2 (gap 2)
- No `unwrap()` introduced in production code; all new errors carry detail + hint
- Accepted overrides untouched: NDJSON still rejected (D-01), no --continue-on-error flag (D-06), --force gate intact (CR-01)
</verification>

<success_criteria>
- Gap 1 closed: missing-ID (or non-string-ID) items in any stdin batch are structurally rejected before the first HTTP call — mixed batches perform zero requests
- Gap 2 closed: HTTP 429 sleeps per Retry-After (clamped ≤60s), retries exactly once; retry success = item success; still-429 = failed item with rate-limit detail, batch exit 1
- Gap 3 closed: structural batch input failures exit 2 (invalid JSON, empty update list, empty ID list, stdin conflicts, missing-ID); per-item failures exit 1; all-ok exits 0
- BATCH-04 fully satisfied: shared utility, exit 0/1/2 matrix, quiet-surviving summary, stdin fully validated pre-HTTP, 429 Retry-After retried once — REQUIREMENTS.md can mark it complete after re-verification
</success_criteria>

<output>
After completion, create `.planning/phases/07-batch-operations-for-all-entities/07-05-SUMMARY.md`
</output>
