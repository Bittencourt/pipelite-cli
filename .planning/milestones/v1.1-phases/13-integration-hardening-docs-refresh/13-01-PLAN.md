---
phase: 13-integration-hardening-docs-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/contract_matrix_test.rs
  - tests/common/mod.rs
  - tests/batch_delete_test.rs
  - tests/webhooks_stub_test.rs
  - tests/trash_stub_test.rs
  - tests/notes_stub_test.rs
  - tests/templates_stub_test.rs
  - tests/custom_fields_stub_test.rs
  - tests/typed_writing_stub_test.rs
autonomous: true
requirements: []
# requirements is [] by design: Phase 13 is cross-cutting VERIFICATION of every
# requirement delivered in Phases 7-12 (ROADMAP: "None exclusively"). The []
# value passes frontmatter.validate --schema plan (verified pre-plan).
# notes: tests/*.rs files_modified beyond contract_matrix_test.rs are CONDITIONAL —
# they are touched ONLY if a matrix row exposes a violation whose pinning test
# belongs in that surface's existing file (fix-and-pin protocol, Task 3).

must_haves:
  truths:
    - "Every Phase 7-12 MUTATION command honors --dry-run: exit 0, zero HTTP (unreachable server, no 'Connection failed'), preview printed (ROADMAP SC-1)"
    - "Destructive/interactive Phase 7-12 commands refuse under --no-input/non-TTY with the locked exit codes (batch delete 1, standard deletes 1, trash purge 2, missing-input prompts 2), zero HTTP, naming the bypass flag (SC-1)"
    - "--quiet suppresses informational stderr (empty-hints, success confirmations) while DATA survives — including the batch 'N ok, M failed' summary, which survives --quiet by design (SC-1 + SC-2 stub anchor)"
    - "--no-color output (stdout AND stderr) contains zero ANSI escape bytes on success and error paths (SC-1)"
    - "Every list-bearing new group (runs, templates, notes, webhooks, trash, audit, custom-fields) renders --format csv with a header row and --format plain as non-empty text via stubs (SC-1 format coverage)"
    - "v1.0-surface smoke passes: the 7 original entities' CRUD paths still behave (deals list/get, orgs create --dry-run, ping, dashboard, completions) (SC-4)"
    - "Full test suite green with no regressions against the v1.0 baseline; any discovered contract violation is FIXED in-phase with a pinning test and a deviation-log entry (SC-4)"
  artifacts:
    - path: "tests/contract_matrix_test.rs"
      provides: "Table-driven contract matrix: per new surface (batch, workflow runs, templates, notes, webhooks, trash, audit, custom-fields, docs + one typed-writing row) × global-flag axis (dry-run / no-input / quiet / no-color / csv+plain), riding tests/common cmd() + spawn_head_capturing_stub_server"
      min_lines: 400
      contains: "struct Case"
    - path: "tests/common/mod.rs"
      provides: "UNCHANGED unless a shared helper is genuinely missing (e.g. an ANSI-scan helper); any addition must not alter existing helper signatures"
      contains: "pub fn spawn_head_capturing_stub_server"
    - path: ".planning/phases/13-integration-hardening-docs-refresh/13-01-SUMMARY.md"
      provides: "Deviation log section: one entry per discovered violation (surface, flag axis, observed vs expected, root cause, fix, pinning test name) — 'no violations found' is also an entry"
      contains: "Deviation"
  key_links:
    - from: "tests/contract_matrix_test.rs"
      to: "tests/common/mod.rs"
      via: "mod common; cmd() for unreachable-server pre-HTTP probes; cmd_with_server + spawn_head_capturing_stub_server for stub-backed rendering rows"
      pattern: "mod common"
    - from: "tests/contract_matrix_test.rs dry-run rows"
      to: "http://127.0.0.1:1 unreachable server"
      via: "counter == 0 / no 'Connection failed' — the established zero-HTTP proof pattern (batch_error_test.rs precedent)"
      pattern: "Connection failed"
    - from: "tests/contract_matrix_test.rs quiet rows"
      to: "batch summary contract"
      via: "positive assertion: 'ok,' summary line PRESENT under --quiet (Phase 7 SC-3 — the summary is data, not chatter)"
      pattern: "ok,"
---

<objective>
Build and run a table-driven (author ONE #[test] per Case row, all delegating to shared axis runners — loop-inside-one-fn designs fail the per-row count gates by intent: all red rows must report) contract-matrix test file that proves every Phase 7-12 surface honors the v1.0 global contract — `--dry-run` (zero HTTP), `--no-input` (locked refusals), `--quiet` (suppress chatter, keep data), `--no-color` (zero ANSI) — plus csv/plain format coverage per new group, then FIX any discovered violation in-phase with a pinning test and close with explicit v1.0-surface smoke assertions and a full-suite gate.

Purpose: ROADMAP SC-1/SC-4 — this is the final feature phase of v1.1; the milestone audit reads its results. Violations found here are the last chance to fix contract drift before release.

Output: tests/contract_matrix_test.rs (matrix, green), pinning tests in existing surface test files (only for discovered violations), 13-01-SUMMARY.md with the deviation log, full suite green.
</objective>

<execution_context>
@/home/pedro/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/pedro/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-integration-hardening-docs-refresh/13-CONTEXT.md (BINDING decisions — table-driven matrix, in-phase fixes, v1.0 baseline, scope = NEW surfaces only)
@.planning/ROADMAP.md (Phase 13 SC-1..SC-4 — the truths this plan proves)
@.planning/STATE.md (Accumulated Context → Decisions: the locked per-surface contracts the matrix pins)
@CLAUDE.md (global contract + conventions)
@tests/common/mod.rs (cmd / cmd_with_server / spawn_head_capturing_stub_server — reuse, do not redefine)

<interfaces>
<!-- Contracts verified in-repo. The matrix pins THESE behaviors; exact CLI grammar must be re-verified against --help when authoring each row (authoring step, Task 1). -->

Shared helpers (tests/common/mod.rs — reuse as-is):
```rust
cmd() -> Command                       // unreachable http://127.0.0.1:1 + fake-test-key — pre-HTTP probes
cmd_with_server(url: &str) -> Command  // stub-backed runs; sets PIPELITE_URL + PIPELITE_SERVER_URL + PIPELITE_API_KEY
spawn_head_capturing_stub_server(&[(u16, &str)]) -> (String, Arc<AtomicUsize>, Arc<Mutex<Vec<String>>>, Arc<Mutex<Vec<String>>>)
// heads lowercased; bodies split at first "\r\n\r\n"; scripted status 0 = transport failure
```

Locked behaviors the matrix pins (sources: src/batch.rs:174-189, STATE.md Decisions, phase suites):
```
--dry-run (MUTATIONS ONLY — reads are side-effect-free and exempt; v1.0 suites never refused reads):
  exit 0, counter == 0, stderr lacks "Connection failed", preview (method + URL) on stdout.
  Typed-writing row is special: `deals create ... --custom-field price=4 --dry-run` with a COLD
  cache (HOME=tmpdir) → counter == 0 (cache-only fallback, 12-02 SC-4), visible note unless --quiet.

--no-input / non-TTY refusals (piped stdin via assert_cmd = non-TTY):
  batch delete (ids via stdin, no --force)  → exit 1 Validation, hint "Re-run with --force", counter == 0
  webhooks delete <id>      (no --force)    → exit 1, "--force" in stderr, counter == 0
  templates delete <id>     (no --force)    → exit 1, same contract
  notes delete <type> <p> <n> (no --force)  → exit 1, same contract
  custom-fields delete <id> (no --force)    → exit 1, same contract
  trash purge --type deals  (no --force)    → exit 2 InvalidInput (STRICTER than delete-1 — locked in
                                              STATE; refusal fires BEFORE the fan-out, counter == 0)
  notes add <type> <pid>    (no --body/--stdin) → exit 2 MissingInput pre-HTTP (prompt suppressed)
  batch update --stdin                      → PROCEEDS (stdin IS the input; no refusal)

--quiet:
  data ALWAYS survives on stdout (rows, JSON, the show-once webhook secret — the secret is DATA);
  informational stderr suppressed: empty-list hints ("No ... yet"), success confirmations
  ("Created ...", "Deleted ..."), the dry-run cache note, the unknown-custom-field warning.
  BATCH SUMMARY IS DATA: the "N ok, M failed" line survives --quiet (Phase 7 SC-3 — pin POSITIVELY).

--no-color:
  zero "\x1b[" bytes in stdout AND stderr — success rows AND an error path (colored crate gates
  on ctx.color; error.rs renders detail/hint with color when enabled).

csv/plain (list-bearing groups only; batch = outcome-summary text, docs = spec passthrough — exempt):
  --format csv   → line 1 is a comma-separated header containing "id"; ≥1 data line for a 1-row fixture
  --format plain → non-empty output containing the fixture's first-column value
```

Surface grammar (authoring step re-verifies each row against `--help`):
```
batch:    deals update --stdin (JSON array on stdin) · deals delete --stdin (IDs on stdin) / id1 id2
runs:     workflows runs list --workflow wf1 · workflows runs get run1 --workflow wf1
templates: templates list · get t1 · create --name T --workflow wf1 · delete t1 --force
notes:    notes list deals d1 · add deals d1 --body x · edit deals d1 n1 --body y · delete deals d1 n1 --force
webhooks: webhooks list · get w1 · create --url https://example.com/hook --events deal.created ·
          update w1 --active · delete w1 --force
trash:    trash list · trash restore deal d1 · trash purge --type deals [--force]
audit:    audit list
custom-fields: custom-fields list --entity-type deals · get cf1 ·
          create --entity-type deals --key price --type number · update cf1 --name price2 ·
          delete cf1 --force
docs:     pipelite docs   (read-only, unauthenticated GET — quiet/no-color axes only)
typed:    deals create --title T --stage stg1 --custom-field price=4 [--dry-run]  (Phase 12 surface)
```

Stub fixtures: minimal one-row JSON envelopes the groups already accept — copy the fixture shape
from each surface's own stub test (tests/{webhooks,trash,notes,templates,audit,custom_fields,
workflow_runs}_stub_test.rs) rather than inventing new shapes. Empty-page fixture:
{"data":[],"meta":{"total":0,"offset":0,"limit":50}}.
</interfaces>

**Matrix scope (BINDING per 13-CONTEXT):** NEW surfaces only — batch (via stdin), workflow runs, templates, notes, webhooks, trash, audit, custom-fields (definitions + one typed-writing row), docs. The v1.0 entities keep their existing suites (deals/orgs/people/activities/pipelines/stages/workflows integration tests) — the matrix adds v1.0-surface SMOKE rows (Task 3), not full re-coverage.

**Fix-and-pin protocol (BINDING per 13-CONTEXT):** The matrix encodes the CONTRACT above, not current behavior. When a row fails:
1. First re-verify the row's grammar against `--help` (an authoring typo is NOT a violation — fix the row).
2. A real violation = code diverges from the locked contract. Fix it in the surface's src with the codebase conventions (hint text names the flag/vocabulary, locked exit codes, quiet suppression via ctx.quiet, color via ctx.color, no unwrap).
3. Add a pinning test to that SURFACE's existing test file (webhooks_stub_test.rs etc.) — not to the matrix — so the fix is pinned at the surface level too.
4. Update any existing tests that pinned the old (wrong) behavior, in the same commit.
5. Log the deviation in 13-01-SUMMARY.md: surface, axis, observed vs expected, root cause, fix, pinning test name. "No violations found" is also a log entry.
Never silently relax a matrix expectation to make it pass — an expectation change requires a contract-level justification in the deviation log.

**Existing tests expected to break: NONE by design** (the matrix file is additive). If a fix-and-pin change breaks an existing test, that is expected fallout of fixing a real violation — enumerate each affected test in the deviation log with its resolution. Run the full suite as the final gate.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Matrix scaffold + behavioral axes (--dry-run zero-HTTP, --no-input refusals)</name>
  <files>tests/contract_matrix_test.rs</files>
  <behavior>
    Table-driven scaffold: `struct Case { surface: &'static str, kind: Kind, args: Vec<&'static str>, stdin: Option<&'static str>, ... }` with `enum Kind { Mutation, ReadOnly, Destructive }` and a static table covering ALL new surfaces (grammar per the interfaces block; re-verify each row against `--help` while authoring). Runners per axis:
    - dry_run_zero_http (Mutation rows, ~10): unreachable server (cmd()); --dry-run appended → exit code 0, stderr lacks "Connection failed", stdout contains the method + endpoint URL preview. Rows: batch deals update --stdin (array fixture), batch deals delete --stdin (IDs), templates create, templates delete, notes add --body, notes edit --body, notes delete, webhooks create --url https://example.com/hook --events deal.created, webhooks update w1 --active, trash restore deal d1, custom-fields create --entity-type deals --key price --type number, custom-fields update cf1 --name price2, custom-fields delete cf1. ReadOnly rows (runs list/get, webhooks/trash/audit/custom-fields/docs lists, docs) are SKIPPED by this runner with a source comment: reads are side-effect-free; --dry-run is a mutation-preview contract (v1.0 precedent — reads fetch under --dry-run in the existing suites).
    - dry_run_typed_writing_cold_cache (1): HOME=tempdir (cold cache) + cmd() unreachable + `deals create --title T --stage stg1 --custom-field price=4 --dry-run` → exit 0, counter-style proof via unreachable server (no "Connection failed" in stderr), stderr contains the cache-fallback note; same invocation with --quiet lacks the note (12-02 SC-4 verbatim).
    - no_input_refusals (Destructive rows, ~8): stdin piped (assert_cmd default = non-TTY) WITHOUT --force → batch delete --stdin → exit 1 + "Re-run with --force" + (unreachable server) no HTTP attempted; webhooks/templates/notes/custom-fields delete → exit 1 + "--force" in stderr; trash purge --type deals → exit 2 + counter-style proof (unreachable) that the refusal fired BEFORE any connection; notes add without --body/--stdin → exit 2 (prompt suppressed, no hang — assert_cmd would deadlock-timeout on a prompt; the existing headless_test.rs pattern proves stdin-piped runs never prompt).
    Test count after Task 1: ≥ 20.
  </behavior>
  <action>
1. Create tests/contract_matrix_test.rs with `mod common;`, the `Case`/`Kind` table (one row per surface×operation from the interfaces block — author each row by FIRST running the command's `--help` via `cargo run -- <group> <sub> --help` to confirm exact flag names; adjust the ROW when grammar differs, never the EXPECTATION), and the two behavioral-axis runners (dry_run_zero_http, no_input_refusals + typed-writing special case).

2. Refusal rows run against `cmd()` (unreachable server): a refusal that reaches HTTP would surface "Connection failed" in stderr — assert its ABSENCE as the zero-HTTP proof (batch_error_test.rs precedent). Exit codes: `.code(1)` for Validation refusals, `.code(2)` for InvalidInput/MissingInput (trash purge is the locked exit-2 outlier; all standard deletes exit 1).

3. Dry-run preview rows also assert the preview NAMES the method + URL (render_dry_run output shape — see any existing dry-run test, e.g. dry_run_test.rs or a surface stub test, for the exact rendered text to substring-match).

4. The typed-writing cold-cache row sets `.env("HOME", tempdir)` per the established per-child-cache-hermetic pattern (cache.rs resolves $HOME/.pipelite/cache in the child process).

5. Run `cargo test --test contract_matrix_test`. RED rows here are candidate VIOLATIONS, not test bugs (grammar was --help-verified in step 1) — do NOT fix code yet; collect them for Task 3. Commit the matrix scaffold (tests only) before touching any src.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && test -f tests/contract_matrix_test.rs && test "$(grep -c '#[test]' tests/contract_matrix_test.rs)" -ge 20 && test "$(grep -c 'mod common' tests/contract_matrix_test.rs)" -eq 1 && test "$(grep -c 'dry_run_zero_http\|no_input_refusals\|dry_run_typed_writing_cold_cache' tests/contract_matrix_test.rs)" -ge 3 && test "$(grep -c 'Connection failed' tests/contract_matrix_test.rs)" -ge 2 && echo GATE-OK</automated>
  </verify>
  <done>
    tests/contract_matrix_test.rs exists with a --help-verified Case table and the two behavioral axes; dry-run rows prove zero-HTTP previews for every new mutation; refusal rows prove the locked exit codes (1 standard deletes/batch, 2 trash purge + missing-input) fire pre-HTTP; the typed-writing dry-run is cache-only proven. Failing rows (if any) are recorded as candidate violations for Task 3, not patched over.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Presentation axes (--quiet suppression, --no-color zero-ANSI, csv/plain per group)</name>
  <files>tests/contract_matrix_test.rs</files>
  <behavior>
    Append three axis runners to the same table:
    - quiet_suppression (~10 rows, stub-backed): each list-bearing group (runs list, templates list, notes list, webhooks list, trash list, audit list, custom-fields list) run against a stub serving an EMPTY page → without --quiet: exit 0 AND the informational empty-hint on stderr; with --quiet: exit 0, data path silent, empty-hint ABSENT. Mutation confirmations: webhooks create (stub 201 with the show-once secret fixture) → secret line PRESENT on stdout both with and without --quiet (the secret is DATA); custom-fields delete --force (stub 204) → "Deleted" confirmation PRESENT without --quiet, ABSENT with --quiet. Batch anchor (SC-2): stub scripted for 2 deletes + `deals delete id1 id2 --force --quiet` → exit 0 AND the "2 ok, 0 failed"-shaped summary line PRESENT in output (Phase 7 SC-3: summary survives --quiet — assert POSITIVELY).
    - no_color_zero_ansi (~4 rows): (a) a table render (webhooks list via stub, 1-row fixture) with --no-color → stdout contains NO "\x1b["; (b) an error path (webhooks get w1 → stub 404) with --no-color → stderr contains NO "\x1b["; (c) trash list with --no-color (table+truncation-heavy surface) → no ANSI; (d) batch update --stdin --force --no-color via stub → no ANSI in stdout or stderr. Scan the RAW output bytes (assert_cmd Output::stdout/stderr as &[u8], or lossy string containing check).
    - csv_and_plain (~14 rows): for EACH list-bearing group (runs list, templates list, notes list, webhooks list, trash list, audit list, custom-fields list — 7 groups × 2 formats): stub 1-row fixture (copy the row shape from that surface's own stub test) → `--format csv`: exit 0, first output line is comma-separated, first line is the header derived from that group's *_table_config().default_columns (runs/templates/notes/webhooks/custom-fields: contains "id"; trash: name,type,deleted_at,deleted_by,linked_parents — NO id, models.rs:961; audit: created_at,actor,action,entity — NO id, models.rs:1001), and a second line exists (the data row); `--format plain`: exit 0, output non-empty, contains the fixture's id value. batch + docs rows carry table comments exempting them (batch output = outcome summary text, already format-locked by its own suites; docs = spec passthrough, --format accepted-but-ignored per Phase 9).
    Test count after Task 2: ≥ 45 total in the file.
  </behavior>
  <action>
1. Extend the runners in tests/contract_matrix_test.rs — same Case table, three new axis functions. Stub fixtures: copy the minimal 1-row JSON response shape from each surface's existing stub test file (they are known-good against the real renderers); empty-page = {"data":[],"meta":{"total":0,"offset":0,"limit":50}}.

2. quiet_suppression: the empty-hint assertions need each group's actual empty-hint wording — grep the surface's handler for the stderr hint (e.g. "No webhooks", "No custom field definitions") and substring-assert a stable prefix. With --quiet the SAME invocation must not contain it. Do not assert exact full sentences — prefixes survive copy edits.

3. The webhook show-once fixture: the create response is the ONLY place the secret appears (11-01 contract) — the stub 201 body carries the secret field; assert its presence under --quiet AND without. This is the stub-level anchor for the live E2E in 13-02.

4. no_color: `colored` gates on ctx.color which --no-color sets false; scan both streams for the "\x1b[" escape prefix. Use assert_cmd's `.output()` and check `String::from_utf8_lossy(&out.stdout)` / stderr.

5. csv: the existing csv renderer emits the header as line 1 (src/output/csv.rs) — assert `lines().next()` contains "," and "id", and `lines().count() >= 2`. plain: assert against the fixture id (plain renderer emits values whitespace-separated).

6. Run the new axes. Again: failures = candidate violations (grammar already verified) — collect, don't patch. Commit.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && test "$(grep -c '#[test]' tests/contract_matrix_test.rs)" -ge 45 && test "$(grep -c 'quiet_suppression\|no_color_zero_ansi\|csv_and_plain' tests/contract_matrix_test.rs)" -ge 3 && test "$(grep -c 'x1b\[' tests/contract_matrix_test.rs)" -ge 1 && grep -q 'ok,' tests/contract_matrix_test.rs && echo GATE-OK</automated>
  </verify>
  <done>
    The matrix now covers all five axes: quiet rows prove data-survives/chatter-suppressed (including the batch summary and the show-once secret under --quiet), no-color rows prove zero ANSI on success and error paths across both streams, and csv/plain rows prove format coverage for every list-bearing new group. ≥45 tests in the file, candidate violations collected.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full matrix → fix-and-pin violations + deviation log + v1.0 smoke + suite gate</name>
  <files>src/commands/webhooks/*.rs, src/commands/trash/*.rs, src/commands/notes/*.rs, src/commands/templates/*.rs, src/commands/custom_fields/*.rs, src/commands/workflows/*.rs, src/batch.rs, tests/contract_matrix_test.rs, tests/webhooks_stub_test.rs, tests/trash_stub_test.rs, tests/notes_stub_test.rs, tests/templates_stub_test.rs, tests/custom_fields_stub_test.rs, tests/typed_writing_stub_test.rs, tests/batch_delete_test.rs</files>
  <behavior>
    - FULL matrix run: `cargo test --test contract_matrix_test` — every row green.
    - For each candidate violation from Tasks 1-2 (if any): the fix + a pinning test in the surface's own test file pinning the corrected behavior (e.g. a missing --quiet guard → quiet_suppresses_confirmation_<surface> in that surface's stub test; a color leak → <surface>_no_color_error_path; a wrong refusal code → code pinned to the locked value).
    - v1.0-surface smoke (append to the matrix file, ~6 tests): deals list (stub 200 1-row) table render exit 0; deals get d1 --format json exit 0 parsing as JSON; orgs create --dry-run (unreachable) exit 0 + POST preview + zero HTTP; ping against a stub 200 {"ok":true}-shaped body exit 0; dashboard against a stub exit 0; completions bash exit 0. These are SMOKE rows — deep v1.0 coverage stays in the existing integration suites.
    - Full suite: `cargo test` — ALL test binaries green, zero regressions.
    - Deviation log: ≥1 entry in 13-01-SUMMARY.md (violations fixed, or "No violations found" if the matrix passed clean on first full run).
  </behavior>
  <action>
1. Run the complete matrix. Triage every failure through the fix-and-pin protocol (in `<context>`): (a) re-verify grammar against --help — typo → fix the row; (b) real contract violation → minimal fix in the surface's src honoring the conventions (hint names the flag/vocabulary, locked exit codes 0/1/2, ctx.quiet gates informational output, ctx.color gates styling, --dry-run intercepts BEFORE any HTTP, no unwrap/expect); (c) pinning test in the surface's existing test file; (d) update any existing test that pinned the old behavior, same commit; (e) deviation-log entry. If a violation's honest fix would require a behavior CHANGE rather than a conformance fix (e.g. the locked exit code itself is wrong), STOP and log it as a deviation requiring an orchestrator decision — do not re-litigate locked contracts unilaterally.

2. Fix scope discipline: touch ONLY the surface named by the failing row. Do NOT refactor adjacent code, do NOT reformat, do NOT "improve" hints beyond what the contract requires. No new crates.

3. Append the v1.0-surface smoke tests to tests/contract_matrix_test.rs (same `mod common` helpers; stub fixtures copy deals_integration.rs row shapes). These anchor ROADMAP SC-4's "no regressions against v1.0 behavior" explicitly in THIS phase's artifact.

4. Gate: `cargo test` full suite. If any PRE-EXISTING test fails for environmental reasons (the known config-hermeticity family from deferred-items), verify it fails identically at the pre-plan commit via a throwaway worktree before classifying — never absorb an unexplained failure.

5. Write the deviation log into the summary (see <output>): one entry per violation OR the explicit no-violations entry, plus the enumerated list of any existing tests updated by fixes.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && set -o pipefail; cargo test --test contract_matrix_test 2>&1 | tail -4 && cargo test 2>&1 | tail -4 && test "${PIPESTATUS[0]}" -eq 0 && test "$(grep -c '#[test]' tests/contract_matrix_test.rs)" -ge 50 && test "$(grep -c 'smoke' tests/contract_matrix_test.rs)" -ge 1 && test "$(grep -rn 'unwrap()' src/commands/webhooks src/commands/trash src/commands/notes src/commands/templates src/commands/custom_fields src/batch.rs 2>/dev/null | grep -v 'unwrap_or' | wc -l | awk '{print ($1>0)?$1:0}')" -eq 0 && echo GATE-OK</automated>
  </verify>
  <done>
    The contract matrix is fully green: every new mutation is zero-HTTP under --dry-run, every destructive command refuses with its locked code and flag-naming hint, --quiet keeps data (batch summary, webhook secret) and drops chatter, --no-color emits zero ANSI, every list-bearing group renders csv+plain, and v1.0 smoke rows pass. Any discovered violation is fixed at the surface, pinned twice (matrix + surface test), and deviation-logged. Full cargo test suite green — no regressions against the v1.0 baseline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests→loopback | Stub servers bind 127.0.0.1:0 with a fake key — no external traffic, no real credentials in any test |
| matrix→src fixes | Fix-and-pin edits production error handling — a sloppy fix could weaken hints or exit codes (the exact contract this phase enforces) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Elevation (test privilege) | stub servers / test env | accept | Loopback-only binds, fake-test-key, no real credentials — established v1.1 test posture |
| T-13-01-02 | Tampering (contract erosion) | fix-and-pin src edits | mitigate | Fix protocol in <context>: locked exit codes and hint conventions are non-negotiable; honest-fix ambiguity escalates to a logged deviation for orchestrator decision, never a silent relaxation; grep gate keeps unwrap() out of fixed code |
| T-13-01-SC | Tampering | cargo installs | accept | No new crates — matrix rides assert_cmd + tests/common only |
</threat_model>

<verification>
- `cargo test --test contract_matrix_test` fully green: ≥50 tests across five axes (dry-run, no-input, quiet, no-color, csv/plain) + v1.0 smoke
- Zero-HTTP proofs: dry-run and refusal rows run against the unreachable server with "Connection failed" asserted absent (or counter == 0 on stub-backed rows)
- Locked exit codes held: standard deletes/batch refusal = 1, trash purge + structural/missing-input = 2
- Quiet contract: batch summary and webhook show-once secret PRESENT under --quiet; empty-hints and confirmations ABSENT
- No-color: zero "\x1b[" bytes in stdout and stderr on success and error paths
- Format coverage: csv header + plain output for all 7 list-bearing new groups
- Deviation log present in 13-01-SUMMARY.md (violations fixed + pinned, or explicit no-violations entry)
- `cargo test` FULL suite green — no regressions against the v1.0 baseline (ROADMAP SC-4)
- Conventions in any fixed code: hints on errors, no unwrap(), --dry-run/--no-input/--quiet/--no-color respected
</verification>

<success_criteria>
- ROADMAP SC-1: every Phase 7-12 command proven to honor --dry-run (mutations: zero HTTP), --no-input (locked refusals), --quiet (data survives), --no-color (zero ANSI), and csv/plain rendering (list-bearing groups) — all via one table-driven file
- ROADMAP SC-2 (stub anchor): batch exit-code contract + show-once secret verified at matrix level (live E2E follows in 13-02)
- ROADMAP SC-4: full suite green, v1.0-surface smoke explicit, violations fixed in-phase with pinning tests per the binding 13-CONTEXT decision
- Deviation log complete — the milestone audit can read exactly what drifted and how it was pinned
</success_criteria>

<output>
After completion, create `.planning/phases/13-integration-hardening-docs-refresh/13-01-SUMMARY.md` — it MUST include a `## Deviation Log` section (one entry per violation: surface, axis, observed vs expected, root cause, fix, pinning test; or the explicit "No violations found" entry) and the list of any pre-existing tests updated by fixes.
</output>
