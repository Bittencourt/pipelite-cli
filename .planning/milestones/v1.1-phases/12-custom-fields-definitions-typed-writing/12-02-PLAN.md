---
phase: 12-custom-fields-definitions-typed-writing
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/custom_fields.rs
  - src/batch.rs
  - src/cli/deals.rs
  - src/cli/orgs.rs
  - src/cli/people.rs
  - src/cli/activities.rs
  - src/commands/deals/create.rs
  - src/commands/deals/update.rs
  - src/commands/orgs/create.rs
  - src/commands/orgs/update.rs
  - src/commands/people/create.rs
  - src/commands/people/update.rs
  - src/commands/activities/create.rs
  - src/commands/activities/update.rs
  - tests/typed_writing_stub_test.rs
  - tests/headless_test.rs
  - tests/deals_integration.rs
  - tests/orgs_integration.rs
  - docs/api-reference.md
  - CHANGELOG.md
autonomous: true
requirements: [CFLD-02, CFLD-03]

must_haves:
  truths:
    - "`--custom-field price=4` on a number definition stores JSON number 4 — the captured wire body has custom_fields.price.is_number() AND as_i64() == Some(4) AND the raw body contains \"price\":4, never \"price\":\"4\" or 4.0 — type-correct writes for number/boolean/date/array/select resolved from cached definitions, matched BY NAME (blob keys are definition names) (ROADMAP SC-2)"
    - "User can bypass type inference with --custom-field-json '{...}' — the parsed object passes through VERBATIM (nested objects/arrays untouched); it and --custom-field are mutually exclusive pre-HTTP exit 2, and it joins every --stdin has_flags exclusivity rule across the 8 handlers (SC-3)"
    - "--dry-run NEVER triggers a definitions fetch (stub counter == 0 with a cold cache) — cache-only fallback sends every value as a raw string with a visible stderr note (run once without --dry-run to warm the cache), suppressed by --quiet; with a WARM cache, --dry-run still writes typed values (SC-4)"
    - "The CLI is the only validation layer: non-numeric on a number definition → exit 2 naming field + type (zero POSTs); non-true/false on boolean → exit 2; select/multi_select values validated client-side against config.options — unknown option → exit 2 listing valid options (webhooks 13-event precedent); writing to a formula definition → REFUSED exit 2 (the server strips formula keys — the value would silently vanish)"
    - "All 8 create/update handlers consume ONE shared resolver (no per-entity copies) — every existing --stdin exclusivity check is normalized to CliError::InvalidInput exit 2 (the 4 create handlers currently use Validation exit 1) and batch::parse_custom_fields is deleted with its callers"
  artifacts:
    - path: "src/custom_fields.rs"
      provides: "CfEntityType enum (Deal/Organization/Person/Activity → api token + KEY_CUSTOM_FIELDS_* handle), resolve_custom_fields(ctx, entity, pairs, dry_run) with fetch-on-miss auto-pagination + cache write, cache-only dry-run branch, infer_typed_value per the full inference table, option validation, formula refusal, aggregated unknown-key warning + unit tests"
      contains: "pub async fn resolve_custom_fields"
    - path: "src/batch.rs"
      provides: "parse_custom_fields REMOVED along with its 2 unit tests — zero callers remain (the resolver replaces it); batch stdin paths are untouched (typed JSON passes through verbatim)"
      contains: "pub fn parse_batches"
    - path: "src/cli/deals.rs"
      provides: "#[arg(long = \"custom-field-json\")] custom_field_json: Option<String> on DealsCreateArgs AND DealsUpdateArgs (mirrored on all 8 arg structs across orgs/people/activities)"
      contains: "custom-field-json"
    - path: "src/commands/deals/create.rs"
      provides: "reference rewiring: both-flags exit-2 check → exclusivity normalized to CliError::InvalidInput → resolver call (dry_run threaded) → typed Option<Value> into DealCreate.custom_fields"
      contains: "resolve_custom_fields"
    - path: "src/commands/activities/update.rs"
      provides: "the 8th call site: resolver called ONCE at the top; the typed Option<Value> flows through BOTH the normal path and update_with_null_completed's raw payload (custom_fields inserted at the existing payload site)"
      contains: "update_with_null_completed"
    - path: "tests/typed_writing_stub_test.rs"
      provides: "CFLD-02/03 coverage (≥20 tests) — the canonical price=4 wire test is written RED before the resolver is wired into deals create"
      contains: "as_i64"
  key_links:
    - from: "src/commands/{deals,orgs,people,activities}/{create,update}.rs (8 files)"
      to: "src/custom_fields.rs resolve_custom_fields"
      via: "one shared resolver call per handler replacing batch::parse_custom_fields — grep -l must count exactly 8 handler files"
      pattern: "resolve_custom_fields"
    - from: "src/custom_fields.rs"
      to: "src/cache.rs KEY_CUSTOM_FIELDS_* + list_custom_field_definitions"
      via: "cache-first: hit → resolve; miss (live only) → GET definitions auto-paginated limit 100 → cache under the entity key (TTL_CUSTOM_FIELDS); dry_run short-circuits BEFORE any miss path"
      pattern: "KEY_CUSTOM_FIELDS"
    - from: "src/custom_fields.rs infer_typed_value"
      to: "CliError::InvalidInput (exit 2)"
      via: "non-numeric number / non-boolean / unknown option / formula write / missing '=' — ALL structural, ALL pre-POST (after the definitions GET when cold; counter == 0 when warm)"
      pattern: "InvalidInput"
    - from: "8 handlers' has_flags expressions (12 sites)"
      to: "CliError::InvalidInput"
      via: "custom_field_json.is_some() added to every site; the 4 create handlers' exclusivity error switched Validation → InvalidInput (exit 2, matching the 4 update handlers)"
      pattern: "custom_field_json.is_some()"
    - from: "src/commands/activities/update.rs:75"
      to: "update_with_null_completed raw payload"
      via: "typed custom_fields passed as the existing Option<Value> parameter; inserted once into the raw payload (Pitfall 6 — applied once, not dropped, not doubled)"
      pattern: "update_with_null_completed"
---

<objective>
Replace string-only custom-field writing with a definitions-aware typed resolver shared by all 8 create/update handlers — `--custom-field price=4` puts JSON number `4` on the wire (fixing the milestone's data-correctness bug), `--custom-field-json` provides a verbatim bypass with strict mutual exclusivity, and `--dry-run` stays honest with a cache-only fallback — plus normalize every handler's exclusivity exit code to 2, proven by wire-level body assertions with the canonical `price=4` test written RED first.

Purpose: CFLD-02/03 — the server validates NOTHING on the v1 API path (validateFieldValues is UI-only), so this resolver is the only layer standing between users and type-confused jsonb blobs (string `"4"` poisoning numeric filters/workflows downstream); it exists precisely because nothing downstream will ever type-check for the CLI.

Output: src/custom_fields.rs (resolver + inference table + unit tests), 8 rewired handlers with --custom-field-json on all 8 arg structs and exit-2-normalized exclusivity, batch::parse_custom_fields deleted, tests/typed_writing_stub_test.rs (≥20 wire tests), extended help-coverage asserts, api-reference + CHANGELOG behavior note. Full suite green.
</objective>

<execution_context>
@/home/pedro/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/pedro/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-custom-fields-definitions-typed-writing/12-CONTEXT.md (locked decisions — Typed Writing, inference rules, unknown keys, --custom-field-json)
@.planning/phases/12-custom-fields-definitions-typed-writing/12-RESEARCH.md (§ Server-Side Value Validation Matrix, § Pattern 1 resolver sketch, § Pattern 2 raw bypass, § Pitfalls 1-8, § Code Examples — wire test + dry-run test)
@.planning/phases/12-custom-fields-definitions-typed-writing/12-01-PLAN.md (wave 1 — models, client methods, KEY_CUSTOM_FIELDS_* constants, cache writes, invalidation contract this plan consumes)
@.planning/REQUIREMENTS.md (CFLD-02/03 text)
@CLAUDE.md
@docs/SKILL.md

<interfaces>
<!-- Contracts established by 12-01 (consumed here) + verified in-repo call sites. Executor should use these directly — no exploration needed. -->

From 12-01 (consumed, do not recreate):
```rust
// src/cache.rs
pub const KEY_CUSTOM_FIELDS_DEAL: &str = "custom_fields_deal";       // entity token "deal"
pub const KEY_CUSTOM_FIELDS_ORG: &str = "custom_fields_organization"; // "organization"
pub const KEY_CUSTOM_FIELDS_PEOPLE: &str = "custom_fields_person";    // "person"
pub const KEY_CUSTOM_FIELDS_ACTIVITY: &str = "custom_fields_activity";// "activity"
pub const TTL_CUSTOM_FIELDS: u64 = 3600;

// src/api/mod.rs
list_custom_field_definitions(&self, entity_type: Option<&str>, limit: u64, offset: u64)
     -> Result<ApiListResponse<CustomFieldDefinition>>;   // order position ASC; includes tombstones
// src/api/models.rs
pub struct CustomFieldDefinition { id, entity_type, name, #[serde(rename="type")] type_: String,
     config: Option<serde_json::Value>, required: bool, position: Option<f64>,
     show_in_list: bool, created_at, updated_at }
// cache invalidation contract: 12-01's mutations invalidate prefix "custom_fields_" — the resolver
// only READS the cache (get) and WRITES on fetch (set); it never invalidates.
```

From src/context.rs (the resolver's inputs — all fields exist today):
```rust
pub struct AppContext { pub client: PipeliteClient, pub quiet: bool, pub dry_run: bool,
     pub cache: Option<CacheStore>, /* + config, output_format, verbose, color, no_input */ }
```

The 8 handler call sites being rewired (verified in-repo, current line numbers):
```
src/commands/deals/create.rs       :30 has_flags (Validation exit 1 at :33) · :91 parse_custom_fields
src/commands/deals/update.rs       :31 has_flags (InvalidInput) · :58 has_flags (no-flags gate) · :112 parse
src/commands/orgs/create.rs        :27 has_flags (Validation exit 1 at :30) · :69 parse
src/commands/orgs/update.rs        :28 has_flags (InvalidInput) · :52 has_flags (no-flags gate) · :88 parse
src/commands/people/create.rs      :29 has_flags (Validation exit 1 at :32) · :91 parse
src/commands/people/update.rs      :30 has_flags (InvalidInput) · :56 has_flags (no-flags gate) · :104 parse
src/commands/activities/create.rs  :29 has_flags (Validation exit 1 at :32) · :81 parse
src/commands/activities/update.rs  :36 has_flags (InvalidInput) · :64 has_flags (no-flags gate) · :75 parse · :79 → update_with_null_completed (raw payload inserts custom_fields at :181-182; param at :155)
```
= 12 has_flags sites (8 stdin-exclusivity + 4 update no-flags gates), 8 parse sites, 4 exit-code normalizations.

Exclusivity error shapes today (do NOT copy the create handlers' shape):
```rust
// WRONG template (deals/orgs/people/activities create.rs) — Validation = exit 1:
return Err(CliError::Validation { detail: "--stdin and individual field flags are mutually exclusive".into(), hint: … });
// CORRECT template (all 4 update.rs) — already InvalidInput = exit 2:
return Err(CliError::InvalidInput { detail: …, hint: … });
// The remaining 2 Validation uses in each create.rs (batch stdin: no-data + bad JSON) are the
// Phase 7 BATCH contract — NOT in scope; leave them untouched.
```

parse_custom_fields today (src/batch.rs:406-421 — DELETED this plan; semantics absorbed by the resolver):
```rust
pub fn parse_custom_fields(pairs: &[String]) -> Result<Option<serde_json::Value>>
// split_once('=') miss → CliError::Validation (exit 1)  ← the resolver's replacement uses
// InvalidInput (exit 2): a malformed k=v is structural input, BATCH-04 precedent
// map.insert(key, Value::String(value))  ← the bug this phase fixes
```

Resolver call shape in every handler (replaces `let custom_fields = batch::parse_custom_fields(&args.custom_field)?;`):
```rust
let custom_fields = custom_fields::resolve_custom_fields(
    ctx, CfEntityType::Deal, &args.custom_field, ctx.dry_run,
    args.custom_field_json.as_deref(),
).await?;
```
(single entry point — the dry_run branch lives INSIDE the resolver so the 8 handlers stay identical; custom_field_json is threaded in so the both-flags check and the verbatim path share one funnel.)

Cache-through fetch pattern to mirror (src/prompt.rs:162-187 get_pipelines_cached): cache.get first; on miss loop `offset += 100` until `data.len() < 100` (server max limit 100 — a one-page fetch silently misses definitions; RESEARCH Don't-Hand-Roll).

From src/error.rs:87-93 (exit codes): InvalidInput/MissingInput → 2 · Validation → 1.

From tests/common/mod.rs (reuse): cmd() unreachable 127.0.0.1:1 · cmd_with_server(url) + .env("HOME", tmp) · spawn_head_capturing_stub_server(&[(u16,&str)]) -> (base, counter, heads, bodies); heads lowercased; bodies split at first "\r\n\r\n".

Wire fixtures for the stub (definitions response — 12-01 serializer shape; entity_type "deal"):
```json
{"data":[
 {"id":"cf1","entity_type":"deal","name":"price","type":"number","config":null,"required":false,"position":10000.0,"show_in_list":false,"created_at":"2026-01-01T00:00:00Z","updated_at":"2026-01-01T00:00:00Z"},
 {"id":"cf2","entity_type":"deal","name":"done","type":"boolean","config":null,"required":false,"position":20000.0,"show_in_list":false,"created_at":"…","updated_at":"…"},
 {"id":"cf3","entity_type":"deal","name":"status","type":"single_select","config":{"options":["new","won","lost"]},"required":false,"position":30000.0,"show_in_list":false,"created_at":"…","updated_at":"…"},
 {"id":"cf4","entity_type":"deal","name":"tags","type":"multi_select","config":{"options":["a","b","c"]},"required":false,"position":40000.0,"show_in_list":false,"created_at":"…","updated_at":"…"},
 {"id":"cf5","entity_type":"deal","name":"close_by","type":"date","config":null,"required":false,"position":50000.0,"show_in_list":false,"created_at":"…","updated_at":"…"},
 {"id":"cf6","entity_type":"deal","name":"calc_total","type":"formula","config":null,"required":false,"position":60000.0,"show_in_list":false,"created_at":"…","updated_at":"…"}
],"meta":{"total":6,"offset":0,"limit":100}}
```
Server value-validation facts (binding, RESEARCH matrix): v1 accepts ANY keys/values (z.record(z.string(), z.unknown())) — every check below is client-side; server STRIPS formula keys on deals/activities/people writes (organizations create passes through — immaterial); entity update MERGES the blob (keys cannot be deleted, only overwritten).
</interfaces>

**CONTEXT AMENDMENTS applied (orchestrator, fact-driven from research — binding):**
1. **Select strictness RESOLVED — strict, exit 2**: select/multi_select values are validated client-side against the definition's config.options; an unknown option → `CliError::InvalidInput` exit 2 with a hint listing the valid options — the webhooks 13-event precedent (WHOK-02). This resolves RESEARCH Open Question 1 as STRICT (the CONTEXT premise "server validates options" is false on the API-key path, and a silent bad option poisons the dropdown corpus). Edge: a definition whose config.options is missing or not an array (the server never validates config shape) → membership check impossible → send the string with a one-line stderr note "definition has no options configured — sent without validation" (quiet-suppressible); do NOT hard-block writes on server-side config sloppiness.
2. **Formula keys RESOLVED — refuse, exit 2**: writing to a `formula`-type definition → `CliError::InvalidInput` exit 2 (detail names the field + type; hint: "formula fields are server-computed — the server strips values written to them; omit this field"). This resolves Open Question 2 as REFUSE, overriding research recommendation A3 (warn-and-send): a guaranteed-silent no-op is a lie on the wire, and the CLI's one job here is honesty. `--custom-field-json` remains the unvalidated bypass — verbatim means verbatim; the server still strips formula keys on its side.
3. **Blob keys = definition NAMES**: the resolver matches `--custom-field KEY` against `definition.name` (validateFieldValues reads values[def.name], custom-fields.ts:41) — NOT id. SERVER-API-DIFF §C was wrong; existing name-keyed behavior is confirmed correct.
4. **Exclusivity normalization**: all 12 has_flags sites gain `|| args.custom_field_json.is_some()`; the 4 create handlers' `--stdin` exclusivity error switches `CliError::Validation` → `CliError::InvalidInput` (exit 2), matching the 4 update handlers. The batch-path Validation errors in create.rs (no-data-on-stdin, bad JSON) are NOT touched (Phase 7 contract).
5. **`missing '='` → exit 2**: with parse_custom_fields deleted, the resolver owns the k=v split; a pair without `=` is structural input → `CliError::InvalidInput` (BATCH-04 structural-exit-2 precedent), detail naming the pair, hint showing key=value form. (Deviation from the old Validation shape is deliberate — the old function and its exit code are deleted together.)

**Planner decisions pinned (agent's-discretion areas):**
- **One resolver entry point**: `pub async fn resolve_custom_fields(ctx: &AppContext, entity: CfEntityType, pairs: &[String], dry_run: bool, json_passthrough: Option<&str>) -> Result<Option<serde_json::Value>>` — the dry-run branch and the json bypass live INSIDE the resolver so all 8 handlers share one identical call shape. Returns the final `custom_fields` value (json object verbatim when json_passthrough is set, after the both-flags check).
- **Inference table** (infer_typed_value — pure, unit-tested): text/url/lookup/file → `Value::String` (never fail); date → ISO string passthrough (never fail — the server has no date case); number → parse i64 first (`Number::from`), fall back to f64 (`Number::from_f64`) so `4` emits exactly `4` (never `4.0`), non-numeric → exit 2 hint "expects a number, e.g. --custom-field price=4"; boolean → strict lowercase "true"/"false" → `Value::Bool`, anything else → exit 2 hint "expects true or false"; single_select → string + option validation; multi_select → comma-split (empty segments skipped) → `Value::Array` of strings + EVERY element validated; unknown definition name → `Value::String` + aggregated warning; formula → exit 2 refusal.
- **Warnings aggregate to ONE stderr line per invocation**: all unknown keys listed together ("not a defined field(s) — sent as string(s): k1, k2"), quiet-suppressible. The dry-run cache-miss note is its own line, quiet-suppressible, wording: "custom-field definitions not cached — values sent as raw strings (run once without --dry-run to warm the cache)".
- **Not the --stdin batch paths**: batch create/update deserialize typed structs — user JSON is already typed and passes verbatim; the resolver serves the 8 flag paths only (CONTEXT blast-radius pin).
- **Reference-then-fan-out**: deals create+update are rewired first (Task 1) as the reference implementation; orgs/people/activities mirror mechanically (Task 2). activities/update resolves ONCE at line 75 and threads the typed value through update_with_null_completed's raw payload (Pitfall 6 — exactly 8 rewired files, the raw path keeps its single insert site).
- **Number i64-first is wire-pinned**: the canonical test asserts on the RAW captured body string (`"price":4` present, `"price":"4"` absent) AND `as_i64() == Some(4)` — json!() comparison can mask 4.0 (RESEARCH Pitfall 2).

**Existing tests — full inventory (the brief demands enumeration):**
1. **tests/headless_test.rs::stdin_and_flags_mutually_exclusive (line ~146)** — `deals create --stdin --title X`: asserts `.failure()` + stderr "mutually exclusive". TODAY exit 1 (deals/create.rs:33 Validation). AFTER normalization exit 2. `.failure()` matches any nonzero so the test would still pass — Task 2 UPDATES it to pin `.code(2)` explicitly (semantics lock).
2. **src/batch.rs unit tests `parse_custom_fields_rejects_missing_equals` + `parse_custom_fields_builds_object` (lines ~462-471)** — DELETED together with parse_custom_fields (zero callers remain after rewiring; leaving it would be dead code). The missing-equals behavior moves into the resolver as InvalidInput exit 2 (amendment 5) and is re-covered by resolver unit + wire tests.
3. **tests/deals_integration.rs:138,203 + orgs_integration.rs:40** — assert `--custom-field` appears in entity help. UNAFFECTED (flag remains). Task 3 EXTENDS the deals asserts to also require `--custom-field-json` (truthfulness for the new flag).
4. **tests/batch_error_test.rs::batch_update_stdin_with_field_flag_exits_2** — `deals update --stdin --title` → `.code(2)`: already green (update handlers are InvalidInput today); stays green, untouched.
5. **tests/dead_flags_test.rs** — pipelines/stages `--custom-field` rejection (exit 2 + hint): unrelated surface, untouched.
6. **tests/batch_error_test.rs DEAL_SUCCESS_BODY / deals_integration fixtures** — contain `"custom_fields":null` in RESPONSE fixtures only; the wire format of responses is unchanged this phase. Untouched.
No existing test exercises `--custom-field k=v` against a stub server for the 8 handlers (grep-verified) — the string-typed wire behavior had NO coverage; this plan adds it.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Canonical price=4 wire test RED + src/custom_fields.rs resolver + deals create/update reference rewiring</name>
  <files>src/custom_fields.rs, src/commands/deals/create.rs, src/commands/deals/update.rs, src/cli/deals.rs, tests/typed_writing_stub_test.rs</files>
  <behavior>
    Unit (src/custom_fields.rs #[cfg(test)] — the inference table):
    - number: "4" → Number with as_i64 == Some(4) (NOT 4.0); "-3" → i64; "4.5" → f64 as_f64 == 4.5; "abc" → Err containing "number" (field+type in message); "" → Err
    - boolean: "true" → Bool(true); "false" → Bool(false); "True"/"1"/"yes" → Err containing "true or false"
    - date/url/lookup/file/text: any string → Value::String passthrough (never Err)
    - single_select: "new" with options ["new","won"] → String("new"); "bogus" → Err whose message lists "new" and "won"; config null → Ok(String) (no options to check); config.options not an array → Ok(String)
    - multi_select: "a,b" → Array["a","b"]; "a,,b," → ["a","b"] (empties skipped); element "bogus" vs options ["a","b"] → Err listing options
    - formula: any → Err containing "formula"
    - missing '=': "noequals" → Err containing "key=value"
    Integration (tests/typed_writing_stub_test.rs — NEW file; tests 1-3 written RED before deals create is rewired):
    1. number_int_body (FIRST, RED): stub [(200 defs), (201 created)]; HOME=tmp (cold); deals create --title T --stage s1 --custom-field price=4 → exit 0; counter == 2 (GET defs + POST); LAST body parses: custom_fields.price.is_number() AND as_i64() == Some(4); RAW body string contains "\"price\":4" and does NOT contain "\"price\":\"4\"" and does NOT contain "4.0"
    2. number_float_body: --custom-field price=4.5 → as_f64 == Some(4.5)
    3. dry_run_cold_cache_zero_http (RED): stub [(201 created)] only — NO defs response scripted; HOME=tmp; --dry-run --custom-field price=4 → exit 0; counter == 0 (ZERO HTTP — criterion 4); body has custom_fields.price == "4" (string fallback); stderr contains "not cached"
    4. dry_run_quiet_suppresses_note: same + --quiet → stderr does NOT contain "not cached"
    5. dry_run_warm_cache_types: two runs, one stub [(200 defs), (201)]: run 1 warm-up via `custom-fields list --entity-type deals` (exactly 1 request — do NOT warm with a live create, that would make 2 requests), run 2 --dry-run --custom-field price=4 → counter == 1 (the warm-up list GET only; the dry-run run adds 0 requests), body price as_i64 == Some(4) (warm cache types correctly under dry-run — cache-only ≠ strings-only), stderr LACKS "not cached"
    6. number_bad_warm_zero_post: two runs [(200 defs), (200 defs)] … run 2: deals update d1 --custom-field price=abc (warm cache) → exit 2, counter == 0 across run 2 (zero HTTP when warm), stderr contains "number"
    7. update_typed_body: deals update d1 --custom-field price=4 (cold) → heads: get defs then put /api/v1/deals/d1; PUT body custom_fields.price as_i64 == Some(4)
  </behavior>
  <action>
1. **tests/typed_writing_stub_test.rs — RED first**: `mod common;` + tests 1-3 BEFORE any resolver wiring; `cargo test --test typed_writing_stub_test` must FAIL (resolver missing / body still string-typed). The defs fixture is the six-definition deal fixture from the interfaces block (serialized into the test as a raw string).

2. **src/custom_fields.rs (NEW)** — the shared resolver, structured as: (a) `pub enum CfEntityType { Deal, Organization, Person, Activity }` with three accessors: `api_token()` → "deal"/"organization"/"person"/"activity" (the server enum), `cache_key()` → the 12-01 KEY_CUSTOM_FIELDS_* constants, `from_str` for handler convenience if useful. (b) `struct FieldDefinition` reuse: import CustomFieldDefinition from api::models (do NOT duplicate the struct). (c) `async fn fetch_definitions_cached(ctx, entity) -> Result<Vec<CustomFieldDefinition>>`: ctx.cache.get(cache_key) first; on miss `list_custom_field_definitions(Some(api_token), 100, offset)` loop until partial page (prompt.rs:162-187 pattern), cache.set(cache_key, &defs, TTL_CUSTOM_FIELDS). (d) `fn resolve_cache_only(ctx, entity, pairs) -> Result<Option<Value>>`: cache.get ONLY (None cache handle = miss); on miss emit the stderr note unless ctx.quiet; every key → Value::String via the missing-= check; zero HTTP by construction (no client field touched — the compiler-enforced proof is that this fn takes no client call path). (e) `fn infer_typed_value(field: &str, def_type: &str, config: Option<&Value>, raw: &str) -> Result<Value>` — the pure inference table from the pinned decisions (i64-first number, strict boolean, option membership via config.options array of strings, comma-split multi_select, formula refusal, missing-= structural error). (f) `pub async fn resolve_custom_fields(ctx, entity, pairs, dry_run, json_passthrough) -> Result<Option<Value>>`: FIRST the json funnel — if json_passthrough is Some: if !pairs.is_empty() → InvalidInput exit 2 ("--custom-field and --custom-field-json are mutually exclusive", hint names both); parse with serde_json::from_str::<Value> → must be Value::Object else InvalidInput exit 2 ("must be a JSON object"); return Some(object) verbatim. Else if pairs empty → Ok(None). Else if dry_run → resolve_cache_only path. Else fetch_definitions_cached → build a name→definition map (BY NAME, amendment 3) → for each pair: known key → infer_typed_value (map errors to InvalidInput with the FIELD NAME + definition type in the detail); unknown key → Value::String + collect for the single aggregated warning; formula → its own InvalidInput; emit the one warning line unless ctx.quiet; return Some(object).

3. **src/commands/deals/create.rs (reference rewiring)**: (a) extend the has_flags expression with `|| args.custom_field_json.is_some()`. (b) switch the exclusivity error `CliError::Validation` → `CliError::InvalidInput` (detail/hint text unchanged — only the variant changes; the two batch-path Validation errors stay untouched). (c) replace `let custom_fields = batch::parse_custom_fields(&args.custom_field)?;` (line 91) with the resolver call (CfEntityType::Deal, ctx.dry_run, args.custom_field_json.as_deref()). DealCreate.custom_fields is already Option<Value> — the typed value drops in unchanged. Respect --no-input: resolver performs no prompts, nothing to gate.

4. **src/commands/deals/update.rs (reference rewiring)**: same three changes at lines 31 (has_flags +json), the stdin exclusivity error is ALREADY InvalidInput — verify and leave, line 58 no-flags gate gains the json term (so `deals update d1 --custom-field-json '{...}'` alone counts as "has flags"), line 112 parse → resolver call.

5. **src/cli/deals.rs**: `#[arg(long = "custom-field-json", value_name = "JSON")] pub custom_field_json: Option<String>` on DealsCreateArgs AND DealsUpdateArgs (doc: "Raw JSON object written verbatim as custom_fields — bypasses type inference; mutually exclusive with --custom-field and --stdin"). Extend both after_help examples with one typed-write example: `pipelite deals create --title "Deal" --stage stg_001 --custom-field price=4  # number definitions store JSON numbers`.

6. **src/batch.rs**: DO NOT remove parse_custom_fields yet (orgs/people/activities still call it until Task 2 — removal happens there to keep every commit green).

7. **tests/typed_writing_stub_test.rs — GREEN**: implement until tests 1-7 pass. Warm-cache runs drive TWO invocations against ONE stub server (each consumes one scripted connection) with the SAME tmp HOME. Exit-code asserts use `.code(0/.2)` explicitly.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo test --test typed_writing_stub_test 2>&1 | tail -5 && test "$(grep -c '#[test]' tests/typed_writing_stub_test.rs)" -ge 7 && test "$(sed -n 's/^fn \([a-z_0-9]*\).*/\1/p' tests/typed_writing_stub_test.rs | head -1)" = "number_int_body" && cargo test --bin pipelite 2>&1 | tail -3 && test "$(grep -c 'pub async fn resolve_custom_fields' src/custom_fields.rs)" -eq 1 && test "$(grep -c 'pub enum CfEntityType' src/custom_fields.rs)" -eq 1 && test "$(grep -c 'KEY_CUSTOM_FIELDS_DEAL' src/custom_fields.rs)" -ge 1 && test "$(grep -c '100' src/custom_fields.rs)" -ge 1 && test "$(grep -c 'resolve_custom_fields' src/commands/deals/create.rs src/commands/deals/update.rs)" -eq 2 && test "$(grep -c 'parse_custom_fields' src/commands/deals/create.rs src/commands/deals/update.rs)" -eq 0 && test "$(grep -c 'custom-field-json' src/cli/deals.rs)" -ge 2 && test "$(grep -B6 'mutually exclusive' src/commands/deals/create.rs | grep -c InvalidInput)" -eq 1 && test "$(grep -c 'not cached' src/custom_fields.rs)" -ge 1 && test "$(grep -c 'as_i64' tests/typed_writing_stub_test.rs)" -ge 2 && echo GATE-OK</automated>
  </verify>
  <done>
    The canonical test (written RED first) proves price=4 lands as JSON number 4 on the wire — raw body contains "price":4, never the quoted or float form — with the definitions GET + POST both counted. --dry-run with a cold cache makes ZERO requests, sends strings, and prints the warm-the-cache note (quiet-suppressible); with a warm cache it types correctly. Number/boolean/select/formula/missing-equals errors are InvalidInput exit 2. deals create+update are rewired on the shared resolver with the exclusivity check normalized to InvalidInput.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fan-out rewiring — remaining 6 handlers, --custom-field-json on all arg structs, 12 has_flags sites, exclusivity normalization, parse_custom_fields removal, test inventory updates</name>
  <files>src/custom_fields.rs, src/cli/orgs.rs, src/cli/people.rs, src/cli/activities.rs, src/commands/orgs/create.rs, src/commands/orgs/update.rs, src/commands/people/create.rs, src/commands/people/update.rs, src/commands/activities/create.rs, src/commands/activities/update.rs, src/batch.rs, tests/headless_test.rs, tests/typed_writing_stub_test.rs</files>
  <behavior>
    Integration (append 7 tests to tests/typed_writing_stub_test.rs — one per remaining entity + the 8th call site):
    1. orgs_create_typed: orgs create --name N --custom-field <org-number-field>=7 (org defs fixture with one number definition, entity_type "organization") → body custom_fields.<name> as_i64 == Some(7); counter == 2
    2. people_create_typed: people create --first-name F --last-name L --custom-field <field>=9 → as_i64 == 9 (people defs fixture)
    3. activities_create_typed: activities create --subject S --custom-field <field>=true on a boolean definition → body Bool(true) (activities defs fixture)
    4. activities_mark_undone_raw_path (the 8th site — Pitfall 6): activities update a1 --mark-undone --custom-field <bool-field>=true → stub [(200 defs), (200 raw-updated)]; counter == 2; LAST body contains "completed_at":null AND the typed custom_fields value (Bool true) — resolved ONCE, carried into the raw payload, not dropped and not doubled
    5. exclusivity_json_stdin: deals create --stdin '[{...}]' --custom-field-json '{}' → exit 2, counter == 0
    6. both_flags_zero_http_warm: warm cache run of deals create --title T --stage s1 --custom-field a=1 --custom-field-json '{}' (required flags present so check_missing does NOT fire first — the both-flags check must be the exit source) → exit 2, counter == 0 (both-flags check fires before the definitions fetch)
    7. update_no_flags_gate_counts_json: deals update d1 --custom-field-json '{"score":1}' alone (no other flags) → proceeds to the resolver and PUTs the verbatim object (proves the no-flags gate accepts json as "has flags" — NOT rejected as "nothing to update")
    Regression pins:
    8. headless_test stdin_and_flags_mutually_exclusive UPDATED: deals create --stdin --title → .code(2) EXPLICITLY (was implicit .failure() at exit 1) + stderr still contains "mutually exclusive"
  </behavior>
  <action>
1. **Mechanical mirror on 6 handlers** (deals create/update in Task 1 are the reference — copy their exact three changes): orgs/create.rs (has_flags line ~27 + Validation→InvalidInput at ~30 + parse at ~69 → CfEntityType::Organization), orgs/update.rs (~28 has_flags+json, ~52 no-flags gate+json, ~88 parse), people/create.rs (~29, ~32, ~91 → CfEntityType::Person), people/update.rs (~30, ~56, ~104), activities/create.rs (~29, ~32, ~81 → CfEntityType::Activity), activities/update.rs (~36, ~64, ~75). Each cli/orgs.rs, cli/people.rs, cli/activities.rs: add `custom_field_json: Option<String>` with the same long flag + doc to BOTH the Create and Update args structs (8 arg structs total across the phase). NO after_help changes needed outside deals (Task 1 set the wording precedent).

2. **activities/update.rs special care (Pitfall 6)**: the resolver call replaces parse at line 75 EXACTLY — the resulting Option<Value> already flows into `update_with_null_completed(ctx, id, args, custom_fields)` (line 79) whose raw payload inserts it at lines 181-182. Verify with a read: the resolver result must be computed BEFORE the `--mark-undone` early return and passed through untouched — resolve ONCE, consume in BOTH paths. The typed_writing_stub_test 4 pins the wire.

3. **src/batch.rs**: delete `parse_custom_fields` (lines ~406-421) and its two unit tests (~462-471). After this commit `grep -rn parse_custom_fields src/` returns ZERO hits (all 8 callers rewired in Tasks 1-2). The missing-equals semantics live on in the resolver (InvalidInput exit 2 — amendment 5).

4. **tests/headless_test.rs**: update `stdin_and_flags_mutually_exclusive` — `.failure()` → `.code(2)` (the normalization pin; stderr assert unchanged).

5. **tests/typed_writing_stub_test.rs**: append tests 1-7. The orgs/people/activities defs fixtures follow the deal fixture shape with entity_type set per entity and 1-2 definitions each (number + boolean suffice). The mark-undone test asserts BOTH "completed_at":null and the typed custom_fields key in the SAME raw body.

6. Run the full custom-field test subset: `cargo test --test typed_writing_stub_test --test headless_test --test batch_error_test` — the batch_error test (inventory #4) must stay green untouched.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo test --test typed_writing_stub_test 2>&1 | tail -5 && test "$(grep -c '#[test]' tests/typed_writing_stub_test.rs)" -ge 14 && test "$(grep -rl 'resolve_custom_fields' src/commands/deals/create.rs src/commands/deals/update.rs src/commands/orgs/create.rs src/commands/orgs/update.rs src/commands/people/create.rs src/commands/people/update.rs src/commands/activities/create.rs src/commands/activities/update.rs | wc -l)" -eq 8 && test "$(grep -rc 'parse_custom_fields' src/ --include='*.rs' | grep -v ':0' | wc -l)" -eq 0 && test "$(grep -rc 'custom_field_json' src/cli/deals.rs src/cli/orgs.rs src/cli/people.rs src/cli/activities.rs | awk -F: '{s+=$2} END {print s}')" -eq 8 && test "$(grep -rc 'custom_field_json.is_some()' src/commands/deals/create.rs src/commands/deals/update.rs src/commands/orgs/create.rs src/commands/orgs/update.rs src/commands/people/create.rs src/commands/people/update.rs src/commands/activities/create.rs src/commands/activities/update.rs | awk -F: '{s+=$2} END {print s}')" -eq 12 && test "$(grep -B6 'mutually exclusive' src/commands/orgs/create.rs src/commands/people/create.rs src/commands/activities/create.rs | grep -c InvalidInput)" -eq 3 && test "$(grep -c 'code(2)' tests/headless_test.rs)" -ge 1 && cargo test --test headless_test --test batch_error_test 2>&1 | tail -3 && cargo test --bin pipelite 2>&1 | tail -3 && echo GATE-OK</automated>
  </verify>
  <done>
    All 8 handlers call the ONE shared resolver (8 files, no per-entity copies); batch::parse_custom_fields and its tests are gone (zero grep hits); --custom-field-json exists on all 8 arg structs and is threaded into all 12 has_flags sites; the 4 create handlers' exclusivity errors are InvalidInput exit 2 (orgs/people/activities grep-proven, deals wire-proven); the activities --mark-undone raw path carries typed values (8th site pinned); headless test pins .code(2). batch_error_test stays green (Phase 7 contract untouched).
  </done>
</task>

<task type="auto">
  <name>Task 3: Full inference wire matrix + docs + CHANGELOG + full-suite gate</name>
  <files>tests/typed_writing_stub_test.rs, tests/deals_integration.rs, tests/orgs_integration.rs, docs/api-reference.md, CHANGELOG.md</files>
  <behavior>
    Integration (append 13 tests to tests/typed_writing_stub_test.rs — warm-cache unless stated, so validation claims are zero-HTTP):
    1. boolean_wire: done=true → body custom_fields.done == Bool(true); done=yes → exit 2, counter == 0, stderr contains "true or false"
    2. select_valid: status=new → String("new"); select_invalid_warm: status=bogus → exit 2, counter == 0, stderr contains "new" AND "won" AND "lost" (valid options listed)
    3. select_no_options_note: defs fixture where the select definition has config null → status=whatever → exit 0, String sent, stderr contains "no options configured"
    4. multi_select_wire: tags=a,c → Array["a","c"]; multi_select_invalid_warm: tags=a,bogus → exit 2, stderr lists "a" "b" "c"
    5. date_passthrough: close_by=2026-01-31 → String("2026-01-31")
    6. text_passthrough: notes_str=hello world (spaces in value) → String("hello world") (split on FIRST '=' only)
    7. unknown_key_warning: mystery=1 → body custom_fields.mystery == "1" (string), exit 0, stderr contains "not a defined field"; with --quiet the warning is absent
    8. formula_refused: calc_total=99 → exit 2, counter == 0 (warm), stderr contains "formula" AND "server" (hint names server-computed stripping)
    9. missing_equals: --custom-field noequals → exit 2, stderr contains "key=value", counter == 0 (warm — the k=v split precedes any fetch? NO: cold the defs fetch happens first ONLY when pairs parse — pin: the split of ALL pairs happens BEFORE the definitions fetch, so counter == 0 even cold)
    10. json_verbatim_nested: --custom-field-json '{"score":{"nested":[1,2,null]},"ratio":1.5}' → body custom_fields deep-equals the input object EXACTLY (nested object, array, null, float all untouched)
    11. json_non_object: --custom-field-json '[1,2]' → exit 2, counter == 0, stderr contains "object"
    12. cache_round_trip_two_runs: one stub [(200 defs), (201), (201)] + one tmp HOME: run 1 deals create --custom-field price=4 (cold: 2 requests), run 2 same (warm: 1 request — POST only)
    13. resolver_unit_matrix already in src/custom_fields.rs (Task 1) — this task adds none there
    Help/docs asserts:
    14. deals_integration.rs deals_create_help + deals_update_help EXTENDED: also contain "--custom-field-json"; orgs_integration.rs orgs_create_help likewise
    - docs/api-reference.md: the four entity flag tables' --custom-field rows updated + --custom-field-json rows added; a "Typed custom-field writing" note documenting the inference rules, client-side validation (server validates nothing), the dry-run cache note, and the formula refusal
    - CHANGELOG.md: behavior note under the v1.1 unreleased section: custom-field values are now type-correct (numbers stored as JSON numbers, booleans as true/false, multi-select as arrays) instead of always strings; --custom-field-json added; both + --stdin exclusivity now exit 2
  </behavior>
  <action>
1. **tests/typed_writing_stub_test.rs**: append the 13 tests per the behavior list. Ordering pin for test 9: in resolve_custom_fields, the missing-= validation of ALL pairs happens BEFORE the definitions lookup — a structural k=v failure is zero-HTTP even on a cold cache (the fetch would be wasted work; BATCH-04 "fully validate before the first HTTP call" spirit). Warm-cache tests drive two invocations on one stub as before.

2. **tests/deals_integration.rs + tests/orgs_integration.rs**: extend the three help assertions (lines 138, 203, 40) with `.stdout(predicate::str::contains("--custom-field-json"))` — one line each, the new flag is part of the documented surface.

3. **docs/api-reference.md**: in the deals/orgs/people/activities create+update flag tables (the four `--custom-field | string | No | key=value (repeatable)` rows around lines 120/163/194/225): update the row text to "key=value (repeatable) — typed against cached definitions" and add a `--custom-field-json | json | No | raw object, verbatim` row to each of the 8 tables. After the last entity section (before `## Error Codes`), add a short **Typed custom-field writing** note block: values are type-correct against cached definitions (number → JSON number with i64 precision, boolean → true/false, date → ISO string, select → validated string, multi_select → comma-split array); the server does NOT validate custom-field values on the API — the CLI does, client-side: unknown option → exit 2 listing valid options; formula fields refuse writes (server strips them); unknown field names are sent as strings with a warning; `--dry-run` uses the cache only (cold cache → strings + a note; run once without --dry-run to warm); `--custom-field-json` bypasses inference verbatim and is mutually exclusive with `--custom-field` and `--stdin` (exit 2).

4. **CHANGELOG.md**: under `## v1.1 (unreleased)` — add a **Changed/Fixed** entry (match the existing entry voice): "`--custom-field` values are now type-correct — numbers are stored as JSON numbers (price=4 → 4, not \"4\"), booleans as true/false, multi-select as JSON arrays — resolved from cached field definitions. The server does not validate custom-field values via the API, so the CLI now validates client-side: select values must match the definition's options (exit 2 otherwise) and formula fields refuse writes. Adds `--custom-field-json` for verbatim raw objects; it is mutually exclusive with `--custom-field` and `--stdin` (exit 2, consistent across all 8 create/update commands). `--dry-run` never fetches definitions — it falls back to the local cache and says so."

5. Run the FULL suite as the gate (`cargo test`): typed_writing (≥20), custom_fields stub (≥30 from 12-01), headless (updated pin), batch_error (untouched, green), deals/orgs integrations (extended), and everything else.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo test --test typed_writing_stub_test 2>&1 | tail -5 && test "$(grep -c '#[test]' tests/typed_writing_stub_test.rs)" -ge 20 && cargo test 2>&1 | tail -4 && test "$(grep -c 'custom-field-json' docs/api-reference.md)" -ge 4 && test "$(grep -c 'type-correct\|type correct' CHANGELOG.md)" -ge 1 && test "$(grep -c 'custom-field-json' tests/deals_integration.rs tests/orgs_integration.rs | awk -F: '{s+=$2} END {print s}')" -eq 3 && test "$(grep -c 'not a defined field' src/custom_fields.rs)" -ge 1 && test "$(grep -c 'formula' src/custom_fields.rs)" -ge 2 && test "$(grep -c 'server-computed\|server computes\|server strips' src/custom_fields.rs docs/api-reference.md)" -ge 1 && echo GATE-OK</automated>
  </verify>
  <done>
    The full inference matrix is wire-proven per type (number i64/f64, strict boolean, option-validated select + multi_select with valid-options hints, date/text passthrough, formula refusal, unknown-key aggregated warning with quiet suppression, dry-run cache-only note). --custom-field-json passes nested objects verbatim and rejects non-objects, partners, and --stdin at exit 2 pre-HTTP. Two-run cache round-trip proven (cold 2 requests → warm 1). Docs tables + typed-writing note + CHANGELOG entry shipped. FULL suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→CLI | k=v pairs and --custom-field-json are arbitrary user strings headed for a jsonb blob the server will store UNVALIDATED — the resolver is the only type gate in existence for API-key writes |
| CLI→server | What the CLI posts is what the DB stores: a string "4" in a number field poisons every downstream numeric filter, report, and formula — type confusion IS the threat this phase exists to kill |
| server→client | The server silently strips formula keys on writes — a value the user "sent" can vanish without a word unless the CLI refuses first |
| cache→CLI | Cached definitions drive inference — a stale type (field retyped text→number server-side) would mistype values until TTL; 12-01's prefix invalidation covers the CLI's own writes, TTL bounds the rest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Tampering (type confusion in jsonb) | --custom-field k=v → custom_fields blob | mitigate | The resolver itself: strict number (i64-first), strict boolean, option membership, comma-split arrays — wire-pinned by raw-body assertions (\"price\":4, as_i64 == Some(4)); nonconforming input exits 2 pre-POST naming field + type |
| T-12-07 | Tampering (formula spoofing) | writes to formula-type definitions | mitigate | Hard refusal, InvalidInput exit 2, hint explaining server-computed stripping — the server strips the key anyway (T-34-04), so any "success" would be a silent lie; --custom-field-json remains the documented unvalidated bypass |
| T-12-08 | Repudiation (silent merge / silent stringification) | --custom-field + --custom-field-json together; unknown keys | mitigate | Both-flags → exit 2 BEFORE any HTTP (warm-cache counter == 0 pinned); unknown keys sent as strings but announced by ONE aggregated stderr warning (quiet-suppressible) — nothing is silent |
| T-12-09 | Deception (dishonest dry-run) | --dry-run + cold cache would fetch, or lie about types | mitigate | Cache-only branch: zero HTTP by construction (counter == 0 pinned with NO defs response even scripted), strings + visible note; warm cache types honestly; note suppressed only by --quiet |
| T-12-10 | DoS (self-inflicted, partial pagination) | one-page definitions fetch missing fields beyond limit 100 | mitigate | Auto-pagination loop to partial page (get_pipelines_cached pattern) — a resolver that silently misses definitions would misinfer types; unit + round-trip tests cover fetch+cache |
| T-12-SC | Tampering | cargo installs | accept | No new crates — RESEARCH § Package Legitimacy Audit: nothing to audit |
</threat_model>

<verification>
- `cargo test` full suite green: typed_writing_stub_test ≥20 wire tests, resolver unit matrix, custom_fields_stub_test (12-01, ≥30) still green, headless/batch_error/dead_flags inventories in their pinned states
- The canonical bug is dead: raw captured body contains "price":4 — is_number() AND as_i64() == Some(4) AND no "price":"4" and no 4.0 anywhere in the body (string + parsed double assertion, Pitfall 2)
- All 8 handlers on ONE resolver (grep -l == 8); 12 has_flags sites carry custom_field_json.is_some(); 4 create-handler exclusivity errors are InvalidInput; parse_custom_fields has zero grep hits in src/
- Zero-HTTP pins (counter == 0): both-flags (warm), json non-object, json+stdin, number/boolean/select/multi_select validation failures (warm), formula refusal (warm), missing-equals (even cold — split precedes fetch), stdin+flags exclusivity
- Dry-run honesty: cold cache + --dry-run → counter == 0 with strings + note (quiet suppresses); warm cache + --dry-run → typed values, no note
- Verbatim bypass: --custom-field-json deep-equals input incl. nested objects/arrays/null/floats; exclusivity with --custom-field AND --stdin at exit 2
- No unwrap()/expect() in production code (resolver included); every error carries a hint naming the field, its type, and the expected input shape; --quiet suppresses exactly the two advisory lines (warm-the-cache note, unknown-key warning) and nothing else
</verification>

<success_criteria>
- CFLD-02 (ROADMAP SC-2): --custom-field price=4 on a number definition stores JSON number 4 — type-correct writes for number/boolean/date/array/select resolved from cached definitions (matched by NAME), client-validated because the server validates nothing
- CFLD-03 (SC-3): --custom-field-json bypasses inference verbatim; mutual exclusivity with --custom-field and --stdin at exit 2 pre-HTTP across all 8 handlers
- SC-4: --dry-run never triggers a definitions fetch — cache-only fallback with a visible (quiet-suppressible) note; warm-cache dry-runs still type correctly
- Amendments honored: strict select/multi_select option validation (exit 2 listing valid options — webhooks precedent), formula writes REFUSED (exit 2 — server strips them), name-keyed resolution, 12-site exclusivity normalization to exit 2, parse_custom_fields deleted with its Validation-shaped k=v error
- Blast radius contained: exactly 8 handler files rewired on one shared module; batch --stdin typed paths untouched (Phase 7 contract green); activities raw path single-resolution pinned
</success_criteria>

<output>
After completion, create `.planning/phases/12-custom-fields-definitions-typed-writing/12-02-SUMMARY.md`
</output>
