---
phase: 09-workflow-runs-templates-docs
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - src/api/mod.rs
  - src/cli/docs.rs
  - src/cli/mod.rs
  - src/commands/docs.rs
  - src/commands/mod.rs
  - src/main.rs
  - tests/docs_stub_test.rs
  - tests/help_examples_test.rs
autonomous: true
requirements: [DOCS-01]

must_haves:
  truths:
    - "`pipelite docs` fetches the OpenAPI 3.1 spec and pretty-prints it to stdout WITHOUT sending an Authorization header (ROADMAP SC-5)"
    - "`--save FILE` writes the spec, creates missing parent directories, prints a one-line confirmation, and refuses to overwrite an existing file unless --force (exit 2 with the --force hint)"
    - "Docs-endpoint errors flow through the Phase 8 error layer carrying the locked hint 'the server may not expose the docs endpoint — check server version' with the server's detail preserved"
    - "--format is accepted but ignored, and the help says so (the spec is JSON, not tabular)"
  artifacts:
    - path: "src/api/mod.rs"
      provides: "get_docs() building a dedicated headerless reqwest::Client (same 5s/30s timeouts) — never self.client, which carries the Authorization default header"
      contains: "fn get_docs"
    - path: "src/cli/docs.rs"
      provides: "DocsArgs {--save FILE, --force} with after_help examples and the --format-ignored note"
      contains: "force"
    - path: "src/commands/docs.rs"
      provides: "Handler: pretty-print to stdout, --save with create_dir_all + exists refusal, error re-wrap substituting the docs hint, quiet-aware confirmation"
      contains: "may not expose the docs endpoint"
    - path: "tests/docs_stub_test.rs"
      provides: "DOCS-01 integration coverage: no-auth request head, pretty JSON, save/force/parent-dirs, 404 + legacy-500 error hints, format ignored"
      contains: "authorization"
  key_links:
    - from: "src/commands/docs.rs"
      to: "src/api/mod.rs get_docs"
      via: "the only consumer of the unauthenticated fetch"
      pattern: "get_docs\\("
    - from: "src/api/mod.rs get_docs"
      to: "dedicated headerless client"
      via: "builds reqwest::Client::builder() locally; self.client (Authorization default header) must not appear in the method body"
      pattern: "reqwest::Client::builder"
    - from: "src/commands/docs.rs error path"
      to: "Phase 8 error layer"
      via: "catches NotFound/Api preserving detail, substitutes the CONTEXT-locked docs hint (handle_response's generic 404 hint would otherwise win)"
      pattern: "may not expose the docs endpoint"
    - from: "src/main.rs"
      to: "Commands::Docs dispatch"
      via: "Docs(DocsArgs) variant on Commands + dispatch arm"
      pattern: "Commands::Docs"
---

<objective>
Add the `pipelite docs` command: fetch the server's OpenAPI 3.1 spec from the public `/api/v1/docs` route with a deliberately headerless HTTP client, pretty-print it (or `--save FILE` with overwrite refusal and parent-dir creation), and route failures through the Phase 8 error layer with the locked docs-specific hint.

Purpose: DOCS-01 — users and agents can read the server's API contract from the terminal. The security-relevant subtlety: the shared client leaks the API key via default headers onto every request, so docs MUST use a bare client instance, proven by a stub test that inspects the raw request head.
Output: get_docs client method, Docs CLI + handler, tests/docs_stub_test.rs, extended help examples, full suite green.
</objective>

<execution_context>
@/home/pedro/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/pedro/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-workflow-runs-templates-docs/09-CONTEXT.md (locked decisions — docs Command)
@.planning/phases/09-workflow-runs-templates-docs/09-RESEARCH.md (§ Server Contract 4, § How the Unauthenticated Client Path Works, § Recommended Approach DOCS-01, § Pitfalls 4/5)
@.planning/REQUIREMENTS.md (DOCS-01 text)
@.planning/phases/09-workflow-runs-templates-docs/09-02-SUMMARY.md (wave 2 landed the last Commands-enum/main.rs edits this plan builds on)
@CLAUDE.md
@docs/SKILL.md

<interfaces>
<!-- Existing contracts the executor needs. Extracted from the live codebase (post-Phase-8). -->

From src/api/mod.rs — why the dedicated client is mandatory:
```rust
// from_credentials (line ~110) installs AUTHORIZATION (+ ACCEPT + USER_AGENT) in reqwest's default_headers,
// which apply to EVERY request from that client instance — there is no per-request removal.
pub async fn handle_response<T: DeserializeOwned>(&self, response, surface: &'static str) -> Result<T>
//   404 -> NotFound { detail, hint: "The requested resource was not found." }   <- generic hint, docs must re-wrap
//   catch-all -> Api { status, detail, hint: "The server returned an error. Try again later." }
// parse_rfc7807 fallback chain ends with legacy `error`/`message` keys -> a 500 body {"error":"OpenAPI specification not available"}
//   renders detail "OpenAPI specification not available" (research-verified legacy shape, route.ts:28-34)
// map_request_error(&self, e) -> CliError::Connection (timeouts/connect failures)
```

Verified docs contract (RESEARCH § Server Contract 4):
```json
// GET /api/v1/docs — NO withApiAuth wrapper: public by design; Content-Type application/json; Cache-Control public (irrelevant to CLI)
// success body: the parsed openapi.yaml as JSON ("openapi": "3.1.0", ~87KB)
// error: older server -> standard 404 (route absent); spec-file-missing -> 500 {"error":"OpenAPI specification not available"} (NOT RFC 7807)
```

From tests/common/mod.rs (created by 09-01 — reuse):
```rust
pub fn cmd_with_server(url: &str) -> Command
pub fn spawn_head_capturing_stub_server(script: &[(u16, &str)]) -> (String, Arc<AtomicUsize>, Arc<Mutex<Vec<String>>>, Arc<Mutex<Vec<String>>>)
// (url, request counter, request heads lowercased, full request bytes) — 09-02 extended the helper with a second shared Vec of bodies;
// recorded request heads (lowercased) are the no-Authorization proof surface
```

From src/cli/mod.rs / src/main.rs (post-09-02): Commands enum already carries Templates; add Docs(DocsArgs) the same way — variant + use + dispatch arm `Commands::Docs(ref args) => commands::docs::run(ctx, args).await`; `pub mod docs;` in cli/mod.rs and commands/mod.rs.
</interfaces>

**Do NOT touch (locked decisions):**
- NO Authorization header on the docs request — the locked decision; the dedicated headerless client instance is the implementation (research Pitfall 4: never `self.client.get(...)` inside get_docs).
- Raw spec only — no summary-table rendering (deferred/rejected per CONTEXT).
- `--format` stays ACCEPTED (global flag, can't be un-registered) but ignored, with the note in help.
- --save refuses to overwrite unless --force; creates missing parent dirs (fs::create_dir_all on the parent only).
- Error hints: the CONTEXT hint replaces the generic 404/Api hint while PRESERVING the server's detail — do not modify handle_response itself (command-level re-wrap keeps the shared mapper untouched, research Pitfall 5).
- Docs responses are NEVER cached (large, server-owned freshness).
- No unwrap()/expect(); no new crates.

**Existing tests that break: NONE (verified).** help_examples_test.rs is EXTENDED (files_modified).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unauthenticated get_docs + Docs command (pretty-print, --save/--force, error re-wrap)</name>
  <files>src/api/mod.rs, src/cli/docs.rs, src/cli/mod.rs, src/commands/docs.rs, src/commands/mod.rs, src/main.rs</files>
  <action>
1. **src/api/mod.rs — get_docs**: `pub async fn get_docs(&self) -> Result<serde_json::Value>` placed after the template methods. Build a LOCAL `reqwest::Client::builder().connect_timeout(Duration::from_secs(5)).timeout(Duration::from_secs(30)).build().context("Failed to build HTTP client")?` — deliberately WITHOUT default_headers so no Authorization, Accept-json-only, or UA baggage rides along (research's verified approach; Duration is already imported in this file). GET `{self.base_url}/api/v1/docs` (base_url already trimmed of trailing slashes), send via `self.send_with_retry(req).await?` (429 retry free of charge — send_with_retry is a method on self and does not touch the authenticated client), then `let spec: serde_json::Value = self.handle_response(response, "docs").await?` (Value is DeserializeOwned; 404/500 map through the standard arms; forbidden_hint("docs") falls through to the default — unreachable on a public route). Doc comment states WHY the local client exists (public route, locked no-auth decision, Pitfall 4) — phrase it as "the shared authenticated client", never the literal `self.client`, so the verify gate counting that token inside the method body stays at zero. The method body must not reference `self.client`.

2. **src/cli/docs.rs (NEW)**: `#[derive(Args)] pub struct DocsArgs { /// Write the spec to FILE (creates missing parent directories) #[arg(long)] pub save: Option<String>, /// Allow --save to overwrite an existing file #[arg(long)] pub force: bool }` with a `--format is ignored` note in the DocsArgs doc comment, and after_help on the Commands variant: "Examples:\n  pipelite docs\n  pipelite docs --save spec.json\n  pipelite docs --save dir/spec.json --force\n\nNote: --format is ignored — the OpenAPI spec is JSON, not tabular output." Wire `Docs(DocsArgs)` into Commands in src/cli/mod.rs (after Templates), plus the use line; add `pub mod docs;` to src/cli/mod.rs and src/commands/mod.rs; add the dispatch arm in src/main.rs.

3. **src/commands/docs.rs (NEW)**: `pub async fn run(ctx: &AppContext, args: &DocsArgs) -> Result<()>`:
   - Fetch: match ctx.client.get_docs().await — on Err, re-wrap preserving detail: match `CliError::NotFound { detail, .. }` and `CliError::Api { status, detail, .. }` rebuilding each variant with the SAME detail and hint "the server may not expose the docs endpoint — check server version" (CONTEXT-locked wording). Other variants (Connection, Auth) pass through untouched.
   - No --save: `println!("{}", serde_json::to_string_pretty(&spec)?)` — always pretty-printed regardless of --format (the flag is ignored by design).
   - --save FILE: if the path exists and !args.force → CliError::InvalidInput { detail: format!("Refusing to overwrite existing file: {FILE}"), hint: "Use --force to overwrite: pipelite docs --save <file> --force" } (exit 2 — the overwrite check happens pre-HTTP, BEFORE the fetch, so a refusal makes zero HTTP calls; this ordering is pinned by the 0-request stub test); else fs::create_dir_all on the parent when the parent is non-empty (map errors through CliError with a hint), fs::write the PRETTY string, then unless ctx.quiet print one line `Wrote OpenAPI spec to {FILE} ({N} bytes)` to stdout (N = written byte count).
   - Nothing else on stdout; no cache interaction anywhere.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo build 2>&1 | tail -3 && test "$(grep -c 'fn get_docs' src/api/mod.rs)" -eq 1 && test "$(sed -n '/pub async fn get_docs/,/^    }$/p' src/api/mod.rs | grep -c 'self\.client')" -eq 0 && test "$(sed -n '/pub async fn get_docs/,/^    }$/p' src/api/mod.rs | grep -c 'reqwest::Client::builder')" -eq 1 && test "$(grep -c 'Commands::Docs' src/main.rs)" -eq 1 && test "$(grep -c 'Docs(DocsArgs)' src/cli/mod.rs)" -eq 1 && test "$(grep -cF 'may not expose the docs endpoint' src/commands/docs.rs)" -eq 1 && test "$(grep -c 'create_dir_all' src/commands/docs.rs)" -ge 1 && test "$(grep -ciF 'format is ignored' src/cli/docs.rs)" -ge 1 && echo GATE-OK</automated>
  </verify>
  <done>
    get_docs builds its own headerless client (grep-proven: no self.client inside the method body, exactly one local builder); the docs command pretty-prints the spec, saves with parent-dir creation and exists-refusal-unless---force before any HTTP, re-wraps 404/Api errors with the locked hint while preserving server detail, and documents the ignored --format in help. Wiring complete through Commands/dispatch.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Docs integration tests + help examples</name>
  <files>tests/docs_stub_test.rs, tests/help_examples_test.rs</files>
  <behavior>
    - pipelite docs against a 200 spec stub: exit 0; the captured request head contains NO line starting with "authorization:"; stdout contains "openapi": "3.1.0" (pretty-printed — the stub serves compact JSON, stdout renders it expanded)
    - pipelite docs --save tmpdir/sub/spec.json: exit 0; file exists and parses as JSON; parent dir was created; stdout contains "Wrote OpenAPI spec to"
    - --save onto an existing file without --force: exit 2; stderr contains "--force"; file content unchanged; 0 requests (refusal is pre-HTTP)
    - --save onto an existing file WITH --force: exit 0; file overwritten with the spec
    - --save under --quiet: file written; stdout does NOT contain "Wrote"
    - docs against a 404 stub: exit 1; stderr contains "may not expose the docs endpoint" AND preserves the served detail text; stderr does NOT contain "The requested resource was not found."
    - docs against a 500 legacy body {"error":"OpenAPI specification not available"}: exit 1; stderr contains "OpenAPI specification not available" without JSON quotes; exit code 1 (Api arm)
    - pipelite docs --format csv: exit 0; stdout still contains "openapi": "3.1.0" (flag ignored)
  </behavior>
  <action>
1. **tests/docs_stub_test.rs (NEW)**: `mod common;` and the eight tests from the behavior list. Spec body fixture: compact `{"openapi":"3.1.0","info":{"title":"Pipelite API","version":"1.0"}}` (compact in, pretty out — the stdout assertion on `"openapi": "3.1.0"` with the space proves pretty-printing happened, since the stub served it without). 404 fixture: `{"error":"Not Found","statusCode":404}` served with status 404. 500 fixture: the legacy body verbatim. Existing-file tests pre-create the file with sentinel content "sentinel" using tempfile::TempDir (already a dev-dependency); the unchanged-content assertion reads the file back and compares. The 0-request refusals use the stub counter (server IS reachable — proving the check fires before HTTP, stronger than the unreachable-server trick). The quiet test passes `-q` before the subcommand. The no-auth assertion filters the recorded head Vec for any line starting with "authorization:" and asserts the result is empty.
2. **tests/help_examples_test.rs**: append `docs_help_has_examples`: `pipelite docs --help` succeeds; stdout contains "Examples:", "--save", and "ignored".
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo test --test docs_stub_test 2>&1 | tail -5 && test "$(grep -c '#\[test\]' tests/docs_stub_test.rs)" -ge 8 && test "$(grep -c 'docs --help' tests/help_examples_test.rs)" -ge 1 && echo GATE-OK && cargo test 2>&1 | tail -4</automated>
  </verify>
  <done>
    Eight integration tests prove DOCS-01: the wire request carries no Authorization header (raw-head assertion), pretty JSON reaches stdout, --save handles parent dirs/overwrite refusal/force/quiet with a pre-HTTP refusal, both docs error shapes carry the locked hint with server detail preserved, and --format is inert. Full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server | The CLI's API key must NOT ride along on requests to the public docs route |
| server→client | Untrusted 404/500 bodies (and the spec itself) are parsed and rendered |
| user→client | --save writes to a user-specified filesystem path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Information Disclosure | API key leaked to the public docs endpoint via shared-client default headers | mitigate | Dedicated headerless reqwest::Client inside get_docs (grep gate: no self.client in body); stub test asserts the raw request head has no authorization line — the contract is tested on the wire, not assumed |
| T-09-09 | Tampering | --save clobbering an existing file | mitigate | Exists-check + exit-2 refusal unless --force, before any HTTP; parent dirs created only beneath the explicitly passed path; refusal tested with a sentinel file |
| T-09-10 | Information Disclosure | Spec/error bodies rendered with hostile content | mitigate | Output paths use to_string_pretty on parsed Value or parse_rfc7807 as_str detail — no raw byte dumps, no unwrap paths |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new crates — reqwest/tempfile already pinned; no legitimacy checkpoint required |
</threat_model>

<verification>
- `cargo test` full suite passes including the 8 new docs_stub tests and all pre-existing suites
- The docs request head contains no authorization line (wire-level proof, not code inspection alone)
- `pipelite docs` pretty-prints the spec; `--save` creates parent dirs, refuses existing files without --force pre-HTTP, and prints the byte-count confirmation (quiet-suppressed)
- A 404 docs response renders the locked "may not expose the docs endpoint" hint with the server's detail; the legacy 500 renders "OpenAPI specification not available" unquoted
- `--format` is documented as ignored and demonstrably inert
- No unwrap()/expect() in production code; docs never touches the cache
</verification>

<success_criteria>
- DOCS-01: the server's OpenAPI 3.1 spec is fetchable via `pipelite docs [--save FILE]` without authentication — ROADMAP SC-5
- All three Phase 9 surfaces share the established conventions (after_help examples, hint-bearing errors, global-flag respect) that Phase 13 will cross-verify
</success_criteria>

<output>
After completion, create `.planning/phases/09-workflow-runs-templates-docs/09-03-SUMMARY.md`
</output>
