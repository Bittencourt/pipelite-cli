---
phase: 03-full-entity-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/models.rs
  - src/api/mod.rs
  - src/cli/mod.rs
  - src/cli/orgs.rs
  - src/cli/people.rs
  - src/commands/mod.rs
  - src/commands/orgs/mod.rs
  - src/commands/orgs/list.rs
  - src/commands/orgs/get.rs
  - src/commands/orgs/create.rs
  - src/commands/orgs/update.rs
  - src/commands/orgs/delete.rs
  - src/commands/people/mod.rs
  - src/commands/people/list.rs
  - src/commands/people/get.rs
  - src/commands/people/create.rs
  - src/commands/people/update.rs
  - src/commands/people/delete.rs
  - src/main.rs
  - tests/orgs_integration.rs
  - tests/people_integration.rs
autonomous: true
requirements:
  - ORG-01
  - ORG-02
  - ORG-03
  - ORG-04
  - ORG-05
  - PEOP-01
  - PEOP-02
  - PEOP-03
  - PEOP-04
  - PEOP-05

must_haves:
  truths:
    - "User can run `pipelite orgs list` and see organizations in table format"
    - "User can run `pipelite orgs get <id>` and see full org details"
    - "User can create, update, and delete organizations via CLI flags"
    - "User can run `pipelite people list` and see people in table format"
    - "User can run `pipelite people get <id>` and see full person details"
    - "User can create, update, and delete people via CLI flags"
    - "Both entities support --stdin batch create using /batch API endpoint"
  artifacts:
    - path: "src/api/models.rs"
      provides: "Organization, Person structs + Create/Update payloads + table configs"
      contains: "struct Organization"
    - path: "src/cli/orgs.rs"
      provides: "OrgsCommands enum with List/Get/Create/Update/Delete"
      exports: ["OrgsCommands"]
    - path: "src/cli/people.rs"
      provides: "PeopleCommands enum with List/Get/Create/Update/Delete"
      exports: ["PeopleCommands"]
    - path: "src/commands/orgs/mod.rs"
      provides: "Org command dispatch"
    - path: "src/commands/people/mod.rs"
      provides: "People command dispatch"
  key_links:
    - from: "src/main.rs"
      to: "src/commands/orgs/mod.rs"
      via: "Commands::Orgs match arm"
      pattern: "Commands::Orgs"
    - from: "src/main.rs"
      to: "src/commands/people/mod.rs"
      via: "Commands::People match arm"
      pattern: "Commands::People"
    - from: "src/commands/orgs/create.rs"
      to: "src/api/mod.rs"
      via: "client.create_org() and client.batch_create_orgs()"
      pattern: "create_org|batch_create_orgs"
    - from: "src/commands/people/create.rs"
      to: "src/api/mod.rs"
      via: "client.create_person() and client.batch_create_people()"
      pattern: "create_person|batch_create_people"
---

<objective>
Implement full CRUD for organizations and people entities, replicating the proven deals pattern.

Purpose: These two entities both have /batch API endpoints (like deals), so they share the same batch-create pattern. Implementing them together keeps the batch-capable entities in one plan.
Output: Working `pipelite orgs` and `pipelite people` commands with list, get, create, update, delete subcommands plus integration tests.
</objective>

<execution_context>
@/home/pedro/.claude/get-shit-done/workflows/execute-plan.md
@/home/pedro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-full-entity-coverage/03-CONTEXT.md
@.planning/phases/03-full-entity-coverage/03-RESEARCH.md

<interfaces>
<!-- Existing patterns to replicate. Executor should copy-adapt from deals, not reinvent. -->

From src/api/models.rs:
```rust
pub struct ApiSingleResponse<T> { pub data: T }
pub struct ApiListResponse<T> { pub data: Vec<T>, pub meta: PaginationMeta }
pub struct TableConfig { pub default_columns: Vec<&'static str> }
// Pattern: Entity struct + EntityCreate + EntityUpdate + entity_table_config()
```

From src/api/mod.rs:
```rust
// Pattern for list: pub async fn list_deals(&self, params: &DealsListParams) -> Result<ApiListResponse<Deal>>
// Pattern for get:  pub async fn get_deal(&self, id: &str, expand: Option<&[String]>) -> Result<Deal>
//   IMPORTANT: get unwraps ApiSingleResponse<T> -> T.data
// Pattern for create: pub async fn create_deal(&self, data: &DealCreate) -> Result<Deal>
//   IMPORTANT: create unwraps ApiSingleResponse<T> -> T.data
// Pattern for update: pub async fn update_deal(&self, id: &str, data: &DealUpdate) -> Result<Deal>
// Pattern for delete: pub async fn delete_deal(&self, id: &str) -> Result<()>
// Pattern for batch: pub async fn batch_create_deals(&self, deals: &[DealCreate]) -> Result<Vec<Deal>>
```

From src/cli/deals.rs:
```rust
// Pattern: #[derive(Subcommand)] enum with List/Get/Create/Update/Delete variants
// Each variant wraps an Args struct
// List args: entity-specific filters + limit/offset/all/fields/expand
// Create args: required fields as Option + --stdin + --custom-field
// Update args: id positional + all fields as Option + --custom-field
// Delete args: id positional only
```

From src/commands/deals/mod.rs:
```rust
pub async fn run(ctx: &AppContext, cmd: &DealsCommands) -> Result<()> {
    match cmd { List(args) => list::run(ctx, args).await, ... }
}
```

From src/cli/mod.rs (Commands enum to extend):
```rust
pub enum Commands {
    Init(InitArgs), Ping, Config(ConfigCommands), Deals(DealsCommands),
    // Add: Orgs(OrgsCommands), People(PeopleCommands)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Organizations entity -- models, API client, CLI, commands, and tests</name>
  <files>src/api/models.rs, src/api/mod.rs, src/cli/orgs.rs, src/cli/mod.rs, src/commands/orgs/mod.rs, src/commands/orgs/list.rs, src/commands/orgs/get.rs, src/commands/orgs/create.rs, src/commands/orgs/update.rs, src/commands/orgs/delete.rs, src/commands/mod.rs, src/main.rs, tests/orgs_integration.rs</files>
  <action>
Implement the organizations entity by copy-adapting the deals pattern:

**1. Data models in src/api/models.rs** -- Add after the Deal section:
- `Organization` struct: id (String), name (String), website (Option<String>), industry (Option<String>), notes (Option<String>), owner_id (String), custom_fields (Option<serde_json::Value>), created_at (String), updated_at (String). Derive Debug, Clone, Serialize, Deserialize.
- `OrganizationCreate` struct: name (String required), website/industry/notes/custom_fields all Option with `skip_serializing_if = "Option::is_none"`.
- `OrganizationUpdate` struct: all fields Option with skip_serializing_if.
- `orgs_table_config()` returning TableConfig with default_columns: ["id", "name", "owner_id", "updated_at"].
- Unit tests: `org_deserializes_from_json_with_nulls`, `org_create_required_only_serializes_without_nulls`.

**2. API client methods in src/api/mod.rs** -- Add to PipeliteClient impl:
- Add Organization, OrganizationCreate, OrganizationUpdate to the use statement from models.
- `list_orgs(&self, params: &OrgsListParams) -> Result<ApiListResponse<Organization>>` -- GET /api/v1/organizations
- `get_org(&self, id: &str, expand: Option<&[String]>) -> Result<Organization>` -- GET /api/v1/organizations/{id}, unwrap ApiSingleResponse
- `create_org(&self, data: &OrganizationCreate) -> Result<Organization>` -- POST /api/v1/organizations, unwrap ApiSingleResponse
- `update_org(&self, id: &str, data: &OrganizationUpdate) -> Result<Organization>` -- PUT /api/v1/organizations/{id}, unwrap ApiSingleResponse
- `delete_org(&self, id: &str) -> Result<()>` -- DELETE /api/v1/organizations/{id}, use handle_delete_response
- `batch_create_orgs(&self, orgs: &[OrganizationCreate]) -> Result<Vec<Organization>>` -- POST /api/v1/organizations/batch
- `OrgsListParams` struct with owner (Option<String>), limit, offset, expand, and `to_query_pairs()` that maps owner to "owner_id".

**3. CLI args in src/cli/orgs.rs** -- New file, copy deals.rs pattern:
- `OrgsCommands` enum: List, Get, Create, Update, Delete with after_help examples using "pipelite orgs" prefix.
- `OrgsListArgs`: --owner filter, --limit (default 50), --offset (default 0), --all, --fields, --expand.
- `OrgsGetArgs`: id positional, --fields, --expand.
- `OrgsCreateArgs`: --name (Option, validated at runtime), --website, --industry, --notes, --custom-field (Vec), --stdin.
- `OrgsUpdateArgs`: id positional, --name, --website, --industry, --notes, --custom-field (Vec).
- `OrgsDeleteArgs`: id positional.

**4. Register in src/cli/mod.rs**:
- Add `pub mod orgs;`
- Add `use orgs::OrgsCommands;`
- Add to Commands enum: `#[command(subcommand, alias = "o")] Orgs(OrgsCommands),`

**5. Command handlers in src/commands/orgs/** -- New directory, 6 files:
- `mod.rs`: dispatch function matching OrgsCommands variants to handlers (same pattern as deals/mod.rs).
- `list.rs`: fetch_page and fetch_all with auto-pagination, using OrgsListParams and orgs_table_config(). Entity-to-values helper.
- `get.rs`: call client.get_org(), render_single with all columns.
- `create.rs`: single_create (validate --name required) and batch_create using client.batch_create_orgs(). Include parse_custom_fields (copy from deals or extract to shared util).
- `update.rs`: build OrganizationUpdate from flags, call client.update_org(), render_single.
- `delete.rs`: call client.delete_org(), print confirmation.

**6. Register in src/commands/mod.rs**: Add `pub mod orgs;`

**7. Wire in src/main.rs**: Add `Commands::Orgs(ref cmd) => { let ctx = AppContext::build(&cli)?; commands::orgs::run(&ctx, cmd).await }` match arm.

**8. Integration tests in tests/orgs_integration.rs** -- Same pattern as deals_integration.rs:
- `orgs_help_shows_subcommands` -- checks list, get, create, update, delete in help
- `orgs_list_help_shows_filter_flags` -- checks --owner, --limit, --offset, --all
- `orgs_create_help_shows_required_flags` -- checks --name, --stdin, --custom-field
- `orgs_get_help_shows_id_argument` -- checks id arg, --fields, --expand
- `orgs_get_without_id_fails_with_exit_code_2`
- `orgs_delete_without_id_fails_with_exit_code_2`
- `orgs_update_help_shows_optional_flags` -- checks --name, --website, --industry, --notes
- `main_help_shows_orgs_subcommand`
  </action>
  <verify>
    <automated>cargo test --test orgs_integration -- --nocapture && cargo test api::models::tests::org -- --nocapture</automated>
  </verify>
  <done>All `pipelite orgs` subcommands are registered (help works), CLI arg parsing is correct (integration tests pass), model serialization tests pass, API client methods compile.</done>
</task>

<task type="auto">
  <name>Task 2: People entity -- models, API client, CLI, commands, and tests</name>
  <files>src/api/models.rs, src/api/mod.rs, src/cli/people.rs, src/cli/mod.rs, src/commands/people/mod.rs, src/commands/people/list.rs, src/commands/people/get.rs, src/commands/people/create.rs, src/commands/people/update.rs, src/commands/people/delete.rs, src/commands/mod.rs, src/main.rs, tests/people_integration.rs</files>
  <action>
Implement the people entity by copy-adapting the deals pattern. IMPORTANT: People have first_name/last_name (not a single "name"), and a computed read-only full_name.

**1. Data models in src/api/models.rs** -- Add after Organization section:
- `Person` struct: id (String), first_name (String), last_name (String), full_name (Option<String>), email (Option<String>), phone (Option<String>), notes (Option<String>), organization_id (Option<String>), owner_id (String), custom_fields (Option<serde_json::Value>), created_at (String), updated_at (String). Derive Debug, Clone, Serialize, Deserialize.
- `PersonCreate` struct: first_name (String required), last_name (String required), email/phone/notes/organization_id/custom_fields all Option with skip_serializing_if.
- `PersonUpdate` struct: all fields Option with skip_serializing_if.
- `people_table_config()` returning TableConfig with default_columns: ["id", "full_name", "email", "organization_id", "updated_at"]. Note: use "full_name" not "name" -- the API returns this computed field.
- Unit tests: `person_deserializes_from_json_with_nulls`, `person_create_required_only_serializes_without_nulls` (should have only first_name and last_name).

**2. API client methods in src/api/mod.rs** -- Add to PipeliteClient impl:
- Add Person, PersonCreate, PersonUpdate to the use statement.
- `list_people(&self, params: &PeopleListParams) -> Result<ApiListResponse<Person>>` -- GET /api/v1/people
- `get_person(&self, id: &str, expand: Option<&[String]>) -> Result<Person>` -- unwrap ApiSingleResponse
- `create_person(&self, data: &PersonCreate) -> Result<Person>` -- unwrap ApiSingleResponse
- `update_person(&self, id: &str, data: &PersonUpdate) -> Result<Person>` -- unwrap ApiSingleResponse
- `delete_person(&self, id: &str) -> Result<()>` -- handle_delete_response
- `batch_create_people(&self, people: &[PersonCreate]) -> Result<Vec<Person>>` -- POST /api/v1/people/batch
- `PeopleListParams` struct with org (Option, maps to "organization_id"), owner (Option, maps to "owner_id"), limit, offset, expand, and `to_query_pairs()`.

**3. CLI args in src/cli/people.rs** -- New file:
- `PeopleCommands` enum: List, Get, Create, Update, Delete with after_help examples.
- `PeopleListArgs`: --org, --owner filters, --limit, --offset, --all, --fields, --expand.
- `PeopleGetArgs`: id positional, --fields, --expand.
- `PeopleCreateArgs`: --first-name (Option), --last-name (Option), --email, --phone, --notes, --org (maps to organization_id), --custom-field (Vec), --stdin.
- `PeopleUpdateArgs`: id positional, --first-name, --last-name, --email, --phone, --notes, --org, --custom-field (Vec).
- `PeopleDeleteArgs`: id positional.

**4. Register in src/cli/mod.rs**:
- Add `pub mod people;`
- Add `use people::PeopleCommands;`
- Add to Commands enum: `#[command(subcommand, alias = "p")] People(PeopleCommands),`

**5. Command handlers in src/commands/people/** -- New directory, 6 files:
- `mod.rs`: dispatch function.
- `list.rs`: fetch_page/fetch_all with PeopleListParams, people_table_config().
- `get.rs`: client.get_person(), render_single.
- `create.rs`: single_create validates --first-name AND --last-name required. batch_create uses client.batch_create_people(). Include parse_custom_fields.
- `update.rs`: build PersonUpdate, client.update_person().
- `delete.rs`: client.delete_person().

**6. Register in src/commands/mod.rs**: Add `pub mod people;`

**7. Wire in src/main.rs**: Add `Commands::People(ref cmd)` match arm.

**8. Integration tests in tests/people_integration.rs**:
- `people_help_shows_subcommands`
- `people_list_help_shows_filter_flags` -- checks --org, --owner, --limit, --offset
- `people_create_help_shows_required_flags` -- checks --first-name, --last-name, --stdin
- `people_get_help_shows_id_argument`
- `people_get_without_id_fails_with_exit_code_2`
- `people_delete_without_id_fails_with_exit_code_2`
- `people_update_help_shows_optional_flags` -- checks --first-name, --last-name, --email, --phone
- `main_help_shows_people_subcommand`
  </action>
  <verify>
    <automated>cargo test --test people_integration -- --nocapture && cargo test api::models::tests::person -- --nocapture</automated>
  </verify>
  <done>All `pipelite people` subcommands are registered (help works), CLI arg parsing is correct (integration tests pass), model serialization tests pass, API client methods compile. Create validates both --first-name and --last-name as required.</done>
</task>

</tasks>

<verification>
- `cargo test` -- all tests pass (existing deals + new orgs + people)
- `cargo build` -- compiles without warnings
- `pipelite --help` shows orgs and people subcommands
- `pipelite orgs --help` shows list/get/create/update/delete
- `pipelite people --help` shows list/get/create/update/delete
</verification>

<success_criteria>
- Organizations and People CRUD fully wired: CLI args -> command handlers -> API client -> models
- Both entities support --stdin batch create via /batch endpoint
- Default table columns match CONTEXT.md decisions (orgs: id, name, owner_id, updated_at; people: id, full_name, email, organization_id, updated_at)
- All integration tests pass
- `cargo test` green
</success_criteria>

<output>
After completion, create `.planning/phases/03-full-entity-coverage/03-01-SUMMARY.md`
</output>
