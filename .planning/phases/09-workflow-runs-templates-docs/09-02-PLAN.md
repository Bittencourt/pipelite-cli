---
phase: 09-workflow-runs-templates-docs
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/cli/workflows.rs
  - src/commands/workflows/runs/detail.rs
  - tests/workflow_runs_stub_test.rs
  - src/api/models.rs
  - src/api/mod.rs
  - src/cache.rs
  - src/cli/templates.rs
  - src/cli/mod.rs
  - src/commands/templates/mod.rs
  - src/commands/templates/list.rs
  - src/commands/templates/get.rs
  - src/commands/templates/create.rs
  - src/commands/templates/delete.rs
  - src/commands/mod.rs
  - src/main.rs
  - tests/templates_stub_test.rs
  - tests/help_examples_test.rs
autonomous: true
requirements: [WRUN-03, TPL-01]

must_haves:
  truths:
    - "User can --watch a run until it reaches a terminal state; polling is a fixed 2s, waiting keeps polling, default exit is 0 on ANY terminal state (even failed), and --exit-status maps failed — plus a defensive cancelled/unknown mapping — to exit 1 (ROADMAP SC-3)"
    - "Watch prints exactly one stderr line per state change (run <id>: X → Y), renders the final state like normal detail, and --quiet suppresses the transition lines"
    - "User can list/get/create/delete workflow templates and correctly finds no update subcommand: `pipelite templates update` exits 2 with the delete-and-recreate hint, and `templates --help` states the server has no template update (ROADMAP SC-4)"
    - "templates create --workflow fetches the workflow and maps triggers[0] → trigger, warns on stderr when the workflow has multiple triggers, and --stdin passes a raw body through verbatim"
    - "templates delete mirrors the workflows delete flow: --dry-run preview first, TTY confirmation, --force bypass, non-interactive refusal without --force (exit 2, zero HTTP)"
  artifacts:
    - path: "src/commands/workflows/runs/detail.rs"
      provides: "Watch loop on runs get: 2s tokio::time::sleep poll, terminal set Completed|Failed|Unknown, stderr transitions, render_detail reuse, exit via watch_exit_code"
      contains: "tokio::time::sleep"
    - path: "src/cli/workflows.rs"
      provides: "--watch and --exit-status flags on WorkflowsRunsGetArgs with after_help documenting the 2s interval and no-timeout semantics"
      contains: "exit_status"
    - path: "src/api/models.rs"
      provides: "WorkflowTemplate (8 verified fields) + WorkflowTemplateCreate (name/description/category/trigger/nodes) + workflow_templates_table_config"
      contains: "pub struct WorkflowTemplate "
    - path: "src/api/mod.rs"
      provides: "list/get/create/delete workflow-templates client methods on surface \"templates\""
      contains: "fn delete_workflow_template"
    - path: "src/cache.rs"
      provides: "KEY_TEMPLATES + TTL_TEMPLATES (3600, mirrors TTL_WORKFLOWS) for completion candidates"
      contains: "KEY_TEMPLATES"
    - path: "src/cli/templates.rs"
      provides: "TemplatesCommands {List, Get, Create, Delete, hidden Update}; after_help carrying the no-update sentence; template id completion candidates"
      contains: "delete and recreate"
    - path: "src/commands/templates/delete.rs"
      provides: "Dry-run intercept → TTY dialoguer confirm → non-interactive exit-2 refusal; single + batch paths; KEY_TEMPLATES invalidation"
      contains: "run_batch_delete"
    - path: "tests/templates_stub_test.rs"
      provides: "TPL-01 integration coverage: renders, triggers[0] mapping + multi-trigger warning, 422 passthrough, confirmation contract, hidden update exit 2"
      contains: "templates update"
  key_links:
    - from: "src/commands/workflows/runs/detail.rs watch loop"
      to: "src/api/mod.rs get_workflow_run"
      via: "each poll fetches detail (status + steps) with both ids"
      pattern: "get_workflow_run\\("
    - from: "src/commands/workflows/runs/detail.rs exit mapping"
      to: "models watch_exit_code + run_status_is_terminal"
      via: "terminal check and exit code come from the unit-tested model helpers, not ad-hoc strings"
      pattern: "watch_exit_code"
    - from: "src/commands/templates/create.rs"
      to: "src/api/mod.rs get_workflow"
      via: "--workflow fetches the workflow, maps triggers.first() → trigger, clones nodes; multi-trigger warning on stderr"
      pattern: "get_workflow\\("
    - from: "src/commands/templates/delete.rs"
      to: "src/batch.rs run_batch_delete"
      via: "multi-ID deletes route through the Phase 7 batch utility (confirmation + summary contract free)"
      pattern: "run_batch_delete"
---

<objective>
Add honest run watching (`--watch`/`--exit-status` polling on `workflows runs get`) and the top-level `templates` command group (list/get/create/delete with the triggers[0]→trigger mapping and the hidden update parse-then-error), proven by scripted stub-server tests.

Purpose: WRUN-03 + TPL-01 — react to run outcomes from scripts, and snapshot workflows into reusable templates. Template create is a CLI-side translation (workflows store a triggers array; templates store one trigger object) — the mapping and its multi-trigger warning are the core correctness risk.
Output: Watch loop + exit-code contract in runs detail, full templates stack (models → API → cache → CLI → handlers), tests/templates_stub_test.rs, extended watch tests, full suite green.
</objective>

<execution_context>
@/home/pedro/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/pedro/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-workflow-runs-templates-docs/09-CONTEXT.md (locked decisions — --watch Semantics, Templates)
@.planning/phases/09-workflow-runs-templates-docs/09-RESEARCH.md (§ Recommended Approach WRUN-03/TPL-01, § Server Contract 3, § Pitfalls 2/3/7, § Code Examples watch loop + mapping)
@.planning/REQUIREMENTS.md (WRUN-03, TPL-01 text)
@.planning/phases/09-workflow-runs-templates-docs/09-01-SUMMARY.md (runs models/detail seam + shared stub helper landed in wave 1)
@CLAUDE.md
@docs/SKILL.md

<interfaces>
<!-- Contracts from 09-01 (wave 1) + existing codebase patterns. -->

From src/commands/workflows/runs/detail.rs (created by 09-01 — the seam this plan extends):
```rust
pub async fn run(ctx: &AppContext, args: &WorkflowsRunsGetArgs) -> Result<()>  // fetch + render_detail
fn render_detail(ctx: &AppContext, args: &WorkflowsRunsGetArgs, detail: &WorkflowRunDetail) -> Result<()>
// Watch reuses render_detail for the final poll; args gains watch: bool, exit_status: bool in this plan.
```

From src/api/models.rs (created by 09-01):
```rust
pub enum WorkflowRunStatus { Pending, Running, Completed, Failed, Waiting, #[serde(other)] Unknown }
pub fn run_status_is_terminal(s: &WorkflowRunStatus) -> bool  // Completed | Failed | Unknown
pub fn watch_exit_code(s: &WorkflowRunStatus, exit_status_flag: bool) -> i32  // Failed|Unknown -> 1 under flag; Completed -> 0
pub struct WorkflowRunDetail { #[serde(flatten)] pub run: WorkflowRun, pub steps: Vec<WorkflowRunStep> }
```

From src/api/mod.rs:
```rust
pub async fn get_workflow(&self, id: &str, expand: Option<&[String]>) -> Result<Workflow>
pub async fn delete_workflow(&self, id: &str) -> Result<()>   // handle_delete_response pattern for template delete (204 -> Ok)
// Workflow.triggers: Option<Vec<serde_json::Value>>, Workflow.nodes: Option<Vec<serde_json::Value>>
```

From src/commands/workflows/delete.rs (the confirmation flow to mirror EXACTLY):
```rust
// run(): collect_delete_ids -> len==1 ? single_delete : batch::run_batch_delete(ctx, "workflow", "workflow(s)", force, &ids, "workflows", KEY_WORKFLOWS, None, async |id| ctx.client.delete_workflow(id).await)
// single_delete(): (1) ctx.dry_run intercept FIRST -> dry_run::render_dry_run_delete("workflow", id, &url, &ctx.output_format, ctx.color)
//   (2) if !force { if stdin.is_terminal() && !ctx.no_input { dialoguer::Confirm...default(false); !confirmed => println "Aborted"; Ok } else { CliError::Validation refusal + "--force" hint } }
//   (3) delete + cache invalidate
// batch.rs: collect_delete_ids(entity, stdin, ids, default_json) ; run_batch_delete(ctx, singular, plural, force, ids, surface, cache_key, None, closure)
```

From src/cache.rs:
```rust
pub const KEY_WORKFLOWS: &str = "workflows";  pub const TTL_WORKFLOWS: u64 = 3600;  // mirror: KEY_TEMPLATES = "templates", TTL_TEMPLATES = 3600
```

From tests/common/mod.rs (created by 09-01 — reuse, do not redefine):
```rust
pub fn cmd() -> Command                                              // unreachable http://127.0.0.1:1 + fake-test-key
pub fn cmd_with_server(url: &str) -> Command
pub fn spawn_head_capturing_stub_server(script: &[(u16, &str)]) -> (String, Arc<AtomicUsize>, Arc<Mutex<Vec<String>>>)
// scripted multi-response IS the polling harness: [(200, running), (200, completed)] — each poll consumes the next connection
```

Verified template wire shapes (RESEARCH § Server Contract 3):
```json
// GET /api/v1/workflow-templates -> {"data":[...],"meta":{...}} ; GET/{id} -> {"data":{...}} ; POST -> 201 {"data":{...}} ; DELETE/{id} -> 204
// item: {"id":"uuid","name":"string","description":"string|null","category":"string|null","trigger":{object},"nodes":[...],"created_at":"ISO"}
// create schema: name 1..200 required; description <=2000 optional; category <=100 optional; trigger REQUIRED object (no inner validation); nodes optional array (server defaults [])
// NO update route exists. Templates are GLOBAL (no ownership scoping) — deletion affects other users; hard delete, double-delete 404s, only 404 possible (never 403).
```
</interfaces>

**Do NOT touch (locked decisions):**
- 2s fixed poll interval — NO flag, documented in help. No timeout — poll until terminal or Ctrl-C (default SIGINT kills the process; the shell reports 130; do NOT add tokio's signal feature).
- Terminal set is exactly {completed, failed} (+defensive Unknown). `waiting` is NOT terminal — it polls (steps show resume_at). Do not exit on waiting.
- Default watch exit is 0 even on failed runs; only --exit-status maps failure to 1.
- The hidden Update variant carries the locked hint verbatim: "The server exposes no template update — delete and recreate to change a template".
- templates are top-level (not nested under workflows); create resolves the trigger from EXACTLY ONE source (--workflow | --trigger | --stdin), else exit 2 InvalidInput.
- No client-side trigger schema validation (server zod 422s flow through the Phase 8 parser untouched).
- No unwrap()/expect(); every error carries a hint; respect --dry-run/--no-input/--quiet/--no-color.

**Existing tests that break: NONE (verified).** This plan EXTENDS tests/workflow_runs_stub_test.rs (watch tests) and tests/help_examples_test.rs — both listed in files_modified. help_examples_test asserts only existing command help; adding Templates/Docs-style variants does not affect main-help assertions.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Watch — --watch/--exit-status poll loop with scripted-poll and SIGINT tests</name>
  <files>src/cli/workflows.rs, src/commands/workflows/runs/detail.rs, tests/workflow_runs_stub_test.rs</files>
  <behavior>
    - script [(200 running), (200 running), (200 completed)]: exit 0; exactly 3 requests; final stdout renders the completed detail
    - same script without --quiet: stderr contains "run <id>: running → completed" exactly once (no line for running → running, none for the first observation)
    - same script with --quiet: stderr does NOT contain "→"
    - script [(200 running), (200 failed)]: without --exit-status exit 0; with --exit-status exit 1
    - script [(200 running), (200 waiting), (200 completed)]: exit 0 with 3 requests (waiting polls — not terminal)
    - --watch on an already-terminal run: 1 request, renders detail, exits per mapping (completed → 0)
    - Ctrl-C during watch (Unix): child killed by SIGINT (ExitStatus::signal() == 2; shells report this as exit 130)
  </behavior>
  <action>
1. **src/cli/workflows.rs**: add to WorkflowsRunsGetArgs: `--watch` (bool; doc comment: poll every 2 seconds until the run reaches a terminal state; no timeout — Ctrl-C stops watching, shells report exit 130) and `--exit-status` (bool; doc comment: exit 1 if the run ends failed, else 0). Extend the Get after_help examples with `pipelite workflows runs get run_123 --workflow wf_123 --watch --exit-status`.

2. **src/commands/workflows/runs/detail.rs**: in run(), fetch once; if !args.watch OR run_status_is_terminal(&detail.run.status) → render_detail and return (the non-watch path is untouched). Otherwise loop: print a stderr transition line only when the status changes AND it is not the first observation AND !ctx.quiet — format `run {run_id}: {prev} → {next}` using as_str() values; if the new status is terminal, call render_detail on the fresh detail and `std::process::exit(watch_exit_code(&status, args.exit_status))`; else `tokio::time::sleep(std::time::Duration::from_secs(2)).await` and re-fetch via ctx.client.get_workflow_run(&args.workflow, &args.run_id). Track prev status as String (as_str) initialized empty so the first observation prints nothing. Do NOT install a SIGINT handler — default SIGINT termination is the locked behavior (research A1); a doc comment notes the shell surfaces 130. The waiting case naturally keeps polling because run_status_is_terminal excludes it (09-01 helper).

3. **tests/workflow_runs_stub_test.rs — append the seven tests** from the behavior list using common::spawn_head_capturing_stub_server (each poll consumes one scripted connection; counter proves poll counts). Failed/w detail bodies mirror 09-01 fixtures with status swapped. The SIGINT test: gate `#[cfg(unix)]`; build via common::cmd_with_server(...).args(["workflows","runs","get","run_1","--workflow","wf_1","--watch"]).spawn() (assert_cmd Command::spawn returns std::process::Child); script several running responses so the child is mid-poll; sleep ~300ms; `std::process::Command::new("kill").args(["-INT", &pid.to_string()]).status()`; `child.wait()` and assert `status.signal() == Some(2)` via `std::os::unix::process::ExitStatusExt` with a comment that the shell reports this as 130. Mark the test `#[ignore = "timing-sensitive in CI; verified locally + live UAT"]` ONLY if flaky in practice — first land it unignored.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo test --test workflow_runs_stub_test 2>&1 | tail -5 && test "$(grep -c '#\[test\]' tests/workflow_runs_stub_test.rs)" -ge 17 && test "$(grep -c 'watch' src/cli/workflows.rs)" -ge 3 && test "$(grep -c 'exit_status' src/cli/workflows.rs)" -ge 1 && test "$(grep -c 'tokio::time::sleep' src/commands/workflows/runs/detail.rs)" -eq 1 && test "$(grep -c 'watch_exit_code(' src/commands/workflows/runs/detail.rs)" -eq 1 && test "$(grep -c 'Duration::from_secs(2)' src/commands/workflows/runs/detail.rs)" -eq 1 && echo GATE-OK</automated>
  </verify>
  <done>
    Watch polls the detail endpoint on a fixed 2s interval until Completed/Failed/Unknown, prints one quiet-suppressible stderr transition per change, renders the final state through the shared detail renderer, and exits 0 by default / 1 with --exit-status on failed (defensive cancelled path covered by the 09-01 unit tests). Scripted stubs prove poll counts, waiting non-terminality, and exit codes; SIGINT kills with signal 2 (shell: 130). Full runs suite green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Templates stack — models, client methods, cache, CLI group, handlers</name>
  <files>src/api/models.rs, src/api/mod.rs, src/cache.rs, src/cli/templates.rs, src/cli/mod.rs, src/commands/templates/mod.rs, src/commands/templates/list.rs, src/commands/templates/get.rs, src/commands/templates/create.rs, src/commands/templates/delete.rs, src/commands/mod.rs, src/main.rs</files>
  <action>
1. **src/api/models.rs**: `WorkflowTemplate` with the 8 verified fields (id, name, description: Option<String>, category: Option<String>, trigger: serde_json::Value, `#[serde(default)] nodes: Vec<serde_json::Value>`, created_at; derive Debug, Clone, Serialize, Deserialize) + `WorkflowTemplateCreate` (name: String, description: Option<String>, category: Option<String>, trigger: serde_json::Value, `#[serde(skip_serializing_if = "Option::is_none")] nodes: Option<Vec<serde_json::Value>>`) + `workflow_templates_table_config()` with default_columns id, name, category, created_at (trigger/nodes reachable via --fields or json). Add unit tests: template fixture round-trips (trigger as nested object, nodes defaulting to empty vec when absent from JSON).

2. **src/api/mod.rs** (place the four methods together after the runs methods): `list_workflow_templates(&self, limit: u64, offset: u64) -> Result<ApiListResponse<WorkflowTemplate>>` (GET /api/v1/workflow-templates), `get_workflow_template(&self, id: &str) -> Result<WorkflowTemplate>`, `create_workflow_template(&self, data: &WorkflowTemplateCreate) -> Result<WorkflowTemplate>` (POST, .json body), `delete_workflow_template(&self, id: &str) -> Result<()>` (DELETE via handle_delete_response). All four pass surface `"templates"` (Phase 8 table falls through to the default permission hint; research verified only 404/429 are reachable — global resource, Pitfall 7).

3. **src/cache.rs**: add `pub const KEY_TEMPLATES: &str = "templates";` and `pub const TTL_TEMPLATES: u64 = 3600;` (mirrors TTL_WORKFLOWS — completion candidates only, research A4). No cache logic in runs/docs; templates list caches (id, name) pairs after a successful fetch and create/delete invalidate — same convention as workflows list/delete.

4. **src/cli/templates.rs (NEW)**: `TemplatesCommands` enum with List, Get, Create, Delete variants — plus a FIFTH hidden variant per the Phase 8 hidden-flag pattern (cli/workflows.rs:113-117): `/// [SERVER] No update route exists — delete and recreate to change a template.` `#[command(hide = true)] Update(TemplatesUpdateArgs)` where TemplatesUpdateArgs carries an optional id plus permissive Option fields (name, description, category) so any invocation parses and the handler rejects. Group after_help (on the Templates variant in cli/mod.rs AND reachable via templates --help): "Workflow templates snapshot a workflow's trigger and nodes for reuse — instantiating a template creates a workflow.\n\nThe server exposes no template update — delete and recreate to change a template.\nNote: templates are deployment-global (any valid API key can read or delete them)." List args: --limit (50), --offset (0), --fields. Get args: id positional with `ArgValueCandidates` from a `template_id_candidates()` helper reading KEY_TEMPLATES (copy the workflow_id_candidates pattern), --fields. Create args: `--name` Option<String>, `--workflow` Option<String> (workflow_id_candidates), `--description`, `--category`, `--trigger <json>` (raw object string), `--nodes <json>` (raw array string), `--stdin` (raw JSON body). Delete args: ids positional (required_unless_present = "stdin", template candidates), `--stdin`, `--force`.

5. **src/cli/mod.rs + src/main.rs + src/commands/mod.rs**: `pub mod templates;` in both cli/mod.rs and commands/mod.rs; `Templates(TemplatesCommands)` variant on Commands (after Workflows, with the group after_help above) + use line + dispatch arm `Commands::Templates(ref cmd) => commands::templates::run(ctx, cmd).await`.

6. **src/commands/templates/**: mod.rs dispatches the five variants. list.rs fetches via params, renders through output::render_list with the templates table config, caches (id, name) under KEY_TEMPLATES with TTL_TEMPLATES. get.rs fetches and renders via render_single (all 8 fields as columns, --fields respected). create.rs resolves the body from EXACTLY ONE source, validating BEFORE any template POST: `--stdin` → read stdin, parse to serde_json::Value, POST verbatim (reject if --workflow or --trigger also present — exit 2); `--workflow <id>` → ctx.client.get_workflow, take triggers.first().cloned() (None/empty → CliError::InvalidInput "Workflow {id} has no triggers to snapshot" with hint "Add a trigger to the workflow first, or create the template with --stdin."), warn on stderr when triggers.len() > 1 (suppressed under --quiet): `warning: workflow has {N} triggers; only the first was captured`; build WorkflowTemplateCreate { name: resolved from --name (prompt via prompt::text when missing on a TTY and !ctx.no_input, else MissingInput exit 2), description, category, trigger, nodes: wf.nodes.clone() }; `--trigger <json>` → parse the object, require --name the same way, nodes from --nodes when present; no source → exit 2 with hint listing the three sources. Then POST, print nothing extra (render like other creates: render_single on the created template), invalidate KEY_TEMPLATES on success. delete.rs mirrors commands/workflows/delete.rs EXACTLY: collect_delete_ids("templates", stdin, ids, r#"["tpl_1","tpl_2"]"#); single → dry-run intercept FIRST (dry_run::render_dry_run_delete("workflow template", id, url, ...)), then the TTY dialoguer Confirm `Delete template {id}?` (prompt shows the id; templates are global — the help note covers the shared-resource caveat), non-interactive refusal is CliError::Validation with the `--force` hint (same as workflows), delete, invalidate KEY_TEMPLATES; multi → batch::run_batch_delete(ctx, "template", "template(s)", force, ids, "templates", KEY_TEMPLATES, None, closure).
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo build 2>&1 | tail -3 && test "$(grep -c 'pub struct WorkflowTemplate ' src/api/models.rs)" -eq 1 && test "$(grep -c 'pub struct WorkflowTemplateCreate ' src/api/models.rs)" -eq 1 && test "$(grep -c 'fn list_workflow_templates' src/api/mod.rs)" -eq 1 && test "$(grep -c 'fn get_workflow_template' src/api/mod.rs)" -eq 1 && test "$(grep -c 'fn create_workflow_template' src/api/mod.rs)" -eq 1 && test "$(grep -c 'fn delete_workflow_template' src/api/mod.rs)" -eq 1 && test "$(grep -c 'pub const KEY_TEMPLATES' src/cache.rs)" -eq 1 && test "$(grep -c 'hide = true' src/cli/templates.rs)" -eq 1 && test "$(grep -cF 'delete and recreate' src/cli/templates.rs)" -ge 1 && test "$(grep -c 'Templates(TemplatesCommands)' src/cli/mod.rs)" -eq 1 && test "$(grep -c 'Commands::Templates' src/main.rs)" -eq 1 && test "$(grep -c 'first()' src/commands/templates/create.rs)" -ge 1 && test "$(grep -c 'run_batch_delete' src/commands/templates/delete.rs)" -eq 1 && echo GATE-OK</automated>
  </verify>
  <done>
    Top-level `pipelite templates` group with list/get/create/delete fully wired (Commands enum, dispatch, completion candidates, KEY_TEMPLATES cache lifecycle). Create maps triggers[0] → trigger with the multi-trigger warning and enforces exactly-one trigger source pre-HTTP. Delete mirrors the workflows confirmation flow including batch routing. `templates update` parses (hidden from help) and rejects with the locked exit-2 hint.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Templates integration tests + help examples</name>
  <files>tests/templates_stub_test.rs, tests/help_examples_test.rs</files>
  <behavior>
    - templates list against a stub: exit 0; stdout contains template ids/names
    - templates get tpl_1: exit 0; stdout contains the template name and category
    - templates create --name T --workflow wf_1 (stub: 200 workflow with ONE trigger, then 201 template): exit 0; captured POST body has "trigger" equal to the workflow's triggers[0] (assert the trigger object's distinguishing key/value), contains "name":"T", contains "nodes", and does NOT contain "triggers" (array never leaks into the create payload)
    - same with a TWO-trigger workflow: stderr contains "2 triggers" warning; POST body trigger is still the first
    - workflow with zero triggers: exit 2; stderr contains "--stdin" hint; exactly 1 request (no template POST attempted)
    - echo '{"name":"S","trigger":{"type":"schedule"}}' | templates create --stdin: exit 0; captured POST body equals the stdin JSON semantics (name + trigger present); exactly 1 request (no workflow fetch)
    - templates create 422 with errors[] body: exit 1; stderr contains the joined field message (Phase 8 layer passthrough)
    - templates create with NO source: exit 2 pre-HTTP; stderr names --workflow, --trigger, --stdin
    - templates create --name T --workflow wf_1 --trigger '{"type":"x"}': exit 2 pre-HTTP (two sources); zero requests
    - templates delete tpl_1 --force against 204: exit 0; 1 request
    - templates delete tpl_1 (non-TTY, no --force): exit 2; stderr contains "--force"; 0 requests
    - templates delete tpl_1 --dry-run: stdout contains "/api/v1/workflow-templates/tpl_1"; 0 requests
    - templates update tpl_1 --name X (unreachable server): exit 2; stderr contains "delete and recreate"; stderr lacks "Connection failed" (zero HTTP)
    - templates --help: stdout contains "no template update"; output does NOT advertise an update subcommand
  </behavior>
  <action>
1. **tests/templates_stub_test.rs (NEW)**: `mod common;` and the fourteen tests from the behavior list. Fixtures: workflow body = complete Workflow JSON (id, name, description, triggers array — build one-trigger and two-trigger variants with distinguishing keys like {"type":"crm_event","entity":"deal"} vs {"type":"schedule"}, nodes [{"id":"n1"}], active, created_by, created_at, updated_at); template body = complete 8-field WorkflowTemplate JSON; 201 create response = same template body with name "T"; 422 body mirrors the 08-01 fixture shape with errors[] [{"field":"name","code":"invalid","message":"Template name too long"}]; 204 delete served with an empty body "" (the stub helper handles Content-Length 0). Request-body assertions read the captured heads PLUS bodies — extend usage by asserting on the recorded request head for query checks, and for POST bodies have the test parse the captured connection buffer: spawn_head_capturing_stub_server records heads only, so capture the POST body via the head's Content-Length against a small per-test reading helper — implement a `read_last_request_body` helper inside this test file (reads from the shared recorded head: simplest is to record full requests in common — if 09-01's helper stores only heads, extend tests/common/mod.rs to record the full request bytes in a second shared Vec, keeping 09-01 tests untouched). Assert exit codes with .code(0)/.code(1)/.code(2) explicitly; pre-HTTP tests use common::cmd() and assert stderr lacks "Connection failed".

2. **tests/help_examples_test.rs**: append `templates_help_has_examples` (templates --help succeeds, contains "Examples:", contains "no template update") and `docs_free_after_templates` placeholder NOT needed — only templates assertions here.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo test --test templates_stub_test 2>&1 | tail -5 && test "$(grep -c '#\[test\]' tests/templates_stub_test.rs)" -ge 14 && test "$(grep -c 'templates' tests/help_examples_test.rs)" -ge 1 && echo GATE-OK && cargo test 2>&1 | tail -4</automated>
  </verify>
  <done>
    Fourteen integration tests prove TPL-01 end-to-end: triggers[0] mapping (with the multi-trigger warning and zero-trigger rejection), stdin verbatim create, single-source enforcement pre-HTTP, 422 passthrough, the full delete confirmation contract (force/non-TTY/dry-run), hidden update exit 2 with the locked hint, and truthful help. Full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client | Untrusted workflow/template payloads (trigger objects, nodes) flow through typed models into output |
| user→client | templates delete is irreversible and the resource is deployment-global — confirmation UX is the only gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | templates delete on another user's template (global resource, server does no ownership check) | mitigate | Delete prompt shows the template id, --force is explicit, --dry-run previews the exact URL; help states templates are deployment-global (research Pitfall 7 — server-side fix out of CLI scope) |
| T-09-05 | Repudiation | Silent trigger loss on templates create from multi-trigger workflows | mitigate | stderr warning names the count and the --stdin escape hatch; stub test asserts the warning fires exactly when triggers.len() > 1 |
| T-09-06 | DoS | --watch infinite poll on a stuck (waiting) run | accept | Locked no-timeout decision; documented in --watch help; Ctrl-C exits via default SIGINT |
| T-09-07 | Repudiation | Hidden update subcommand confusing scripts | mitigate | Exit 2 InvalidInput with the delete-and-recreate hint BEFORE any HTTP (parse-then-error, Phase 8 pattern); tested zero-HTTP |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new crates — dialoguer/comfy-table/tokio already pinned; no legitimacy checkpoint required |
</threat_model>

<verification>
- `cargo test` full suite passes: runs suite now ≥17 tests (watch included), templates suite ≥14 tests, plus all pre-existing suites
- `pipelite workflows runs get <run> --workflow <wf> --watch` exits 0 on completed and failed; `--exit-status` yields 1 only on failed; waiting keeps polling (scripted proof)
- `pipelite templates update` → exit 2, delete-and-recreate hint, zero HTTP, hidden from --help
- `templates create --workflow` POST body carries exactly triggers[0] as "trigger" — never a "triggers" array — with the multi-trigger warning
- `templates delete` honors the dry-run→confirm→force ordering; non-interactive refusal is exit 2 with zero HTTP
- No unwrap()/expect() in production code; every new error carries a hint
</verification>

<success_criteria>
- WRUN-03: honest polling watch with the locked exit-code contract — ROADMAP SC-3
- TPL-01: list/get/create/delete templates; `update` correctly does not exist as a real subcommand — ROADMAP SC-4
- Open Question 2 adopted: the hidden-runs probe (09-01) carries the user's --status; watch treats waiting as non-terminal per research verification
</success_criteria>

<output>
After completion, create `.planning/phases/09-workflow-runs-templates-docs/09-02-SUMMARY.md`
</output>
