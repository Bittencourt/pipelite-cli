---
phase: 01-batch-operations-for-all-entities
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/cli/orgs.rs
  - src/cli/people.rs
  - src/cli/activities.rs
  - src/commands/orgs/update.rs
  - src/commands/orgs/delete.rs
  - src/commands/people/update.rs
  - src/commands/people/delete.rs
  - src/commands/activities/update.rs
  - src/commands/activities/delete.rs
autonomous: true
requirements: [BATCH-01, BATCH-02, BATCH-03]

must_haves:
  truths:
    - "pipelite orgs update --stdin processes JSON array of org updates with id fields"
    - "pipelite orgs delete id1 id2 id3 deletes multiple orgs with confirmation"
    - "pipelite people update --stdin processes JSON array of person updates with id fields"
    - "pipelite people delete id1 id2 id3 deletes multiple people with confirmation"
    - "pipelite activities update --stdin processes JSON array of activity updates with id fields"
    - "pipelite activities delete id1 id2 id3 deletes multiple activities with confirmation"
    - "All batch ops use continue-on-error via BatchOutcome"
  artifacts:
    - path: "src/cli/orgs.rs"
      provides: "OrgsUpdateArgs with --stdin, OrgsDeleteArgs with ids Vec and --stdin"
      contains: "required_unless_present"
    - path: "src/cli/people.rs"
      provides: "PeopleUpdateArgs with --stdin, PeopleDeleteArgs with ids Vec and --stdin"
      contains: "required_unless_present"
    - path: "src/cli/activities.rs"
      provides: "ActivitiesUpdateArgs with --stdin, ActivitiesDeleteArgs with ids Vec and --stdin"
      contains: "required_unless_present"
    - path: "src/commands/orgs/update.rs"
      provides: "batch_update function"
      contains: "async fn batch_update"
    - path: "src/commands/people/update.rs"
      provides: "batch_update function"
      contains: "async fn batch_update"
    - path: "src/commands/activities/update.rs"
      provides: "batch_update function"
      contains: "async fn batch_update"
  key_links:
    - from: "src/commands/orgs/update.rs"
      to: "src/batch.rs"
      via: "BatchOutcome::finalize"
      pattern: "batch::BatchOutcome"
    - from: "src/commands/orgs/delete.rs"
      to: "src/batch.rs"
      via: "BatchOutcome::finalize"
      pattern: "batch::BatchOutcome"
    - from: "src/commands/people/update.rs"
      to: "src/batch.rs"
      via: "BatchOutcome::finalize"
      pattern: "batch::BatchOutcome"
    - from: "src/commands/people/delete.rs"
      to: "src/batch.rs"
      via: "BatchOutcome::finalize"
      pattern: "batch::BatchOutcome"
    - from: "src/commands/activities/update.rs"
      to: "src/batch.rs"
      via: "BatchOutcome::finalize"
      pattern: "batch::BatchOutcome"
    - from: "src/commands/activities/delete.rs"
      to: "src/batch.rs"
      via: "BatchOutcome::finalize"
      pattern: "batch::BatchOutcome"
---

<objective>
Replicate the deals batch update and batch delete pattern to orgs, people, and activities.

Purpose: Extend batch operations to 3 more entities following the exact reference implementation from Plan 01.
Output: Batch update + batch delete working for orgs, people, and activities.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-batch-operations-for-all-entities/01-CONTEXT.md
@.planning/phases/01-batch-operations-for-all-entities/01-01-SUMMARY.md

<interfaces>
<!-- Reference implementation from Plan 01 — follow this pattern exactly -->
<!-- Executor MUST read the deals files as the canonical pattern -->

From src/batch.rs (created in Plan 01):
```rust
pub struct BatchOutcome { pub total: usize, pub succeeded: usize, pub failed: usize }
impl BatchOutcome {
    pub fn new(total: usize) -> Self;
    pub fn record_success(&mut self);
    pub fn record_failure(&mut self, index: usize, id: &str, err: &dyn std::fmt::Display);
    pub fn finalize(self, entity_name: &str, operation: &str) -> Result<()>;
}
pub fn read_stdin_json<T: serde::de::DeserializeOwned>(entity_hint: &str) -> Result<Vec<T>>;
```

API client methods:
```rust
// Orgs
pub async fn update_org(&self, id: &str, data: &OrganizationUpdate) -> Result<Organization>;
pub async fn delete_org(&self, id: &str) -> Result<()>;
// People
pub async fn update_person(&self, id: &str, data: &PersonUpdate) -> Result<Person>;
pub async fn delete_person(&self, id: &str) -> Result<()>;
// Activities
pub async fn update_activity(&self, id: &str, data: &ActivityUpdate) -> Result<Activity>;
pub async fn delete_activity(&self, id: &str) -> Result<()>;
```

Cache keys: `KEY_ORGS`, `KEY_PEOPLE`, `KEY_ACTIVITIES`

API URLs: `/api/v1/organizations/{id}`, `/api/v1/people/{id}`, `/api/v1/activities/{id}`

Table configs: `orgs_table_config()`, `people_table_config()`, `activities_table_config()`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Batch update + delete for orgs and people (CLI args + handlers)</name>
  <files>src/cli/orgs.rs, src/cli/people.rs, src/commands/orgs/update.rs, src/commands/orgs/delete.rs, src/commands/people/update.rs, src/commands/people/delete.rs</files>
  <read_first>
    - src/commands/deals/update.rs (reference batch_update implementation from Plan 01)
    - src/commands/deals/delete.rs (reference batch_delete implementation from Plan 01)
    - src/cli/deals.rs (reference CLI args changes from Plan 01)
    - src/cli/orgs.rs (current OrgsUpdateArgs and OrgsDeleteArgs)
    - src/cli/people.rs (current PeopleUpdateArgs and PeopleDeleteArgs)
    - src/commands/orgs/update.rs (current single-update handler)
    - src/commands/orgs/delete.rs (current single-delete handler)
    - src/commands/people/update.rs (current single-update handler)
    - src/commands/people/delete.rs (current single-delete handler)
    - src/api/models.rs (OrganizationUpdate, PersonUpdate struct fields)
  </read_first>
  <action>
Apply the exact same pattern from deals (Plan 01) to orgs and people. For each entity, make these changes:

**CLI Args (src/cli/orgs.rs and src/cli/people.rs):**
- Change `UpdateArgs.id: String` to `id: Option<String>` with `#[arg(required_unless_present = "stdin")]`
- Add `#[arg(long)] pub stdin: bool` to UpdateArgs
- Change `DeleteArgs.id: String` to `ids: Vec<String>` with `#[arg(required_unless_present = "stdin")]`
- Add `#[arg(long)] pub stdin: bool` to DeleteArgs
- Keep all existing ArgValueCandidates annotations on id/ids fields
- Update after_help on Update and Delete subcommands to include batch examples

**Update handlers (src/commands/orgs/update.rs and src/commands/people/update.rs):**
Follow deals/update.rs exactly:
1. Add `use std::io::{self, IsTerminal, Read}; use crate::batch;`
2. Check `args.stdin` at top of `run()`, route to `batch_update(ctx).await`
3. Extract id with `args.id.as_deref().ok_or_else(|| CliError::Validation { detail: "Missing ID".to_string(), hint: "Provide an ID or use --stdin.".to_string() })?` in single-update path. Do NOT use `.unwrap()` — CLAUDE.md forbids it in production code.
4. Add `batch_update` function identical to deals pattern but using:
   - Orgs: `OrganizationUpdate`, `ctx.client.update_org`, `KEY_ORGS`, `orgs_table_config()`, entity_name `"organization"`, API URL `/api/v1/organizations/{id}`
   - People: `PersonUpdate`, `ctx.client.update_person`, `KEY_PEOPLE`, `people_table_config()`, entity_name `"person"`, API URL `/api/v1/people/{id}`

**Delete handlers (src/commands/orgs/delete.rs and src/commands/people/delete.rs):**
Follow deals/delete.rs exactly:
1. Add all necessary imports (batch, CliError, dialoguer, io)
2. Add `collect_ids` function (identical pattern, change hint text to match entity)
3. Add `single_delete` function (extracted from current run)
4. Add `batch_delete` function with confirmation prompt:
   - Orgs: `"Delete {} organization(s)?"`, `ctx.client.delete_org`, `KEY_ORGS`, entity_name `"organization"`
   - People: `"Delete {} person(s)?"` (or `"people"`), `ctx.client.delete_person`, `KEY_PEOPLE`, entity_name `"person"`
5. Update `run()` to call `collect_ids` then route to single_delete or batch_delete
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/orgs.rs contains `pub id: Option<String>` in OrgsUpdateArgs
    - src/cli/orgs.rs contains `pub ids: Vec<String>` in OrgsDeleteArgs
    - src/cli/orgs.rs OrgsUpdateArgs and OrgsDeleteArgs both contain `pub stdin: bool`
    - src/cli/people.rs contains `pub id: Option<String>` in PeopleUpdateArgs
    - src/cli/people.rs contains `pub ids: Vec<String>` in PeopleDeleteArgs
    - src/commands/orgs/update.rs contains `async fn batch_update`
    - src/commands/orgs/update.rs contains `BatchOutcome::new`
    - src/commands/orgs/update.rs uses `ok_or_else` with CliError (NOT unwrap) for args.id extraction
    - src/commands/orgs/delete.rs contains `async fn batch_delete`
    - src/commands/orgs/delete.rs contains `dialoguer::Confirm`
    - src/commands/people/update.rs contains `async fn batch_update`
    - src/commands/people/update.rs uses `ok_or_else` with CliError (NOT unwrap) for args.id extraction
    - src/commands/people/delete.rs contains `async fn batch_delete`
    - `cargo check` succeeds
  </acceptance_criteria>
  <done>
    Orgs and people have working batch update (--stdin) and batch delete (multi-ID + --stdin + confirmation). All use BatchOutcome for continue-on-error. No unwrap() in production code. cargo check passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Batch update + delete for activities (CLI args + handlers)</name>
  <files>src/cli/activities.rs, src/commands/activities/update.rs, src/commands/activities/delete.rs</files>
  <read_first>
    - src/commands/deals/update.rs (reference batch_update implementation)
    - src/commands/deals/delete.rs (reference batch_delete implementation)
    - src/cli/deals.rs (reference CLI args)
    - src/cli/activities.rs (current ActivitiesUpdateArgs and ActivitiesDeleteArgs)
    - src/commands/activities/update.rs (current single-update handler)
    - src/commands/activities/delete.rs (current single-delete handler)
    - src/api/models.rs (ActivityUpdate struct fields)
  </read_first>
  <action>
Apply the exact same pattern from deals to activities:

**CLI Args (src/cli/activities.rs):**
- Change `ActivitiesUpdateArgs.id: String` to `id: Option<String>` with `#[arg(required_unless_present = "stdin")]`
- Add `#[arg(long)] pub stdin: bool` to ActivitiesUpdateArgs
- Change `ActivitiesDeleteArgs.id: String` to `ids: Vec<String>` with `#[arg(required_unless_present = "stdin")]`
- Add `#[arg(long)] pub stdin: bool` to ActivitiesDeleteArgs
- Keep existing ArgValueCandidates annotations
- Update after_help examples

**Update handler (src/commands/activities/update.rs):**
Same pattern as deals. Use `ok_or_else` with `CliError::Validation` (NOT `.unwrap()`) for `args.id` extraction. Entity-specific values:
- `ActivityUpdate`, `ctx.client.update_activity`, `KEY_ACTIVITIES`, `activities_table_config()`
- Entity name: `"activity"`, API URL: `/api/v1/activities/{id}`

**Delete handler (src/commands/activities/delete.rs):**
Same pattern as deals. Entity-specific values:
- `"Delete {} activity(ies)?"` confirmation prompt
- `ctx.client.delete_activity`, `KEY_ACTIVITIES`, entity_name `"activity"`
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo check 2>&1 | tail -5 && cargo test activities 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/activities.rs contains `pub id: Option<String>` in ActivitiesUpdateArgs
    - src/cli/activities.rs contains `pub ids: Vec<String>` in ActivitiesDeleteArgs
    - src/cli/activities.rs both UpdateArgs and DeleteArgs contain `pub stdin: bool`
    - src/commands/activities/update.rs contains `async fn batch_update`
    - src/commands/activities/update.rs contains `BatchOutcome::new`
    - src/commands/activities/update.rs uses `ok_or_else` with CliError (NOT unwrap) for args.id extraction
    - src/commands/activities/delete.rs contains `async fn batch_delete`
    - src/commands/activities/delete.rs contains `dialoguer::Confirm`
    - `cargo check` succeeds
    - `cargo test activities` passes
  </acceptance_criteria>
  <done>
    Activities has working batch update and batch delete. All 4 entities (deals + orgs + people + activities) now support batch operations. No unwrap() in production code. cargo check and cargo test pass.
  </done>
</task>

</tasks>

<verification>
- `cargo check` passes
- `cargo test` passes (all existing tests)
- `pipelite orgs update --help` shows --stdin flag
- `pipelite people delete --help` shows multiple IDs and --stdin
- `pipelite activities update --help` shows --stdin flag
</verification>

<success_criteria>
- Orgs, people, and activities all have batch update (--stdin) and batch delete (multi-ID + --stdin + confirmation)
- All use BatchOutcome for continue-on-error reporting
- Pattern matches deals reference implementation exactly
- No unwrap() in production code
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-batch-operations-for-all-entities/01-02-SUMMARY.md`
</output>
