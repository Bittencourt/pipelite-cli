---
phase: 10-notes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/models.rs
  - src/api/mod.rs
  - src/output/format.rs
  - src/cli/notes.rs
  - src/cli/mod.rs
  - src/commands/mod.rs
  - src/commands/notes/mod.rs
  - src/commands/notes/list.rs
  - src/commands/notes/body.rs
  - src/commands/notes/add.rs
  - src/commands/notes/edit.rs
  - src/commands/notes/delete.rs
  - src/main.rs
  - tests/notes_stub_test.rs
  - tests/help_examples_test.rs
  - docs/api-reference.md
autonomous: true
requirements: [NOTE-01, NOTE-02, NOTE-03, NOTE-04]

must_haves:
  truths:
    - "User can list notes on any deal, organization, person, or activity — newest first (server order), --limit/--offset passthrough, table shows id/created_at/truncated ~80 flattened content, full text via --json (ROADMAP SC-1)"
    - "User can add a note via --body flag, @file, --stdin, or interactive prompt on any of the four note-capable entities; the wire body is exactly {\"content\": ...}; multiple explicit sources and unreadable @file reject pre-HTTP with exit 2 (ROADMAP SC-2)"
    - "User can edit a note by ID (PATCH /api/v1/notes/{noteId} — parent args accepted but unused); no confirmation; --dry-run previews; the prompt/help text explains the server has no single-note GET and points at `notes list <type> <id> --json` (ROADMAP SC-3)"
    - "User can delete a note by ID: --dry-run preview first (never prompts), TTY confirm, --force bypass, non-TTY without --force refuses with exit 1 and zero HTTP (ROADMAP SC-4)"
    - "`notes list pipelines <id>` and `notes get ...` reject with exit 2, actionable locked hints, and zero HTTP; 403 on foreign-note PATCH/DELETE renders the registered notes Forbidden hint (ROADMAP SC-5)"
  artifacts:
    - path: "src/api/models.rs"
      provides: "Note (serializer-exact: id, entity_type SINGULAR string, entity_id, content, author_id Option, source, created_at/updated_at Option; NO deleted_at, NO expanded map) + NoteCreate/NoteUpdate (content only) + notes_table_config"
      contains: "pub struct Note "
    - path: "src/api/mod.rs"
      provides: "list_notes(parent segment, parent_id, limit, offset) + create_note + update_note(note_id) + delete_note(note_id), all on surface \"notes\" (forbidden_hint already registered at line 96)"
      contains: "fn list_notes"
    - path: "src/cli/notes.rs"
      provides: "NotesCommands {List, Add, Edit, Delete, hidden Get} with plural entity-type positional (plain String — NOT clap::ValueEnum), per-subcommand after_help, group after_help stating no-single-GET + valid types"
      contains: "NotesCommands"
    - path: "src/commands/notes/mod.rs"
      provides: "dispatch + resolve_entity_type (4-capable-types map to REST segments, pre-HTTP exit 2) + hidden Get parse-then-error rejection"
      contains: "notes are only available on"
    - path: "src/commands/notes/body.rs"
      provides: "resolve_body: --body (literal/@file/@-) > --stdin > TTY prompt > MissingInput; XOR source check before ANY read (@- + --stdin = two sources)"
      contains: "fn resolve_body"
    - path: "src/commands/notes/delete.rs"
      provides: "templates single_delete contract: dry-run intercept FIRST → TTY Confirm → non-TTY CliError::Validation refusal → delete_note"
      contains: "render_dry_run_delete"
    - path: "tests/notes_stub_test.rs"
      provides: "NOTE-01..04 + SC-5 integration coverage (≥25 tests): wire shapes, body source matrix, zero-HTTP rejections, delete contract, truncation/newline flattening, 422 passthrough"
      contains: "notes are only available on"
  key_links:
    - from: "src/commands/notes/add.rs"
      to: "src/api/mod.rs create_note"
      via: "resolved body posts {\"content\": …} to /api/v1/{segment}/{parent_id}/notes"
      pattern: "create_note\\("
    - from: "src/commands/notes/body.rs"
      to: "fs::read_to_string + stdin"
      via: "@file/@- reads; XOR explicit-source check precedes every read"
      pattern: "read_to_string"
    - from: "src/commands/notes/edit.rs / delete.rs"
      to: "/api/v1/notes/{noteId}"
      via: "PATCH/DELETE URL carries ONLY the note ID — entity-type/parent-id are validated then ignored"
      pattern: "api/v1/notes/"
    - from: "src/api/mod.rs notes methods"
      to: "forbidden_hint table (line 96)"
      via: "handle_response/handle_delete_response with surface \"notes\" — no new registration"
      pattern: '"notes"'
    - from: "src/commands/notes/list.rs"
      to: "output::render_list + truncate_with_ellipsis"
      via: "table-value builder flattens newlines then truncates ~80; json/plain/csv keep full raw text"
      pattern: "truncate_with_ellipsis"
---

<objective>
Add the top-level `notes` command group — list/add/edit/delete on deals, orgs, people, activities (the first parent-scoped sub-resource) — with the locked body-source precedence (--body > @file > --stdin > prompt), pre-HTTP rejection surfaces (non-capable parents, hidden `notes get`, multiple sources, unreadable @file), and the templates delete contract — proven by head+body-capturing stub-server tests.

Purpose: NOTE-01..04 — notes are how users annotate CRM records from scripts and the terminal; this is the first surface where the request path (collection) differs from the mutation path (item-by-note-ID).

Output: Note model triplet, 4 client methods, cli/notes.rs + commands/notes/ (list/add/edit/delete/body/mod), `truncate_with_ellipsis` activated, tests/notes_stub_test.rs (≥25 tests), help truthfulness tests, docs/api-reference.md Notes section. Full suite green.

**Plan-structure check (CONTEXT: "single plan acceptable if small"):** real file count is 16 (13 source + 2 test + 1 doc; wiring files cli/mod.rs, main.rs, commands/mod.rs are 1–3-line touches). That is under the 17-file, 3-task precedent of 09-02 which passed the checker — ONE plan, 3 tasks.
</objective>

<execution_context>
@/home/pedro/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/pedro/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-notes/10-CONTEXT.md (locked decisions — Command Shape & Input Sources, List & Edit UX, Delete & Rejection Surfaces, Models)
@.planning/phases/10-notes/10-RESEARCH.md (§ API-1..4, § Serializer Field Inventory, § Recommended Approach, § Pitfalls 1–8, § Code Examples, § Open Questions)
@.planning/REQUIREMENTS.md (NOTE-01..NOTE-04 text)
@CLAUDE.md
@docs/SKILL.md

<interfaces>
<!-- Contracts verified in-repo. Executor should use these directly — no exploration needed. -->

From src/api/mod.rs (pattern to copy: template client methods ~lines 929-1003):
```rust
pub async fn list_workflow_templates(&self, limit: u64, offset: u64) -> Result<ApiListResponse<WorkflowTemplate>>
    // GET .query(&[("limit", …), ("offset", …)]) -> send_with_retry -> handle_response(response, "templates")
pub async fn create_workflow_template(&self, data: &WorkflowTemplateCreate) -> Result<WorkflowTemplate>
    // POST .json(data) -> ApiSingleResponse<T> unwrap .data
pub async fn delete_workflow_template(&self, id: &str) -> Result<()>
    // DELETE -> handle_delete_response(response, "templates")   // 204 -> Ok
// Notes methods mirror these with surface "notes" — ALREADY in forbidden_hint table (api/mod.rs:96):
//   "notes" => "You can only modify your own notes (or use an admin key)."
```

From src/cli/templates.rs (group pattern) + src/commands/templates/mod.rs (parse-then-error):
```rust
#[derive(Subcommand)] pub enum TemplatesCommands { List(..), Get(..), Create(..), Delete(..),
    /// [SERVER] No update route exists …
    #[command(hide = true)] Update(TemplatesUpdateArgs) }   // permissive args, any invocation parses
// dispatch: TemplatesCommands::Update(_) => Err(CliError::InvalidInput { detail, hint })  // exit 2, zero HTTP
```

From src/commands/templates/delete.rs::single_delete (delete contract to mirror EXACTLY):
```rust
// (1) if ctx.dry_run -> dry_run::render_dry_run_delete(label, id, &url, &ctx.output_format, ctx.color)  FIRST
// (2) if !force { if stdin.is_terminal() && !ctx.no_input { dialoguer::Confirm…default(false); !confirmed => println!("Aborted"); Ok }
//               else { CliError::Validation { detail: "Refusing to delete without confirmation in non-interactive mode.", hint: "Use --force …" } } }
// (3) ctx.client.delete_X(id).await?;   // then quiet-suppressed confirmation println
// ctx.client.base_url() is available for dry-run URL building
```

From src/dry_run.rs:
```rust
pub fn render_dry_run(method: &str, url: &str, body: &serde_json::Value, format: &OutputFormat, color: bool) -> Result<()>
pub fn render_dry_run_delete(label: &str, id: &str, url: &str, format: &OutputFormat, color: bool) -> Result<()>
```

From src/commands/workflows/trigger.rs::parse_data_flag (@file precedent — NOTE: it uses Validation; CONTEXT locks InvalidInput for notes):
```rust
if let Some(path) = s.strip_prefix('@') { fs::read_to_string(path).map_err(|e| …detail: format!("Failed to read file '{path}': {e}")…)? }
```

From src/output/format.rs (currently `#[allow(dead_code)]` at line 92 — REMOVE the attribute):
```rust
pub fn truncate_with_ellipsis(s: &str, max_width: usize) -> String   // char-safe, unit-tested in-file
```

From src/output/mod.rs:
```rust
pub fn render_list(items: &[serde_json::Value], format: &OutputFormat, columns: &[String], fields: &Option<Vec<String>>, color: bool, meta: Option<&PaginationMeta>) -> Result<()>
pub fn render_single(item: &serde_json::Value, format: &OutputFormat, columns: &[String], fields: &Option<Vec<String>>, color: bool) -> Result<()>
```

From src/prompt.rs:
```rust
pub fn check_missing(missing: &[String], usage_hint: &str) -> Result<(), CliError>  // -> MissingInput exit 2
// require_text(&Option<String>, flag_name, label, &mut missing, no_input) — stages/create.rs is the usage pattern
```

From tests/common/mod.rs (reuse, do not redefine):
```rust
pub fn cmd() -> Command                                    // unreachable 127.0.0.1:1 + fake-test-key (pre-HTTP tests)
pub fn cmd_with_server(url: &str) -> Command
pub fn spawn_head_capturing_stub_server(script: &[(u16, &str)]) -> (String, Arc<AtomicUsize>, Arc<Mutex<Vec<String>>>, Arc<Mutex<Vec<String>>>)
// returns (base_url, request_counter, heads(lowercased), full bodies); split a body entry at first "\r\n\r\n" for the payload
```

Verified wire shapes (RESEARCH § Code Examples — stub fixtures):
```json
// GET list 200: {"data":[Note…],"meta":{"total":1,"offset":0,"limit":50}}
// Note: {"id":"n1","entity_type":"deal","entity_id":"d1","content":"hello","author_id":"u1","source":"user","created_at":"2026-09-01T10:00:00.000Z","updated_at":"…"}   // entity_type SINGULAR; author_id nullable; NO deleted_at; no expand map
// POST/PATCH success: 201/200 {"data": Note}
// DELETE: 204 empty body; sequential re-delete -> 404 {"detail":"Note not found"}
// 403: {"type":"…/FORBIDDEN","title":"Forbidden","status":403,"detail":"You don't have access to this resource"}
// 422: {"type":"…/VALIDATION_ERROR","status":422,"detail":"Request validation failed","errors":[{"field":"content","code":"too_small","message":"Note content is required"}]}
// 404 parent: {"detail":"Deal not found"} (label per segment: Deal/Organization/Person/Activity)
```

Entity-type map (CLI plural → REST segment; response `entity_type` stays SINGULAR):
```
deals -> deals | orgs -> organizations | people -> people | activities -> activities
```
</interfaces>

**Open-question resolutions (binding for this plan):**
1. **Empty-list hint: YES** — on empty list print ONE stderr line `No notes on {entity_type} {parent_id} yet — add one with: pipelite notes add {entity_type} {parent_id} --body "<text>"` (exit 0, suppressed by --quiet). NO probe request — research verified notes have no hidden rows server-side (runs-list probe not needed).
2. **entity_id mismatch warning: SKIP (deferred)** — note IDs are UUIDs obtained via `notes list --json`; a mismatched parent surfaces as the server's 404 anyway. No post-response check in edit/delete.
3. **`--body @-` + `--stdin`: TWO explicit sources → exit 2** pre-HTTP (RESEARCH Pitfall 7: both are stdin-backed; the XOR check must run before ANY read to avoid consuming the other's data).

**Do NOT touch / locked decisions:**
- Top-level `notes` group with entity-type positional — NEVER nested ×4 under entities.
- `entity_type` is a plain `String` positional validated in the handler — do NOT use `clap::ValueEnum` (clap's rejection would bypass the locked "notes are only available on…" message).
- No client-side content trim or length cap — server owns validation (422 flows through the Phase 8 parser untouched).
- No cache for notes — no KEY_NOTES, no completion candidates, no invalidation.
- No `--all` flag (server page cap is 100; decision was conditional on a 1000-cap).
- No `--expand` on notes → NO `#[serde(flatten)] expanded` on the model; NO deleted_at field (server omits it).
- Edit: no confirmation (non-destructive); `--dry-run` previews the PATCH. Delete: confirm contract exactly as templates single_delete; single delete only (no batch, no run_batch_delete).
- `--body @-` is equivalent to `--stdin` when alone; together they are two sources → exit 2.
- Dry-run precedes prompt AND network everywhere (add/edit/delete) — --dry-run never prompts.
- Hidden `notes get` hint verbatim: "the server has no single-note GET — use `notes list <type> <id> --json`".
- No unwrap()/expect() in production code; every error carries a hint; respect --dry-run/--no-input/--quiet/--no-color.
- 403 handling: pass surface "notes" to handle_response/handle_delete_response — the hint is ALREADY registered (api/mod.rs:96); no table edits.

**Existing tests that break: NONE (verified).** help_examples_test::main_help_lists_subcommands asserts only init/ping/config presence (line 5–14) — adding the `notes` group does not affect it (same precedent as templates in 09-02). Removing `#[allow(dead_code)]` from truncate_with_ellipsis (output/format.rs:92) activates an in-file unit-tested function; no other attribute exists in that file.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Note model, list_notes client, top-level `notes` group wiring, list handler + rejection surfaces</name>
  <files>src/api/models.rs, src/api/mod.rs, src/output/format.rs, src/cli/notes.rs, src/cli/mod.rs, src/commands/mod.rs, src/commands/notes/mod.rs, src/commands/notes/list.rs, tests/notes_stub_test.rs</files>
  <behavior>
    Unit (models.rs #[cfg(test)]):
    - Note fixture with "entity_type":"deal", author_id null, and NO deleted_at key parses; asserts entity_type == "deal" (singular, Pitfall 1) and author_id.is_none()
    Integration (tests/notes_stub_test.rs — NEW file, 8 tests):
    - list renders table: stub 200 list fixture (1 note) → exit 0; head contains "get /api/v1/deals/d1/notes?limit=50&offset=0"; stdout contains the note id, created_at, and content
    - --limit 10 --offset 5 → head query "limit=10&offset=5"
    - content "line1\nline2" → table stdout contains "line1 line2" (flattened, Pitfall 3)
    - 120-char content → table stdout contains "…" and lacks the final 20 chars; a second invocation with --format json prints the FULL content
    - stub 404 {"detail":"Deal not found"} → exit 1, stderr contains "Deal not found"
    - stub 200 {"data":[],"meta":{"total":0,"offset":0,"limit":50}} → exit 0, stderr contains "No notes"; same with --quiet → stderr lacks "No notes"
    - `notes list pipelines 123` (unreachable server via common::cmd()) → exit 2, stderr contains "notes are only available on", counter == 0, stderr lacks "Connection failed"
    - `notes get deals 123` → exit 2, stderr contains "no single-note GET", counter == 0; `notes --help` stdout does NOT contain "get"
  </behavior>
  <action>
1. **src/api/models.rs**: `Note` struct — serializer-exact per RESEARCH § Field Inventory: id: String, entity_type: String (plain String — singular values arrive from the server; do NOT model a serde enum of plural CLI names), entity_id: String, content: String, author_id: Option<String> with #[serde(default)], source: String, created_at: Option<String> with #[serde(default)], updated_at: Option<String> with #[serde(default)]. NO deleted_at, NO expanded map. Derive Debug, Clone, Serialize, Deserialize (match WorkflowTemplate). Add `notes_table_config() -> TableConfig` with default_columns ["id", "created_at", "content"] next to workflow_templates_table_config. Add the unit tests from the behavior list.

2. **src/api/mod.rs**: one method for now — `pub async fn list_notes(&self, route_segment: &str, parent_id: &str, limit: u64, offset: u64) -> Result<ApiListResponse<Note>>` under a `// -- Notes --` header placed after the templates methods: GET `{base}/api/v1/{route_segment}/{parent_id}/notes` with .query(&[("limit", …), ("offset", …)]), send_with_retry, handle_response(response, "notes") — surface key "notes" (already in forbidden_hint; GET/POST never 403 but the key is correct for future-proofing and the doc comment says so). Doc comment notes: server order createdAt DESC id DESC (newest first, not configurable), page cap 100, no --all by design.

3. **src/output/format.rs**: delete the `#[allow(dead_code)]` line above truncate_with_ellipsis (the function becomes live in step 5).

4. **src/cli/notes.rs (NEW)**: `NotesCommands` enum with List and a hidden Get variant ONLY in this task (Add/Edit/Delete variants + their args are added by Task 2). List(NotesListArgs): entity_type: String positional (doc comment: one of deals, orgs, people, activities), parent_id: String positional, --limit (u64, default 50), --offset (u64, default 0), --fields (Option<Vec<String>>, value_delimiter ','). after_help on List with Examples (plain, --limit/--format json, and the view-before-edit line: `pipelite notes list deals d1 --format json   # view before editing`). Hidden Get(NotesGetArgs): permissive args (entity_type: Option<String>, parent_id: Option<String>, note_id: Option<String>) — parse-then-error pattern; doc comment "[SERVER] No single-note GET exists — list and read --json instead." List args after_help mentions the ~80-char truncated column and that --json shows full text.

5. **src/commands/notes/mod.rs (NEW)**: `run(ctx, cmd)` dispatching List → list::run and Get(_) → Err(CliError::InvalidInput { detail: "The server exposes no single-note GET".into(), hint: "the server has no single-note GET — use `notes list <type> <id> --json`".into() }) — parse-then-error, zero HTTP (templates/mod.rs:23-28 pattern). Add `pub fn resolve_entity_type(entity_type: &str) -> Result<&'static str>`: maps "deals"→"deals", "orgs"→"organizations", "people"→"people", "activities"→"activities"; anything else → CliError::InvalidInput { detail: format!("Notes are not available on '{entity_type}' — notes are only available on deals, orgs, people, activities"), hint: "Valid entity types: deals, orgs, people, activities (e.g. pipelite notes list deals <deal-id>)" }. This is the ONLY gate for the positional — case-sensitive, no clap ValueEnum (Pitfall 6).

6. **src/commands/notes/list.rs (NEW)**: run() calls resolve_entity_type, then ctx.client.list_notes(segment, &args.parent_id, args.limit, args.offset). Convert notes to Values via a local `notes_to_values(&[Note], truncate: bool)` — when truncate: replace '\n' and '\r' with a space in content, then truncate_with_ellipsis(content, 80); when !truncate: full raw text. Pass truncate = true ONLY for OutputFormat::Table (json/plain/csv keep full raw content — the agent's-discretion call: plain is a machine-readable format like json). Render via output::render_list(&values, &ctx.output_format, &columns-from-notes_table_config, &args.fields, ctx.color, Some(&response.meta)). Empty list → the resolved open-question-1 stderr hint (skip when ctx.quiet), still exit 0 (pattern: workflows/runs/list.rs:44-69 minus the probe).

7. **Wiring**: `pub mod notes;` in src/cli/mod.rs and src/commands/mod.rs; `Notes(NotesCommands)` variant on Commands after Templates with group after_help: "Notes annotate deals, organizations, people, and activities.\nThe server exposes no single-note GET — use `notes list <type> <id> --json` to read a note before editing.\n\nExamples:\n  pipelite notes list deals d1\n  pipelite notes add deals d1 --body \"Followed up\"" (+ use line). src/main.rs: dispatch arm `Commands::Notes(ref cmd) => commands::notes::run(ctx, cmd).await` next to the Templates arm (main.rs:98 pattern).

8. **tests/notes_stub_test.rs (NEW — Wave 0 gap closed here)**: `mod common;` + the 8 tests from the behavior list. Fixtures mirror the interface block's wire shapes. Heads are lowercased by the stub — assert lowercase path/query substrings. Pre-HTTP tests use common::cmd() and assert BOTH the exit code and the absence of "Connection failed" plus counter == 0 where a server exists. Use `.code(0/.1/.2)` explicitly.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo test --test notes_stub_test 2>&1 | tail -5 && test "$(grep -c '#\[test\]' tests/notes_stub_test.rs)" -ge 8 && cargo test --bin pipelite 2>&1 | tail -3 && test "$(grep -c 'pub struct Note ' src/api/models.rs)" -eq 1 && test "$(grep -c 'fn notes_table_config' src/api/models.rs)" -eq 1 && test "$(grep -c 'fn list_notes' src/api/mod.rs)" -eq 1 && test "$(grep -c 'NotesCommands' src/cli/mod.rs)" -ge 2 && test "$(grep -c 'Commands::Notes' src/main.rs)" -eq 1 && test "$(grep -c 'allow(dead_code)' src/output/format.rs)" -eq 0 && test "$(grep -c 'truncate_with_ellipsis' src/commands/notes/list.rs)" -ge 1 && test "$(grep -c 'notes are only available on' src/commands/notes/mod.rs)" -ge 1 && test "$(grep -c 'no single-note GET' src/commands/notes/mod.rs)" -ge 1 && echo GATE-OK</automated>
  </verify>
  <done>
    `pipelite notes list <deals|orgs|people|activities> <parent-id>` renders id/created_at/truncated flattened content (full text via --json), passes --limit/--offset to the wire, prints the quiet-suppressible empty hint, and maps parent 404s to NotFound exit 1. Non-capable types and the hidden `notes get` reject exit 2 with the locked hints and zero HTTP. Note model matches the serializer exactly (singular entity_type, nullable author_id, no deleted_at). Note unit + 8 stub tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Body resolver + add/edit/delete handlers (NOTE-02/03/04)</name>
  <files>src/commands/notes/body.rs, src/commands/notes/add.rs, src/commands/notes/edit.rs, src/commands/notes/delete.rs, src/commands/notes/mod.rs, src/cli/notes.rs, src/api/models.rs, src/api/mod.rs, tests/notes_stub_test.rs</files>
  <behavior>
    Unit (models.rs #[cfg(test)]):
    - NoteCreate { content } serializes to a JSON object with EXACTLY one key "content" (Pitfall 2); same for NoteUpdate
    Integration (append 17 tests to tests/notes_stub_test.rs):
    - add --body "hello from flag" → head "post /api/v1/deals/d1/notes"; captured body parses to {"content":"hello from flag"} (exactly one key); 201 fixture → exit 0, stdout contains created id
    - add --body @tmpfile (tempfile crate is a dev-dep) → body content equals file text
    - add --body @/nonexistent/nope.md → exit 2, stderr contains "Check that the file path", counter == 0, stderr lacks "Connection failed"
    - add --stdin with write_stdin → body content equals stdin data
    - add --body @- with write_stdin → body content equals stdin data
    - add --body x --stdin → exit 2, counter == 0
    - add --body @- --stdin (write_stdin supplied) → exit 2, counter == 0 (open question 3)
    - add --no-input (no source) → exit 2 MissingInput, stderr contains "--body", counter == 0
    - add --dry-run → stdout contains "POST" and the URL and the content, counter == 0
    - edit deals d1 n1 --body "new text" → head "patch /api/v1/notes/n1"; head does NOT contain "/deals/"; body {"content":"new text"}; 200 fixture → exit 0, stdout contains "Updated note n1"
    - edit 403 fixture → exit 1, stderr contains "You can only modify your own notes"
    - edit --body "   " with stub 422 errors[] fixture → exit 1, stderr contains "Note content is required" (whitespace-only passes through to the server — no client trim, locked)
    - edit --dry-run → stdout contains "PATCH" and "/api/v1/notes/n1", counter == 0
    - delete deals d1 n1 --force with stub 204 "" → exit 0, counter == 1, head "delete /api/v1/notes/n1"
    - delete deals d1 n1 (non-TTY, no --force) → exit 1, stderr contains "--force", counter == 0
    - delete --dry-run → stdout contains "/api/v1/notes/n1", counter == 0
    - delete→delete script [(204,""),(404,"Note not found")] against one server: first run exit 0, second run exit 1 with stderr "Note not found" (sequential re-delete is 404 — Pitfall 5)
  </behavior>
  <action>
1. **src/commands/notes/body.rs (NEW)**: `pub fn resolve_body(body: &Option<String>, stdin: bool, no_input: bool, prompt_context: Option<(&str, &str)>) -> Result<String>` implementing the locked precedence. Order of operations: (a) count explicit sources — body.is_some() and stdin each count; two or more → CliError::InvalidInput { detail: "Multiple note body sources given (--body and --stdin)", hint: "Pass exactly one: --body <text|@file>, --body @- (reads stdin), or --stdin." } — this check runs BEFORE any read so @- and --stdin never both consume stdin (Pitfall 7). (b) Some(body): if it strip_prefix('@') → "-" means read stdin to end via std::io::read_to_string, else fs::read_to_string(path) mapped to CliError::InvalidInput { detail: format!("Failed to read file '{path}': {e}"), hint: "Check that the file path is correct and the file exists." } (trigger.rs:61-82 shape but InvalidInput per the CONTEXT lock — NOT Validation). (c) stdin flag → read stdin to end. (d) neither: if the input is a TTY && !no_input && prompt_context is Some — dialoguer::Input for the note text (label for edit carries the locked wording: "New note content (the server offers no single-note GET — run `notes list <type> <id> --json` to view existing content first)"); otherwise → CliError::MissingInput { detail: "No note body provided", hint: "Provide --body <text>, --body @file, --body @-, or --stdin (or run interactively without --no-input)." } (check_missing/prompt.rs pattern). No trimming, no length check — server-owned (locked).

2. **src/api/models.rs**: add `NoteCreate { content: String }` and `NoteUpdate { content: String }` (derive Debug, Clone, Serialize, Deserialize) with the one-key serialization unit test. NO other fields — the server reads nothing else (authorId/source are server-forced; sending them is dead weight).

3. **src/api/mod.rs**: three more methods in the `// -- Notes --` section: `create_note(&self, route_segment: &str, parent_id: &str, data: &NoteCreate) -> Result<Note>` (POST `{base}/api/v1/{segment}/{parent_id}/notes` .json(data), unwrap ApiSingleResponse, surface "notes"); `update_note(&self, note_id: &str, data: &NoteUpdate) -> Result<Note>` (PATCH `{base}/api/v1/notes/{note_id}` — ONLY the note id in the URL); `delete_note(&self, note_id: &str) -> Result<()>` (DELETE same URL, handle_delete_response(response, "notes")). Doc comments state the 403 author-or-admin semantics on PATCH/DELETE and soft-delete 204 (re-delete → 404).

4. **src/cli/notes.rs**: extend NotesCommands with Add(NotesAddArgs), Edit(NotesEditArgs), Delete(NotesDeleteArgs) — each with entity_type: String + parent_id: String positionals (grammar-locked, validated then unused by edit/delete requests), edit/delete add note_id: String positional. Add args: --body Option<String> (doc: "Note text, @filepath to read from a file, or @- to read stdin") and --stdin flag. Edit args: same two source flags. Delete args: --force flag. after_help examples per subcommand: add (flag form, @file form, stdin pipe), edit (`notes list <type> <id> --format json` view-before-edit example + edit), delete (plain, --force, --dry-run). Update the group after_help in cli/mod.rs if needed to stay truthful (it already lists list/add examples from Task 1 — extend with edit/delete lines).

5. **src/commands/notes/add.rs (NEW)**: run() → resolve_entity_type → resolve_body(args.body.as_ref(), args.stdin, ctx.no_input, prompt context ("Add note", wording: "Note content")) — BUT dry-run needs the resolved body for the preview, so resolution happens BEFORE the dry-run intercept; then if ctx.dry_run → dry_run::render_dry_run("POST", &url, &json!({"content": body}), &ctx.output_format, ctx.color) and return (zero HTTP, never prompts — resolve_body's prompt step must be skipped under dry_run: pass no_input = ctx.no_input || ctx.dry_run into resolve_body so a dry-run with no explicit source previews an empty-body rejection instead of prompting; doc comment explains). Then ctx.client.create_note(segment, &args.parent_id, &NoteCreate { content: body }); success: --format json → render_single of the created note (columns from notes_table_config), else unless ctx.quiet println!("Created note {id} on {entity_type} {parent_id}", id = note.id).

6. **src/commands/notes/edit.rs (NEW)**: resolve_entity_type (grammar validation; the value is otherwise unused — open question 2: no entity_id mismatch warning), resolve_body with the edit prompt wording, dry-run intercept (render_dry_run("PATCH", note-url, body)), ctx.client.update_note(&args.note_id, &NoteUpdate { content }), success line "Updated note {id}" (quiet-suppressed) or render_single under --format json. No confirmation (locked).

7. **src/commands/notes/delete.rs (NEW)**: copy templates/delete.rs::single_delete verbatim-adjusted: resolve_entity_type first; dry-run intercept FIRST → dry_run::render_dry_run_delete("note", note_id, &url, &ctx.output_format, ctx.color); TTY dialoguer::Confirm "Delete note {id}?" default(false), "Aborted" on decline; non-TTY && !force → CliError::Validation { detail: "Refusing to delete without confirmation in non-interactive mode.", hint: "Use --force to skip confirmation: pipelite notes delete <type> <parent-id> <note-id> --force" } (exit 1, zero HTTP); then ctx.client.delete_note(&args.note_id) (no cache invalidation — no notes cache); quiet-suppressed "Deleted note {id}". No batch path — single delete only (locked).

8. **src/commands/notes/mod.rs**: extend the dispatch with Add/Edit/Delete arms; edit/delete handlers call resolve_entity_type too, so the rejection surfaces stay consistent across the group.

9. **tests/notes_stub_test.rs**: append the 17 tests from the behavior list. Stdin tests: assert_cmd `.write_stdin(...)`. Tempfiles via the tempfile dev-dep. Body assertions: take the LAST captured body entry, split at the first "\r\n\r\n", parse as JSON, assert it equals serde_json::json!({"content": …}) — the exact-one-key assertion pins Pitfall 2. The delete→re-delete test: one spawn_head_capturing_stub_server with [(204, ""),(404, note-404-json)], run cmd_with_server twice (each run consumes one scripted connection).
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo test --test notes_stub_test 2>&1 | tail -5 && test "$(grep -c '#\[test\]' tests/notes_stub_test.rs)" -ge 25 && test "$(grep -c 'pub struct NoteCreate' src/api/models.rs)" -eq 1 && test "$(grep -c 'pub struct NoteUpdate' src/api/models.rs)" -eq 1 && test "$(grep -c 'fn resolve_body' src/commands/notes/body.rs)" -eq 1 && test "$(grep -c 'read_to_string' src/commands/notes/body.rs)" -ge 2 && test "$(grep -c 'fn create_note' src/api/mod.rs)" -eq 1 && test "$(grep -c 'fn update_note' src/api/mod.rs)" -eq 1 && test "$(grep -c 'fn delete_note' src/api/mod.rs)" -eq 1 && test "$(grep -c 'api/v1/notes/' src/commands/notes/edit.rs)" -ge 1 && test "$(grep -c 'render_dry_run_delete' src/commands/notes/delete.rs)" -eq 1 && test "$(grep -c 'CliError::Validation' src/commands/notes/delete.rs)" -ge 1 && test "$(grep -c 'is_terminal' src/commands/notes/delete.rs)" -ge 1 && test "$(grep -c 'render_dry_run(' src/commands/notes/add.rs)" -ge 1 && test "$(grep -c 'render_dry_run(' src/commands/notes/edit.rs)" -ge 1 && echo GATE-OK</automated>
  </verify>
  <done>
    `notes add` posts exactly {"content": …} sourced by the locked precedence (flag > @file > @- /--stdin > prompt; every ambiguity rejected exit 2 pre-HTTP; unreadable @file exit 2 + hint); `notes edit` PATCHes /api/v1/notes/{noteId} with no confirmation and the no-single-GET wording in its prompt/help; `notes delete` runs the exact templates delete contract (dry-run first → TTY confirm → --force → non-TTY exit-1 refusal); 403 renders the registered notes hint; whitespace-only bodies reach the server and the 422 errors[] message renders; sequential re-delete 404s as NotFound. 25+ stub tests green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Help truthfulness tests + docs/api-reference.md Notes section + full-suite gate</name>
  <files>tests/help_examples_test.rs, docs/api-reference.md</files>
  <behavior>
    - notes --help: exit 0, stdout contains "Examples:", contains "no single-note GET", contains "--body"; stdout does NOT contain "--all"; does NOT contain "notes get"
    - notes list --help, notes add --help, notes delete --help: each exit 0 with "Examples:" present
    - docs/api-reference.md: a `## \`pipelite notes\`` section exists documenting all four subcommands, the four valid entity types, --body/@file/--stdin precedence, --force, and the no-single-GET note
  </behavior>
  <action>
1. **tests/help_examples_test.rs**: append `notes_help_truthful` (notes --help contains "Examples:" and "no single-note GET" and "--body"; does NOT contain "--all"; does NOT contain "notes get" — the templates_help test at line 106 is the pattern) and `notes_subcommands_have_examples` (loop ["notes","list"], ["notes","add"], ["notes","edit"], ["notes","delete"] each asserting success + "Examples:").

2. **docs/api-reference.md**: insert `## \`pipelite notes\`` before `## Error Codes` (line 369), matching the section style of `## \`pipelite workflows\``. Content: purpose line (notes on deals/orgs/people/activities — the four capable types); subcommand table or list: `notes list <type> <parent-id> [--limit N] [--offset N] [--fields …]` (newest first; table truncates content to ~80 chars — use --format json for full text; no --all: server page cap 100, iterate --offset); `notes add <type> <parent-id> --body <text|@file|@->` (or --stdin; interactive prompt on a TTY; exactly one source; whitespace-only is rejected by the server with 422); `notes edit <type> <parent-id> <note-id> --body …` (no single-note GET — view first with `notes list <type> <id> --format json`); `notes delete <type> <parent-id> <note-id> [--force]` (soft delete; confirmation unless --force; re-deleting a deleted note 404s); the rejection surfaces (notes get → exit 2 hint; non-capable types → exit 2); a note that only the note's author or an admin may edit/delete (403 otherwise). Include one copy-pasteable example block per subcommand (the after_help strings are the source of truth — keep them consistent).

3. Run the FULL suite as the phase gate.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && cargo test 2>&1 | tail -4 && test "$(grep -c 'notes' tests/help_examples_test.rs)" -ge 3 && test "$(grep -c '^## .pipelite notes.' docs/api-reference.md)" -eq 1 && test "$(grep -c 'no single-note GET' docs/api-reference.md)" -ge 1 && test "$(grep -c '@file' docs/api-reference.md)" -ge 1 && test "$(grep -c -- '--force' docs/api-reference.md)" -ge 1 && echo GATE-OK</automated>
  </verify>
  <done>
    Help pages are truthful (no-single-GET stated, no --all, hidden get unadvertised, examples on every subcommand); docs/api-reference.md has a complete Notes section consistent with the after_help strings; FULL cargo test suite green (new notes suites + all pre-existing suites).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client | Untrusted note payloads (free-text content with newlines/control chars) flow through the typed model into table output |
| user→client | `@file` reads an arbitrary user-named path; note delete is user-permanence (soft-deleted rows are unreachable via the API) — confirmation UX is the gate |
| user→server | Foreign-note PATCH/DELETE is denied by the server's author-or-admin gate; the CLI must render, never guess, permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering (display) | list table rendering of multi-line/control-char content | mitigate | notes_to_values flattens \n/\r → space and truncates ~80 in the table builder only; stub test with "line1\nline2" proves one row |
| T-10-02 | Information disclosure / DoS | --body @file reading an arbitrary/huge path | mitigate | Unreadable → InvalidInput exit 2 with a check-the-path hint before any HTTP; no shell interpolation; size intentionally uncapped (locked — server caps content at 200k) |
| T-10-03 | Repudiation | Silent data loss on `notes delete` (soft delete is API-irreversible) | mitigate | Exact templates delete contract: dry-run preview first (never prompts), TTY Confirm, --force explicit bypass, non-TTY refusal exit 1 with zero HTTP — all five behaviors stub-tested |
| T-10-04 | Elevation of privilege | Editing/deleting another user's note | mitigate | Server author-or-admin 403 rendered via handle_response/_delete surface "notes" → the registered hint "You can only modify your own notes (or use an admin key)."; CLI performs no client-side permission guessing; 403 path stub-tested |
| T-10-05 | Repudiation | Hidden `notes get` confusing scripts | mitigate | Parse-then-error: exit 2 InvalidInput with the locked hint BEFORE any HTTP (counter == 0 tested); absent from --help (tested) |
| T-10-SC | Tampering | cargo installs | accept | No new crates — clap/reqwest/serde/dialoguer/tempfile already pinned in Cargo.toml; RESEARCH § Package Legitimacy Audit: nothing to audit, no slopcheck required |
</threat_model>

<verification>
- `cargo test` full suite green: notes_stub_test ≥25 tests, models unit tests, help truthfulness tests, plus all pre-existing suites (templates/help/batch/runs untouched and passing)
- Wire honesty: list GET carries limit/offset query params; add POST body is exactly {"content": …}; edit/delete hit /api/v1/notes/{noteId} with the parent args absent from the URL (head-captured proof)
- Zero-HTTP rejections: non-capable type, hidden get, multiple body sources, unreadable @file, missing body under --no-input, non-TTY delete without --force — each exits with the locked code (2/2/2/2/2/1) and a stub counter of 0 or an unreachable-server run with no "Connection failed"
- Delete ordering: --dry-run previews before any prompt; non-TTY without --force refuses exit 1; sequential re-delete renders the server's 404 "Note not found"
- 403 on PATCH/DELETE renders the pre-registered notes hint — no new registration work in api/mod.rs (grep: forbidden_hint table untouched)
- No unwrap()/expect() in production code; every new error carries a hint; --dry-run/--no-input/--quiet/--no-color respected on every new surface
</verification>

<success_criteria>
- NOTE-01 (ROADMAP SC-1): list on all four capable types — newest-first server order, --limit/--offset passthrough (no --all), id/created_at/truncated columns, full text via --json
- NOTE-02 (SC-2): add via flag, @file, stdin, or TTY prompt; single-source enforcement pre-HTTP; wire body exactly {"content": …}
- NOTE-03 (SC-3): edit by note ID; `notes list <type> <id> --json` documented as the view-before-edit path in after_help, prompt wording, and api-reference (criterion-3 wording)
- NOTE-04 (SC-4): delete by note ID with dry-run → confirm → --force contract
- SC-5: non-capable parents + `notes get` rejected exit 2 with actionable hints, zero HTTP
- Open questions resolved as binding: (1) empty-list stderr hint, quiet-suppressed, no probe; (2) no entity_id mismatch warning; (3) --body @- + --stdin = two sources → exit 2
</success_criteria>

<output>
After completion, create `.planning/phases/10-notes/10-01-SUMMARY.md`
</output>
