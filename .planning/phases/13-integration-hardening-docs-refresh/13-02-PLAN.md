---
phase: 13-integration-hardening-docs-refresh
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - docs/SKILL.md
  - docs/api-reference.md
  - README.md
  - CHANGELOG.md
  - scripts/e2e-v1.1.sh
  - docs/e2e-v1.1-report.md
  - .planning/phases/07-batch-operations-for-all-entities/deferred-items.md
  - .planning/phases/08-foundations-error-layer-models-pagination/deferred-items.md
  - .planning/milestones/v1.0-phases/01-foundation/deferred-items.md
autonomous: false
# autonomous: false — the final task is a checkpoint:human-action: the live E2E
# RUN requires the user's admin credentials (env vars, never committed). The
# agent authors the script + report template; the human/orchestrator executes.
requirements: []
# requirements is [] by design: Phase 13 is cross-cutting VERIFICATION (ROADMAP:
# "None exclusively"). [] passes frontmatter.validate --schema plan (verified).
user_setup:
  - service: pipelite-server
    why: "Live E2E execution (final checkpoint) — script and report are authored without credentials"
    env_vars:
      - name: PIPELITE_SERVER_URL
        source: "User-provided live server URL (session env — NOT in any repo file)"
      - name: PIPELITE_API_KEY
        source: "User-provided ADMIN key (session env — NOT in any repo file; non-admin keys cannot reach audit/purge surfaces)"

must_haves:
  truths:
    - "docs/SKILL.md documents all 9 new surfaces (batch, workflow runs, templates, notes, webhooks, trash, audit, custom-fields, docs) following its existing structure, plus the exit-code contract (0/1/2), hint conventions, and batch/cache/typed-writing patterns (ROADMAP SC-3)"
    - "docs/api-reference.md has a complete section per new group with endpoints + flags, and every documented example's flags verified against the real --help output (SC-3)"
    - "README enumerates the new commands (it has a Commands section — verified in-repo — so per 13-CONTEXT it IS updated)"
    - "CHANGELOG v1.1 is released-ready: full feature list for Phases 7-12, Breaking Changes intact, the custom-fields data-correctness callout present, no 'unreleased' marker"
    - "scripts/e2e-v1.1.sh covers the 4 live checks (batch exit codes under --quiet, webhook show-once secret, trash list→restore round-trip, typed custom-field write verified server-side) using ONLY env-var credentials; docs/e2e-v1.1-report.md template exists with redaction placeholders; the E2E RUN is a human-action checkpoint (SC-2)"
    - "api/mod.rs size decision recorded: 1805 lines < ~2000 threshold → keep single file (STATE.md watch item closed for this milestone)"
    - "All three deferred-items.md files audited: resolved items marked with dates + evidence, still-open items carried forward to the milestone audit"
  artifacts:
    - path: "docs/SKILL.md"
      provides: "Extended skill file: new-surface sections + exit-code contract + hint conventions + batch/cache/typed-writing patterns; stale facts corrected (line counts, cache keys, endpoint table)"
      min_lines: 350
      contains: "Exit codes"
    - path: "docs/api-reference.md"
      provides: "9 complete group sections incl. the 4 missing ones (batch, workflows runs, templates, docs); examples --help-verified"
      contains: "pipelite templates"
    - path: "CHANGELOG.md"
      provides: "Finalized v1.1: Added list covering Phases 7-12, Breaking Changes preserved, data-correctness callout, released-ready header"
      contains: "v1.1"
    - path: "scripts/e2e-v1.1.sh"
      provides: "Live E2E: 4 scenarios on throwaway e2e- records, env-var credentials required, secrets redacted, safe cleanup (never purges)"
      min_lines: 80
      contains: "PIPELITE_API_KEY"
    - path: "docs/e2e-v1.1-report.md"
      provides: "Report template: per-scenario result placeholders, secret shown-once yes/no (redacted), non-admin Forbidden note (stub-covered, no non-admin key available)"
      contains: "## Results"
    - path: ".planning/phases/08-foundations-error-layer-models-pagination/deferred-items.md"
      provides: "Per-item annotations: RESOLVED <date> + evidence, or CARRIED to milestone audit"
      contains: "RESOLVED"
  key_links:
    - from: "docs/SKILL.md"
      to: "src commands"
      via: "every documented group/command matches the real CLI grammar (--help is the source of truth; stale line counts/keys corrected)"
      pattern: "custom-fields"
    - from: "scripts/e2e-v1.1.sh"
      to: "environment credentials"
      via: "reads PIPELITE_SERVER_URL + PIPELITE_API_KEY from env, refuses to run when unset — zero credentials in the committed file (grep gate)"
      pattern: "PIPELITE_API_KEY"
    - from: "docs/e2e-v1.1-report.md"
      to: "ROADMAP SC-2"
      via: "one results section per SC-2 live check: batch exit codes --quiet, show-once secret, trash round-trip, typed write server-side (+ the Phase 12 deferred item closure)"
      pattern: "typed"
---

<objective>
Refresh all documentation to match the shipped v1.1 CLI (SKILL.md, api-reference.md gap audit, README command enumeration, CHANGELOG finalization), close the phase's loose ends (api/mod.rs size decision, deferred-items audit), and author — but not run — the live-server E2E script + report template covering the four cross-cutting SC-2 checks, with execution reserved for the human via env-var credentials.

Purpose: ROADMAP SC-2/SC-3 — the milestone audit that follows this phase reads these artifacts as evidence the milestone holds together; docs that lag the CLI are the last broken window.

Output: updated docs/SKILL.md + docs/api-reference.md + README.md + CHANGELOG.md, scripts/e2e-v1.1.sh + docs/e2e-v1.1-report.md template, annotated deferred-items files, recorded api/mod.rs decision, human-action checkpoint for the E2E run.
</objective>

<execution_context>
@/home/pedro/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/pedro/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-integration-hardening-docs-refresh/13-CONTEXT.md (BINDING: docs refresh scope, README only-if-enumerates, E2E deferral, api/mod.rs decision, deferred-items audit)
@.planning/ROADMAP.md (Phase 13 SC-2/SC-3)
@.planning/STATE.md (Deferred Items table + api/mod.rs watch item + Phase 12 typed-writing/cache decisions to document)
@docs/SKILL.md (READ FIRST — extend, do not rewrite)
@docs/api-reference.md (existing sections: notes, webhooks, trash, audit, custom-fields; verify + fill gaps)
@CHANGELOG.md
@README.md
@CLAUDE.md
@.planning/phases/08-foundations-error-layer-models-pagination/deferred-items.md
@.planning/phases/07-batch-operations-for-all-entities/deferred-items.md

<interfaces>
<!-- Verified in-repo facts the executor needs — no exploration required. -->

**api-reference.md gap audit (pre-verified via section-header grep):**
EXISTING sections: `pipelite notes`, `pipelite webhooks`, `pipelite trash`, `pipelite audit`, `pipelite custom-fields` (+ Typed custom-field writing), Global Options, Error Codes, HTTP Status Mapping.
MISSING sections (add): batch operations (`<entity> update --stdin` array/NDJSON, `<entity> delete` multi-ID/stdin, `--continue-on-error`, exit-code contract, `--force` for non-TTY deletes), `workflows runs` (list/get under `workflows runs`, `--workflow` required, `--status`, `--include-dry-run`, `--watch`, `--exit-status`), `pipelite templates` (list/get/create --workflow/--trigger/--nodes/--stdin, NO update — server has none, delete --force), `pipelite docs` (--save, overwrite refusal, --format accepted-but-ignored, unauthenticated).
Existing-section audit: verify flags/endpoints still match --help (e.g. webhooks --active/--inactive tri-state, trash type aliases, audit 4 filters + limit clamp ≤100, custom-fields --key=name mapping + tombstone note).

**README facts (verified):** HAS `## Commands` (line ~66) with Global Flags table, Entity Management examples per entity, `### Utility Commands` (dashboard/config/cache/completions), and a project-structure listing (~line 480) that omits the new cli/commands dirs → README IS updated (13-CONTEXT condition met).

**CHANGELOG facts (verified):** `## v1.1 (unreleased)` exists with Breaking Changes (Phase 8 dead flags), Changed/Fixed (typed writing data-correctness callout present), Added (RFC 7807, 403, 409), Fixed (stages list, --expand). Missing: feature entries for batch ops, runs/watch, templates, docs, notes, webhooks, trash, audit, custom-fields definitions; released-ready header.

**SKILL.md stale facts (verified):** describes only "7 entity types"; Quick Orientation cites `api/mod.rs ~1078 lines` (actual 1805) and old models.rs count; cache keys list missing `webhooks`, `templates`, `custom_fields_*` (grep src/cache.rs for the true KEY_ set); endpoint table has no batch/runs/notes/webhooks/trash/audit/custom-field-definitions/docs endpoints; no exit-code contract section; no batch/typed-writing patterns.

**api/mod.rs decision (pre-measured):** 1805 lines < ~2000 threshold → KEEP SINGLE FILE. Record in 13-02-SUMMARY.md; the SKILL.md Quick Orientation line-count update (Task 1) reflects current reality.

**deferred-items inventory (3 files, pre-read):**
- v1.0 `01-foundation/deferred-items.md`: flaky config env-var tests (config_path_env_override, env_var_precedence_server_url) — check current status.
- `07-batch/deferred-items.md`: 3 env-dependent tests (deals_list_limit_zero_is_accepted, cli_skeleton config_set + global_flags) — STATE says these went hermetic in Phase 8 → verify + mark.
- `08-foundations/deferred-items.md`: (1) same env-dependent trio, (2) build warnings (WorkflowRunTrigger unused, TTL_WORKFLOWS unused, get_workflows_cached unused; workflows/list.rs total already resolved in 08-02), (3) intermittent config unit-test flake — verify each empirically.

**E2E scenario mechanics (from phase contracts):**
- Batch exit codes under --quiet: create 2 throwaway records → `<entity> update --stdin` (typed or plain) under --quiet → exit 0 + "2 ok, 0 failed"-style summary PRESENT; malformed stdin → exit 2 pre-HTTP; one bad ID + --continue-on-error → exit 1 + "N ok, M failed". Cleanup via batch delete --force.
- Show-once secret: `webhooks create --url https://example.com/e2e --events deal.created` → secret line present exactly once, full 64 chars untruncated; `webhooks list` + `webhooks get <id>` → secret ABSENT; delete --force. Report records shown-once yes/no with the secret REDACTED.
- Trash round-trip: create throwaway → delete → `trash list --type <t>` finds it → `trash restore <type> <id>` → record fetchable again. Do NOT purge (permanent, admin-only); note the leftover trash entry in the report.
- Typed write server-side (Phase 12 deferred item): ensure `custom-fields create --entity-type orgs --key e2e_price --type number` exists → `orgs create --name e2e-typed-write --custom-field e2e_price=4` → `orgs get <id> --format json` → assert the field is JSON NUMBER 4 (jq: `.e2e_price == 4` and type number, not "4") → cleanup org + definition.
- Non-admin Forbidden checks: SKIPPED live (no non-admin key available per 13-CONTEXT) — report notes stub coverage instead (error_layer_stub_test.rs).
</interfaces>

**Docs authoring rules (BINDING):** every flag/endpooint/example written into docs must be verified against the live `--help` output or src during authoring — a doc that documents a nonexistent flag is the same lie class as the dead flags this milestone removed. Extend docs/SKILL.md in its existing voice/structure; do not rewrite sections that are still accurate.
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/SKILL.md refresh — 9 new surfaces + exit-code contract + batch/cache/typed-writing patterns + stale-fact corrections</name>
  <files>docs/SKILL.md</files>
  <action>
1. READ docs/SKILL.md fully first. Extend — preserve existing accurate sections (Project Identity, Entity CRUD Pattern, Output/Dry-Run/Error patterns, Configuration). DO NOT rewrite sections that are still true.

2. Correct stale facts (verified against src): Project Identity/What-This-Project-Does → mention the new command groups (batch via stdin, workflow runs, templates, notes, webhooks, trash, audit, custom-fields, docs) alongside the 7 entities; Quick Orientation line counts → current reality (`wc -l src/api/mod.rs src/api/models.rs` at authoring time — ~1805 / check models.rs); cache keys list → grep src/cache.rs for `pub const KEY_` and list the true set (incl. webhooks, templates, custom_fields_*); endpoints table → add all new endpoints, derived from src/api/mod.rs method doc comments (batch POST /api/v1/{entity}/batch, GET /api/v1/workflows/{id}/runs + runs detail path, workflow-templates endpoints, notes collection/item routes, webhooks, trash restore/purge, audit, custom-field-definitions, docs spec fetch — copy exact paths from the client methods, do not guess).

3. Add a `## Exit Code Contract` section: 0 = success (incl. batch all-ok, declined-but-safe flows where designed), 1 = item failure or user refusal (Validation: batch --continue-on-error partial failure, standard destructive deletes refused in non-TTY without --force), 2 = structural rejection BEFORE any HTTP (InvalidInput/MissingInput: unknown enums, mutual exclusivity, missing args, trash purge non-TTY refusal, batch malformed input). Include the zero-HTTP guarantee for exit 2 and hint conventions: every error carries an actionable `hint`, pre-HTTP rejections name the offending flag and the valid vocabulary.

4. Add a `## Batch Pattern` section: stdin JSON array or NDJSON via `<entity> update --stdin` / `<entity> delete --stdin|id1 id2`; `--continue-on-error` semantics + the `N ok, M failed` summary that survives `--quiet`; non-TTY deletes require explicit `--force` (batch.rs contract); pre-validation rejects structurally broken input before the first HTTP.

5. Add a `## Custom Fields & Typed Writing Pattern` section: definitions group is the type source; `--custom-field key=value` typed via cached definitions (number i64-first, strict boolean, select option-validated, multi_select arrays, formula refused exit 2, unknown names as strings + one quiet-suppressible warning); `--custom-field-json` verbatim bypass; mutual exclusivity with `--custom-field` and `--stdin` at exit 2 across all 8 handlers; `--dry-run` is cache-only with a visible note; mutations invalidate the `custom_fields_` cache prefix.

6. Add a `## New Surfaces (v1.1)` section (or per-surface subsections in the existing style) covering the 9 groups compactly: batch (→ Batch Pattern), workflows runs (list/get/watch/exit-status + statuses hint + 2s poll), templates (no update — server has none; triggers[0] mapping; --stdin verbatim), notes (top-level group, entity-type positional, body-source precedence --body > @file > --stdin > prompt, no single-note GET), webhooks (13-event validation, show-once secret, https enforced), trash (dual vocabulary 9→4 tabs, restore no-confirm, purge strongest-confirmation exit-2 non-TTY refusal), audit (admin-gated, 4 filters, limit ≤100), custom-fields (→ Typed Writing Pattern), docs (unauthenticated spec fetch, --save overwrite refusal). One or two sentences each + pointer to docs/api-reference.md for full flags — SKILL.md is the agent-orientation file, api-reference is the reference.

7. Update `## Cache System` TTL/keys facts and `## Build & Test` if stale. Keep total addition proportional (~100-150 new lines) — this is an extension, not a rewrite.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && test "$(grep -c '^## ' docs/SKILL.md)" -ge 14 && grep -q 'Exit Code Contract' docs/SKILL.md && grep -q 'Batch Pattern' docs/SKILL.md && grep -qi 'typed writing' docs/SKILL.md && grep -q 'custom_fields' docs/SKILL.md && grep -q 'webhooks' docs/SKILL.md && grep -q 'trash' docs/SKILL.md && grep -q 'templates' docs/SKILL.md && grep -q 'runs' docs/SKILL.md && grep -q 'notes' docs/SKILL.md && grep -q 'audit' docs/SKILL.md && grep -q 'docs' docs/SKILL.md && test "$(grep -c '1078' docs/SKILL.md)" -eq 0 && echo GATE-OK</automated>
  </verify>
  <done>
    docs/SKILL.md documents all 9 new surfaces, the exit-code contract (0/1/2) with hint conventions, and the batch/cache/typed-writing patterns; stale line counts, cache keys, and the endpoint table are corrected against src; existing accurate sections untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: api-reference.md gap audit + README command enumeration + CHANGELOG v1.1 finalization</name>
  <files>docs/api-reference.md, README.md, CHANGELOG.md</files>
  <action>
1. **api-reference.md gap audit (13-CONTEXT binding):** for EACH of the 9 groups verify a section exists with endpoints + flags; add the 4 missing sections (batch, workflows runs, templates, docs — see <interfaces> inventory) following the existing section format (purpose line, endpoint table, per-subcommand flag tables, one copy-pasteable example each, inserted before `## Error Codes`). Re-verify the 5 existing sections against current `--help` (webhooks: 13 events, --active/--inactive; trash: 9→4 aliases, purge exit-2 note; audit: 4 filters, clamp; custom-fields: --key=name mapping, select alias, tombstone note, typed-writing section; notes: body-source precedence) — correct anything drifted.

2. **--help validation pass (mandatory):** for every command documented (new AND audited), run `pipelite <group> [sub] --help` (cargo run) and confirm each documented flag exists and each documented default matches. Keep a checklist in the summary: group → sections verified → drift found/fixed. A flag in docs that --help does not show is a defect — remove or fix it in the same task.

3. **README (it enumerates commands — 13-CONTEXT condition met):** add the new groups to `## Commands`: extend the Workflows block with `workflows runs list/get --watch --exit-status`; add blocks for templates (no update), notes, webhooks, trash, audit, custom-fields (+ one typed `--custom-field price=4` example), docs, and a batch example beyond the existing deals-create one (e.g. `<entity> update --stdin` array + `--continue-on-error` summary). Update the project-structure listing (~line 480) to include the new cli/ and commands/ dirs (webhooks.rs, trash.rs, audit.rs, notes.rs, templates.rs, custom_fields.rs, batch.rs, custom_fields.rs...). Match the existing voice (short code-block examples per group).

4. **CHANGELOG finalization (13-CONTEXT binding):** in `## Added`, add entries for: batch update/delete across all 7 entities (stdin array/NDJSON, --continue-on-error, trustable exit codes + summary); workflow runs list/get with --watch/--exit-status; workflow templates (list/get/create/delete); `pipelite docs` OpenAPI fetch with --save; notes CRUD on 4 entities; webhooks CRUD with show-once signing secret + 13-event validation; trash list/restore/purge; audit log viewer; custom-field definitions CRUD + typed --custom-field writing (cross-reference the existing data-correctness entry in Changed/Fixed — keep that callout prominent, it is the milestone's data-integrity headline). Preserve Breaking Changes verbatim (Phase 8 decisions). Change `## v1.1 (unreleased)` to released-ready wording (`## v1.1 — Server v2 Parity (released 2026-09-04)` — match the file's style; no "unreleased" marker remains). Confirm the intro paragraph still reads true.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && test "$(grep -c '^## `pipelite ' docs/api-reference.md)" -ge 15 && grep -q '^## `pipelite templates`' docs/api-reference.md && grep -q 'workflows runs' docs/api-reference.md && grep -qi 'batch' docs/api-reference.md && grep -q '^## `pipelite docs`' docs/api-reference.md && grep -q 'custom-fields' docs/api-reference.md && test "$(grep -c 'unreleased' CHANGELOG.md)" -eq 0 && grep -q 'custom-fields create' README.md && grep -q 'trash restore\|trash list' README.md && grep -q 'runs list' README.md && echo GATE-OK</automated>
  </verify>
  <done>
    api-reference.md has complete endpoint+flag sections for all 9 new groups (batch, runs, templates, docs added; 5 existing audited against --help) with a documented verification checklist; README enumerates every new group with examples and an updated structure listing; CHANGELOG v1.1 is released-ready with the full Phase 7-12 feature list, Breaking Changes intact, and the data-correctness callout prominent.
  </done>
</task>

<task type="auto">
  <name>Task 3: Loose ends — api/mod.rs decision, deferred-items audit, E2E script + report template</name>
  <files>scripts/e2e-v1.1.sh, docs/e2e-v1.1-report.md, .planning/phases/07-batch-operations-for-all-entities/deferred-items.md, .planning/phases/08-foundations-error-layer-models-pagination/deferred-items.md, .planning/milestones/v1.0-phases/01-foundation/deferred-items.md</files>
  <action>
1. **api/mod.rs size decision (13-CONTEXT binding):** measure `wc -l src/api/mod.rs` (1805 at planning; re-measure at execution) — under the ~2000 threshold → DECISION: keep single file, no split. Record the decision + measurement + date in 13-02-SUMMARY.md (this closes the STATE.md watch item for the milestone audit). Do NOT split the file.

2. **deferred-items audit (3 files, inventory in <interfaces>):** verify each item's CURRENT status empirically, then annotate IN the file: `**RESOLVED 2026-09-04:** <evidence>` or `**CARRIED to v1.1 milestone audit:** <reason>`. Evidence required per item:
   - Env-dependent trio (07 #1, 08 #1): grep the test files/cmd helpers for hermetic HOME handling (`.env("HOME"`); if present, mark RESOLVED with the mechanism named; if absent, run the three named tests once with a real `~/.pipelite/config.toml` present and record the outcome honestly.
   - Build warnings (08 #2): `cargo build 2>&1 | grep -c warning` + `cargo build 2>&1 | grep 'never used\|never constructed'` — mark each named item RESOLVED (gone) or CARRIED (still warned).
   - Config test flake (08 #3 + v1.0 #1): run the named tests 10× (`for i in $(seq 10); do cargo test <name> || echo FAIL; done` style) with a real config present; flake observed → CARRIED with the observed rate; clean → note "10/10 green, watch remains" and CARRY anyway (a 1-in-20 flake needs more runs than 10 to clear — keep it on the audit's radar honestly).
   Roll up the dispositions into 13-02-SUMMARY.md (the milestone audit reads the summary, not three scattered files).

3. **scripts/e2e-v1.1.sh (NEW — author only, do NOT run; execution is Task 4's human checkpoint):** bash with `set -euo pipefail`. Guards FIRST: require `PIPELITE_SERVER_URL` + `PIPELITE_API_KEY` env vars — exit with a usage message if unset (never read creds from files, never embed defaults). Resolve the binary via `cargo build --release && cargo run --release --` or `target/release/pipelite` (state which). Four scenarios (mechanics in <interfaces>):
   (a) batch_exit_codes_quiet — throwaway `e2e-` prefixed records; update-batch under --quiet (exit 0 + summary present), malformed stdin (exit 2), one-bad-ID + --continue-on-error (exit 1 + summary); cleanup via batch delete --force.
   (b) webhook_show_once_secret — create → capture output; assert the secret appears exactly once, untruncated (64 chars); list + get → secret ABSENT from both; delete --force. REDACT the secret before anything reaches the report (record length + shown-once yes/no, never the value).
   (c) trash_round_trip — create throwaway → delete → trash list --type finds it → restore → record fetchable; do NOT purge; note the leftover trash entry.
   (d) typed_write_server_side — ensure definition `e2e_price` (number) on orgs exists (create if missing) → org create --custom-field e2e_price=4 → read back JSON → assert NUMBER 4 (jq type check — the Phase 12 deferred item); cleanup org + definition.
   Structure: per-scenario functions emitting `PASS/FAIL <scenario>: <one-line evidence>` to stdout AND appending to the report file; a `trap` for best-effort cleanup; every assertion prints observed vs expected on failure. Defensive rules: all records prefixed `e2e-`; NEVER call `trash purge`; never echo the API key or webhook secret.

4. **docs/e2e-v1.1-report.md (NEW — template):** header (date placeholder, server URL placeholder, key ADMIN yes/no), one `##` section per scenario with `Result: PASS/FAIL/SKIPPED` + evidence placeholders, a `## Results` summary table, and a `Forbidden (non-admin) checks` section pre-filled: SKIPPED live — no non-admin key available; covered by stubs (error_layer_stub_test.rs) per 13-CONTEXT. Include the note that the filled report is the SC-2 evidence for the milestone audit.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && test -x scripts/e2e-v1.1.sh && bash -n scripts/e2e-v1.1.sh && test "$(grep -c 'PIPELITE_API_KEY\|PIPELITE_SERVER_URL' scripts/e2e-v1.1.sh)" -ge 2 && test "$(grep -ciE 'fake-test-key|pk_[a-z0-9]{8}|whsec_[a-z0-9]{8}' scripts/e2e-v1.1.sh docs/e2e-v1.1-report.md)" -eq 0 && test "$(grep -cE 'pipelite .+trash purge' scripts/e2e-v1.1.sh)" -eq 0 && grep -qi 'never .*(purge|echo)' scripts/e2e-v1.1.sh && grep -q '^## Results' docs/e2e-v1.1-report.md && grep -q 'SKIPPED' docs/e2e-v1.1-report.md && grep -q 'RESOLVED 2026\|CARRIED' .planning/phases/08-foundations-error-layer-models-pagination/deferred-items.md && grep -q 'RESOLVED 2026\|CARRIED' .planning/phases/07-batch-operations-for-all-entities/deferred-items.md && grep -q 'RESOLVED 2026\|CARRIED' .planning/milestones/v1.0-phases/01-foundation/deferred-items.md && echo GATE-OK</automated>
  </verify>
  <done>
    api/mod.rs decision (keep single file, 1805 < 2000) recorded in the summary; all three deferred-items files carry per-item RESOLVED-with-evidence or CARRIED annotations with a summary roll-up; scripts/e2e-v1.1.sh is executable, syntax-clean, env-gated (no embedded credentials — grep-proven), covers the 4 SC-2 scenarios with throwaway-record safety (no purge); docs/e2e-v1.1-report.md template has per-scenario placeholders and the non-admin SKIPPED note.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Run the live E2E (requires your admin credentials) and finalize the report</name>
  <files>docs/e2e-v1.1-report.md</files>
  <action>
HUMAN-ACTION CHECKPOINT — the agent CANNOT do this step: it requires the user's live admin credentials, which live in the session environment, never in repo files. What the agent built (Task 3): scripts/e2e-v1.1.sh (env-gated live E2E) + docs/e2e-v1.1-report.md (template).

The human executes:
1. In the session shell, export the credentials: `export PIPELITE_SERVER_URL=<live server>` and `export PIPELITE_API_KEY=<ADMIN key>`.
2. Run: `bash scripts/e2e-v1.1.sh` (builds release first, ~1-2 min). Expected: four `PASS <scenario>` lines; only `e2e-`-prefixed throwaway records touched; trash purge NEVER invoked; the created webhook deleted; one harmless trash entry left from the round-trip (documented).
3. Fill docs/e2e-v1.1-report.md from the script output — record the webhook secret as "shown once: yes/no (64 chars, redacted)"; NEVER paste the secret or the API key into the report.
4. If any scenario FAILs: paste the scenario's observed-vs-expected line so the agent can triage it as a gap-closure follow-up before the milestone audit.

Resume signal: type "approved" (report filled / all PASS) or paste the failing scenario output.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite-cli && grep -q 'PASS' docs/e2e-v1.1-report.md && ! grep -q 'Result: PASS/FAIL/SKIPPED' docs/e2e-v1.1-report.md && echo GATE-OK || echo 'PENDING — awaiting human E2E run (blocking checkpoint; do not auto-advance)'</automated>
  </verify>
  <done>
    docs/e2e-v1.1-report.md is filled from a real script run: all four scenarios PASS (or failures triaged into gap-closure follow-ups), secret recorded redacted as shown-once yes/no, non-admin Forbidden section notes the stub-coverage rationale — the SC-2 audit evidence exists.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo→git history | Committed files (docs, scripts, report) must never contain the API key or the webhook signing secret — docs are public-adjacent and git history is forever |
| script→live server | The E2E performs REAL mutations (batch delete, webhook create/delete, trash, org create/delete) against production data |
| docs→users | A documented flag that does not exist is the same lie class as the dead flags v1.1 removed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Information disclosure | scripts/e2e-v1.1.sh, docs/e2e-v1.1-report.md | mitigate | Env-var-only credentials with an unset-guard; grep gate proves no credential-shaped literals in committed files; the report records the webhook secret's LENGTH + shown-once yes/no, never its value |
| T-13-02-02 | Destruction (live data) | E2E scenarios | mitigate | Only `e2e-`-prefixed throwaway records; batch deletes target only records the script created; `trash purge` is never invoked (grep-gated); trap-based best-effort cleanup; the trash round-trip leaves a harmless trash entry, documented |
| T-13-02-03 | Repudiation (docs drift) | SKILL.md / api-reference.md / README | mitigate | Mandatory --help validation pass per documented command; drift corrected in-task; checklist recorded in the summary |
| T-13-02-SC | Tampering | cargo installs | accept | No new crates — bash + jq (system) + the existing binary only |
</threat_model>

<verification>
- `grep` gates: SKILL.md ≥14 `##` sections incl. Exit Code Contract + Batch Pattern + typed writing; stale "1078" gone; api-reference.md ≥15 `pipelite` sections incl. the 4 added; CHANGELOG has zero "unreleased"; README enumerates custom-fields/trash/runs
- Every documented flag verified against `--help` (checklist in summary)
- scripts/e2e-v1.1.sh: `bash -n` clean, executable, env-gated, no credential literals (grep-proven), no `trash purge` invocation, four scenario functions
- docs/e2e-v1.1-report.md: per-scenario PASS/FAIL/SKIPPED placeholders + Results table + non-admin SKIPPED note
- deferred-items: all three files annotated RESOLVED-with-evidence or CARRIED; roll-up in summary
- api/mod.rs decision recorded (keep single file) with the re-measured line count
- Live E2E executed by the human (Task 4) with env-var credentials; report filled; failures triaged
</verification>

<success_criteria>
- ROADMAP SC-3: docs/SKILL.md and docs/api-reference.md document every new command group with endpoints and conventions, verified against the real CLI
- ROADMAP SC-2: the four cross-cutting live checks are scripted, safely bounded, and executed via the human checkpoint with the report as audit evidence (incl. the Phase 12 deferred typed-write item closed server-side)
- CHANGELOG released-ready; README current; api/mod.rs watch item decided (single file); deferred items dispositioned for the milestone audit
- No credentials or secrets in any committed file — grep-proven
</success_criteria>

<output>
After completion (post-checkpoint), create `.planning/phases/13-integration-hardening-docs-refresh/13-02-SUMMARY.md` — it MUST include: the api/mod.rs size decision (measurement + keep-single-file), the deferred-items disposition roll-up (resolved vs carried), the docs --help validation checklist, and the E2E outcome (four scenarios + filled report path).
</output>
